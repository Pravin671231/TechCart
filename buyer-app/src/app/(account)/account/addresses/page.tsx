import { AddressListContent } from "@/features/addresses/AddressListContent";

export const metadata = {
  title: "Saved Addresses",
};

export default function AddressesPage() {
  return <AddressListContent />;
}
