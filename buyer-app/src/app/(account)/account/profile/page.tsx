import { ProfileContent } from "@/features/authentication/account/ProfileContent";

export const metadata = { title: "Edit Profile" };

export default function ProfilePage() {
  return <ProfileContent />;
}
