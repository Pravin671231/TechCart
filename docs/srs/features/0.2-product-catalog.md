# SRS v0.2 — Product Catalog

**Status:** Draft — open questions resolved 2026-07-22; full-document approval still pending before moving to milestone/issue breakdown.
**Depends on:** none for buyer-facing read paths; admin write paths are temporarily protected by a shared-secret header until Authentication (v0.3) replaces it with real RBAC — see Dependencies and Resolved Decisions.

---

## 1. Feature Overview

The Product Catalog is the system's source of truth for what's for sale: products organized into categories, browsable and searchable by buyers, fully managed by admins. It's the first feature built on top of the M0/M1 infrastructure and the first to touch MongoDB for real — `backend/src/config/db.ts` is currently a no-op stub, and this feature replaces it with a live Mongoose connection. Every later feature (Cart, Orders, Dashboard) references `products`/`categories`, so this version's data model is effectively load-bearing for the rest of the SRS. The catalog spans heterogeneous categories via category-governed specifications (`categorySpecification`), a first-class brand entity, and per-variant sellable items (`productVariants`) with their own SKU/price/stock.

Scope is deliberately sized for the "small-to-medium catalog store" constraint already fixed in `SRS.md` §2.5: two-level category hierarchy, MongoDB Atlas Search for search/filtering (per `docs/architecture.md` §9's decision, revisit past ~50k SKUs). A product is single-SKU by default; a product needing a size/color matrix instead owns one or more `productVariants`, each an independently priced/stocked/SKU'd sellable item under a shared parent product — see §10 Decision #7.

---

## 2. Functional Requirements

### Buyer-facing browsing, search, filtering

- `FR-CAT-001`: Buyer can view a paginated list of published products on a catalog listing page.
- `FR-CAT-002`: Buyer can view a paginated list of published products filtered to a single category (and, for a parent category, its subcategories).
- `FR-CAT-003`: Buyer can view a single product's detail page by its slug, showing all publicly visible fields (name, description, price, images, category, stock status).
- `FR-CAT-004`: Buyer can search products by keyword; results match against product name and description (case-insensitive, partial match).
- `FR-CAT-005`: Buyer can filter the catalog/search/category listing by a price range (min/max), applied against `price.sellingPrice`.
- `FR-CAT-006`: Buyer can sort listing results by: relevance (search only), price ascending, price descending (both against `price.sellingPrice`), newest first.
- `FR-CAT-007`: Listing/search/category endpoints are paginated with a fixed default page size and a server-enforced maximum page size.
- `FR-CAT-008`: A product with zero stock is still visible in listings and on its detail page, marked "out of stock," and excluded from any "in stock only" filter.
- `FR-CAT-009`: A product with status `draft` or `archived` is never returned by any buyer-facing endpoint, regardless of query parameters.
- `FR-CAT-010`: An empty result set (no matches for a search/filter combination) returns a valid empty response, not an error.

### Admin — product management

- `FR-CAT-011`: Admin (catalog-manager or super-admin) can create a product with: name, SKU, description, price, category, brand, stock quantity, and 1+ images.
- `FR-CAT-012`: SKU is unique across all products and product variants (one shared namespace — see `productVariants`); creating or updating a product or variant with a duplicate SKU is rejected with a validation error.
- `FR-CAT-013`: Product slug is auto-generated from name at creation time; on collision with an existing slug, a numeric suffix is appended to keep it unique.
- `FR-CAT-014`: Admin can update any editable field of an existing product (name, description, price, category, stock, images, status).
- `FR-CAT-015`: Admin can set a product's status to `draft`, `published`, or `archived`.
- `FR-CAT-016`: Admin can delete a product; deletion is a soft delete (status → `archived`), not a hard row delete — later features (Orders) will reference products by ID and must not break on a deleted product.
- `FR-CAT-017`: Admin can view a paginated, searchable, sortable list of all products regardless of status (draft/published/archived all visible in the admin grid).
- `FR-CAT-018`: Price is stored as an object `{ mrp, currency, discount, sellingPrice }`; `mrp` is an integer number of paise and must be > 0; validation rejects zero, negative, non-integer, or non-numeric `mrp` input.
- `FR-CAT-019`: Stock quantity must be a non-negative integer; validation rejects negative or non-integer input.
- `FR-CAT-020`: Admin can adjust a product's stock quantity independently of other fields (a dedicated stock-update path, not just full-record edit).

### Admin — category management

- `FR-CAT-021`: Admin can create a category with: name, and an optional parent category (max depth: one level — a category may have a parent, but a subcategory may not itself have children).
- `FR-CAT-022`: Category slug is auto-generated from name, unique, same collision-handling as `FR-CAT-013`.
- `FR-CAT-023`: Admin can update a category's name or parent.
- `FR-CAT-024`: Admin can delete a category only if it has zero products directly assigned to it and zero subcategories; deletion is rejected with a clear error otherwise (products/subcategories must be reassigned or removed first). A successful deletion also cascades to delete that category's `categorySpecification` definitions.
- `FR-CAT-025`: Admin can view a list of all categories, showing parent/child structure and product count per category.
- `FR-CAT-026`: Buyer-facing endpoints can list all categories (for navigation menus), returning only the parent/child structure — no admin-only fields.

### Images

- `FR-CAT-027`: Admin can upload product/brand images, stored via **Cloudflare R2** (S3-compatible object storage) per `docs/architecture.md`'s system diagram; the API stores the returned object URL(s), not raw image bytes.
- `FR-CAT-028`: A product must have at least 1 and at most 8 images; upload/edit requests violating this bound are rejected.
- `FR-CAT-029`: Uploaded images are restricted to JPEG, PNG, and WebP; other formats are rejected before reaching R2.

### Admin — temporary access control (pre-Authentication)

- `FR-CAT-030`: Every admin endpoint (all product, category, brand, specification, and variant create/update/delete/stock-adjust routes, plus the image-upload presign endpoint) requires a shared-secret header (e.g. `X-Admin-Key`) matching a server-side environment variable, checked by a single reusable Express middleware — not hand-rolled per route.
- `FR-CAT-031`: A request to an admin endpoint with a missing or incorrect shared-secret header is rejected with a 401, following the existing error contract, before it reaches any controller logic.
- `FR-CAT-032`: The shared-secret middleware is written as an isolated, swappable layer — replacing it with real session/role-based auth in v0.3 must not require changes to route paths, controllers, or services, only the guard itself.

### Admin — brand management

- `FR-CAT-033`: Admin can create a brand with name and optional logo/description.
- `FR-CAT-034`: Brand slug is auto-generated from name, unique, same collision-handling as `FR-CAT-013`.
- `FR-CAT-035`: Admin can update a brand's name, logo, or description.
- `FR-CAT-036`: Admin can delete a brand only if zero products of **any** status (draft/published/archived) reference it; rejected with a clear error otherwise.
- `FR-CAT-037`: Admin can view a list of all brands with product count per brand.
- `FR-CAT-038`: Buyer-facing endpoint lists all brands (for filter UI) — public, no admin-only fields.
- `FR-CAT-039`: Product create/update requires a valid `brand` reference; rejected if the brand doesn't exist. Brand is **required** on every product.
- `FR-CAT-040`: Buyer can filter a catalog/search/category listing by brand.

### Admin — category-governed specifications

- `FR-CAT-041`: Admin can define a specification field for a category (name, type [string/number/boolean/enum], unit, options if type=enum, required flag, filterable flag).
- `FR-CAT-042`: Admin can update or delete a category's specification field definition; deleting one currently used by ≥1 product is rejected with a clear error.
- `FR-CAT-043`: Product create/update validates every provided specification value against its own category's specification definitions (correct type, value in `options` if enum, all `required` specs present, unknown keys rejected) and stores it in the type-matched field (`stringValue`/`numberValue`/`booleanValue`) — never a generic `Mixed` value.
- `FR-CAT-044`: Buyer-facing product detail page shows all of a product's specification name/value pairs.
- `FR-CAT-045`: Buyer can filter a category listing by any specification marked `filterable` (e.g. RAM = 8GB), served via an Atlas Search `embeddedDocuments` mapping over `specifications`.

### Admin — product variants (sellable, not tags)

- `FR-CAT-046`: Admin can add a variant to a product: a `sku` (required, globally unique — same uniqueness rule and namespace as `FR-CAT-012`'s product SKU), one or more attribute pairs (`{ name, value, label?, swatch? }`, e.g. `Color=Red` + `Size=L`), an optional set of 0–8 of its own images (`FR-CAT-028`'s count bound applies; falls back to the parent product's images if omitted), its own price (`mrp` + `discount`), and its own `stock`.
- `FR-CAT-047`: Admin can update a variant, or deactivate it (`active: false` — a soft delete, mirroring `FR-CAT-016`'s rationale that future Order references must not break on a removed variant). Variants are never hard-deleted.
- `FR-CAT-048`: No two variants of the same product may share an identical attribute-pair combination; validated at the application layer (not a DB unique index, since `attributes` is an array).
- `FR-CAT-049`: A variant's price/stock fields follow the exact same validation as the product's own: `mrp` > 0 integer paise, `currency` fixed `"INR"`, `discount` 0–99, `sellingPrice` always server-computed, `stock` a non-negative integer.
- `FR-CAT-050`: A product with zero active variants is sold using its own `sku`/`price`/`stock` (original `FR-CAT-011`–`020` behavior, unchanged). A product with one or more active variants is sold **per-variant** — the base product's own `price`/`stock` become "starting from" display fields only, not the purchasable unit.
- `FR-CAT-051`: Buyer-facing product detail page defaults to displaying the first (or lowest-`sellingPrice`) active variant's price/stock/images when a product has variants; selecting a different variant updates the displayed price, stock, and images accordingly (falling back to the parent product's images if the selected variant has none of its own).
- `FR-CAT-052`: Buyer can filter a category listing by a variant attribute name/value (e.g. Color = Red), served from a backend-maintained, denormalized `products.availableVariantAttributes` rollup — not a live join against `productVariants` at query time.
- `FR-CAT-053`: A variant has no independent visibility status — it inherits entirely from its parent product's status. A draft/archived product's variants never appear buyer-facing, per the existing `FR-CAT-009` guarantee, regardless of the variant's own `active` flag.

### Images — Cloudflare R2 upload mechanics

- `FR-CAT-054`: Admin requests a short-lived presigned upload URL from the backend for each image before uploading directly to R2; the backend never receives raw image bytes.
- `FR-CAT-055`: The presigned-URL request declares the image's content-type and is rejected server-side if it isn't JPEG/PNG/WebP, before a presigned URL is issued (enforces `FR-CAT-029`).
- `FR-CAT-056`: The presign response returns a **server-generated** object key of the form `{purpose}/{uuid}.{ext}` (e.g. `product-images/{uuid}.webp`, `brand-logos/{uuid}.webp`) — never a path keyed by the parent entity's ID, since the product/brand may not exist yet at upload time. The client cannot choose or influence the key.
- `FR-CAT-057`: Registering an uploaded image's URL against a product or brand validates that the object key was actually issued by a prior presign call (tracked server-side, not yet consumed) — arbitrary or unrecognized object URLs are rejected.
- `FR-CAT-058`: Presigned upload URLs expire 5 minutes after issuance.
- `FR-CAT-059`: The same presign endpoint serves both product images and brand logos, parameterized by a `purpose` field (`product-image` | `brand-logo`) that determines the path prefix and any size/format constraints.
- `FR-CAT-060`: Registering uploaded image URLs against a product enforces the 1–8 image count bound (`FR-CAT-028`) server-side, regardless of how many presigned URLs were requested.

### Price — structured price mechanics

- `FR-CAT-061`: `discount` is an integer percentage **0–99** (not 100 — a `discount: 100` would force `sellingPrice: 0`, violating the `> 0` rule); default `0` if omitted; validation rejects out-of-range or non-integer input.
- `FR-CAT-062`: `sellingPrice` is computed server-side as `mrp - floor(mrp * discount / 100)` on every create/update — never accepted directly from the client — guaranteeing it always stays consistent with `mrp`/`discount`, and (given the 0–99 discount bound) always `>= 1`.

---

## 3. Baseline Non-Functional Requirements

Feature-scoped only — see `SRS.md` §4 item 3 for why this isn't exhaustive (full system NFRs land in v0.8/v0.9).

- [ ] All admin write endpoints (create/update/delete on products and categories) validate input server-side with Zod schemas — this is the first feature to add a `zod` dependency to `backend`.
- [ ] All buyer-facing read endpoints validate query parameters (page, limit, price range, sort) server-side; invalid values are rejected or clamped, never passed through to the DB layer unvalidated.
- [ ] `products` collection indexes: unique index on `sku`, unique index on `slug`, index on `category`, index/text-or-Atlas-Search index supporting `FR-CAT-004`'s keyword search.
- [ ] `categories` collection indexes: unique index on `slug`, index on `parent`.
- [ ] All endpoints follow the existing error contract (`{ success: false, code, message }`, per `docs/architecture.md` §6) — no ad hoc error shapes introduced by this feature.
- [ ] `backend/src/config/db.ts`'s Mongoose connection includes basic startup failure handling (the process should not silently serve traffic against a dead DB connection) — full connection-resilience/retry policy is deferred to v0.8, this is just "don't fail silently."
- [ ] No secrets (Cloudflare R2 credentials, admin shared-secret) committed to the repo; sourced from environment variables, following the existing `.env.example` pattern in `backend/`.
- [ ] The admin shared-secret guard (`FR-CAT-030`–`032`) is a single middleware applied at the router level, not duplicated per-controller — keeps the v0.3 swap-out a one-place change.
- [ ] `brands` collection: unique index on `slug`.
- [ ] `categorySpecifications` collection: compound unique index on `(category, name)`.
- [ ] `productVariants` collection: unique index on `sku` (global, shared namespace with `products.sku`). Duplicate attribute-combination prevention (`FR-CAT-048`) is an application-level check, not a DB index, since `attributes` is an array.
- [ ] `products.brand` indexed.
- [ ] `products.availableVariantAttributes` (denormalized rollup) indexed via Atlas Search, backing `FR-CAT-052`'s filter without a live join.
- [ ] Atlas Search indexes `specifications` as an `embeddedDocuments` field keyed by `specification` + whichever of `stringValue`/`numberValue`/`booleanValue` applies, backing `FR-CAT-045`.
- [ ] **Atlas Search scope is deliberately narrow**: only free-text keyword search (`FR-CAT-004`) and spec/variant-attribute faceted filtering (`FR-CAT-045`, `FR-CAT-052`) use Atlas Search. Category, brand, status, price-range, and in-stock filters remain plain indexed MongoDB queries.
- [ ] `price.sellingPrice` is never trusted from client input on either `products` or `productVariants` — always recomputed server-side from `mrp`/`discount` on every write.
- [ ] R2 presigned URLs expire 5 minutes after issuance; object keys are server-generated (never client-chosen), and registering an image validates the key was actually issued and not yet consumed.

---

## 4. User Stories

- As a **guest**, I want to browse products by category, so that I can find things I'm interested in without creating an account.
- As a **guest**, I want to search for a product by name, so that I can quickly find something specific.
- As a **guest**, I want to filter and sort a product listing by price, so that I can shop within my budget.
- As a **guest**, I want to filter products by brand and by variant options like color or size, so that I can narrow down to exactly what I want.
- As a **registered buyer**, I want the same browsing/search/filtering as a guest (no catalog feature is gated behind login — that's a Cart/Checkout concern, not Catalog).
- As a **catalog manager**, I want to create and edit products with images, price, and stock, so that the storefront reflects what's actually available to sell.
- As a **catalog manager**, I want to organize products into categories, so that buyers can navigate the storefront logically.
- As a **catalog manager**, I want to define specification fields and add priced/stocked variants to a product, so that I can accurately represent products that come in multiple configurations.
- As a **catalog manager**, I want to mark a product out of stock or archive it, so that buyers don't see things that aren't purchasable, without losing the product's history.
- As a **super admin**, I want the same catalog-management capabilities as a catalog manager (role distinction here is only relevant once v0.3 RBAC exists).

---

## 5. Data Model / API Contract

### `brands` collection

| Field                   | Type                      | Notes                        |
| ----------------------- | ------------------------- | ---------------------------- |
| `_id`                   | ObjectId                  |                              |
| `name`                  | string, required          |                              |
| `slug`                  | string, required, unique  | auto-generated, `FR-CAT-034` |
| `logo`                  | string (R2 URL), optional |                              |
| `description`           | string, optional          |                              |
| `createdAt`/`updatedAt` | timestamps                |                              |

### `categorySpecifications` collection

| Field                   | Type                                        | Notes                                                       |
| ----------------------- | ------------------------------------------- | ----------------------------------------------------------- |
| `_id`                   | ObjectId                                    |                                                             |
| `category`              | ObjectId ref → `categories`, required       | scoped to the exact category, no inheritance to/from parent |
| `name`                  | string, required                            | e.g. "RAM"                                                  |
| `type`                  | enum: `string`, `number`, `boolean`, `enum` |                                                             |
| `unit`                  | string, optional                            | e.g. "GB"                                                   |
| `options`               | string[], required if `type: enum`          |                                                             |
| `required`              | boolean, default `false`                    |                                                             |
| `filterable`            | boolean, default `false`                    | surfaced as a buyer-facing facet                            |
| `createdAt`/`updatedAt` | timestamps                                  |                                                             |

### `productVariants` collection

One row per sellable attribute _combination_, each independently priced and stocked:

| Field                   | Type                                                                                     | Notes                                                                                                                                           |
| ----------------------- | ---------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `_id`                   | ObjectId                                                                                 |                                                                                                                                                 |
| `product`               | ObjectId ref → `products`, required                                                      | the parent product this variant belongs to                                                                                                      |
| `sku`                   | string, required, unique                                                                 | global namespace, shared with `products.sku` — `FR-CAT-046`                                                                                     |
| `attributes`            | `[{ name: string, value: string, label?: string, swatch?: string }]`, required, 1+ items | e.g. `[{name:"Color",value:"Red"},{name:"Size",value:"L"}]`; no two variants of the same product may share an identical set — `FR-CAT-048`      |
| `images`                | string[] (R2 URLs), optional, 0–8 items                                                  | same count bound as `products.images` (`FR-CAT-028`); if empty, the PDP falls back to the parent product's images when this variant is selected |
| `price.mrp`             | integer, required, > 0                                                                   | paise — same shape/rules as `products.price.mrp`                                                                                                |
| `price.currency`        | string, required, fixed `"INR"`                                                          |                                                                                                                                                 |
| `price.discount`        | integer, optional, default `0`, 0–99                                                     |                                                                                                                                                 |
| `price.sellingPrice`    | integer, required, > 0, <= `price.mrp`                                                   | backend-computed, identical rule to `products.price.sellingPrice`                                                                               |
| `stock`                 | integer, required, >= 0                                                                  |                                                                                                                                                 |
| `active`                | boolean, default `true`                                                                  | soft-delete flag — `FR-CAT-047`                                                                                                                 |
| `createdAt`/`updatedAt` | timestamps                                                                               |                                                                                                                                                 |

### `products` collection

| Field                        | Type                                                                                                                             | Notes                                                                                                                        |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `_id`                        | ObjectId                                                                                                                         |                                                                                                                              |
| `name`                       | string, required                                                                                                                 |                                                                                                                              |
| `slug`                       | string, required, unique                                                                                                         | auto-generated from `name`, `FR-CAT-013`                                                                                     |
| `sku`                        | string, required, unique                                                                                                         | shared uniqueness namespace with `productVariants.sku` — `FR-CAT-012`                                                        |
| `description`                | string, required                                                                                                                 |                                                                                                                              |
| `brand`                      | ObjectId ref → `brands`, required                                                                                                | `FR-CAT-039`                                                                                                                 |
| `category`                   | ObjectId ref → `categories`, required                                                                                            |                                                                                                                              |
| `images`                     | string[] (Cloudflare R2 URLs), 1–8 items                                                                                         | `FR-CAT-027`–`029`                                                                                                           |
| `specifications`             | `[{ specification: ObjectId ref → categorySpecifications, stringValue?: string, numberValue?: number, booleanValue?: boolean }]` | exactly one value field populated, matching the referenced spec's `type` — `FR-CAT-043`                                      |
| `availableVariantAttributes` | `[{ name: string, value: string }]`, denormalized, backend-maintained                                                            | rollup of all `active` variants' attribute pairs, kept in sync on variant add/update/deactivate; backs `FR-CAT-052`'s filter |
| `price.mrp`                  | integer, required, > 0                                                                                                           | paise (INR minor unit) — the "original"/MRP price, `FR-CAT-018`                                                              |
| `price.currency`             | string, required, fixed `"INR"`                                                                                                  | matches the India-first market constraint (`SRS.md` §2.5)                                                                    |
| `price.discount`             | integer, optional, default `0`, 0–99                                                                                             | percentage off `mrp` — `FR-CAT-061`                                                                                          |
| `price.sellingPrice`         | integer, required, > 0, <= `mrp`                                                                                                 | **backend-computed** as `mrp - floor(mrp * discount / 100)`, not independently admin-settable — `FR-CAT-062`                 |
| `stock`                      | integer, required, >= 0                                                                                                          | authoritative only when the product has zero active variants — `FR-CAT-050`                                                  |
| `status`                     | enum: `draft`, `published`, `archived`                                                                                           | default `draft`                                                                                                              |
| `createdAt` / `updatedAt`    | timestamps                                                                                                                       |                                                                                                                              |

Variant instance data is **not** embedded on `products` — it lives in the separate `productVariants` collection above, joined/populated into API responses at read time (never stored embedded in the DB). All buyer-facing price filtering (`FR-CAT-005`), sorting (`FR-CAT-006`), and the price round-trip acceptance criterion operate on `price.sellingPrice`.

### `categories` collection

| Field                     | Type                                  | Notes                       |
| ------------------------- | ------------------------------------- | --------------------------- |
| `_id`                     | ObjectId                              |                             |
| `name`                    | string, required                      |                             |
| `slug`                    | string, required, unique              | `FR-CAT-022`                |
| `parent`                  | ObjectId ref → `categories`, nullable | one level max, `FR-CAT-021` |
| `createdAt` / `updatedAt` | timestamps                            |                             |

Deleting a category (already guarded to zero products/subcategories, `FR-CAT-024`) cascades to delete that category's `categorySpecification` definitions.

### API endpoints (paths indicative — exact routing/module split is a `backend/` implementation detail, not fixed here)

| Method       | Path                                         | Auth (v0.2: shared-secret header — `FR-CAT-030`; v0.3+: real RBAC) | Purpose                                                                                                                                   |
| ------------ | -------------------------------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- |
| GET          | `/api/products`                              | public                                                             | List/search/filter/sort/paginate — `FR-CAT-001`, `004`–`010`                                                                              |
| GET          | `/api/products/:slug`                        | public                                                             | Product detail — `FR-CAT-003`                                                                                                             |
| POST         | `/api/products`                              | shared-secret → catalog-manager, super-admin                       | Create — `FR-CAT-011`                                                                                                                     |
| PATCH        | `/api/products/:id`                          | shared-secret → catalog-manager, super-admin                       | Update — `FR-CAT-014`, `015`                                                                                                              |
| PATCH        | `/api/products/:id/stock`                    | shared-secret → catalog-manager, super-admin                       | Stock-only update — `FR-CAT-020`                                                                                                          |
| DELETE       | `/api/products/:id`                          | shared-secret → catalog-manager, super-admin                       | Soft delete — `FR-CAT-016`                                                                                                                |
| GET          | `/api/admin/products`                        | shared-secret → catalog-manager, super-admin                       | Admin grid, all statuses — `FR-CAT-017`                                                                                                   |
| GET          | `/api/categories`                            | public                                                             | Category tree for nav — `FR-CAT-026`                                                                                                      |
| GET          | `/api/admin/categories`                      | shared-secret → catalog-manager, super-admin                       | Admin list with counts — `FR-CAT-025`                                                                                                     |
| POST         | `/api/categories`                            | shared-secret → catalog-manager, super-admin                       | Create — `FR-CAT-021`                                                                                                                     |
| PATCH        | `/api/categories/:id`                        | shared-secret → catalog-manager, super-admin                       | Update — `FR-CAT-023`                                                                                                                     |
| DELETE       | `/api/categories/:id`                        | shared-secret → catalog-manager, super-admin                       | Delete (guarded) — `FR-CAT-024`                                                                                                           |
| GET          | `/api/brands`                                | public                                                             | List brands for buyer filter nav — `FR-CAT-038`                                                                                           |
| GET          | `/api/admin/brands`                          | shared-secret → catalog-manager, super-admin                       | Admin list with product counts — `FR-CAT-037`                                                                                             |
| POST         | `/api/brands`                                | shared-secret → catalog-manager, super-admin                       | Create — `FR-CAT-033`                                                                                                                     |
| PATCH        | `/api/brands/:id`                            | shared-secret → catalog-manager, super-admin                       | Update — `FR-CAT-035`                                                                                                                     |
| DELETE       | `/api/brands/:id`                            | shared-secret → catalog-manager, super-admin                       | Delete (guarded) — `FR-CAT-036`                                                                                                           |
| POST         | `/api/categories/:id/specifications`         | shared-secret → catalog-manager, super-admin                       | Define a spec field — `FR-CAT-041`                                                                                                        |
| PATCH/DELETE | `/api/categories/:id/specifications/:specId` | shared-secret → catalog-manager, super-admin                       | Update/delete a spec definition — `FR-CAT-042`                                                                                            |
| POST         | `/api/products/:id/variants`                 | shared-secret → catalog-manager, super-admin                       | Add a variant (sku, attributes, price, stock, images) — `FR-CAT-046`                                                                      |
| PATCH        | `/api/products/:id/variants/:variantId`      | shared-secret → catalog-manager, super-admin                       | Update a variant, or set `active: false` — `FR-CAT-047`                                                                                   |
| POST         | `/api/uploads/presign`                       | shared-secret → catalog-manager, super-admin                       | Request a presigned R2 upload URL; body includes `purpose` (`product-image`\|`brand-logo`) and declared content-type — `FR-CAT-054`–`059` |

Response schemas (exact JSON shape) are left to `backend`'s Zod implementation, matching this doc's existing level of abstraction — not expanded here.

---

## 6. UI/UX Requirements

**`buyer-app`** (per `docs/architecture.md` §4.1's rendering strategy — catalog listing and product detail are both ISR):

- Catalog listing page: product grid, category filter, brand filter, filterable-spec facets, variant-attribute facets, price-range filter, sort control, pagination controls. Loading state (skeleton grid), empty state ("no products match your filters"), error state (fetch failure — non-blocking, doesn't crash the page).
- Category page: same grid, pre-filtered to the category, breadcrumb showing parent category if any.
- Search results page: same grid, keyword shown, empty state distinct from the filter-empty state ("no results for '...'").
- Product detail page: images (gallery/carousel), name, price, description, category link, brand name/link, specification table, stock-status badge (in stock / out of stock — no "add to cart" control yet, that's v0.4), and — when the product has variants — a variant selector (e.g. color swatches / size buttons) that updates the displayed price, stock, and images, defaulting to the first/lowest-priced active variant.

**`admin-app`** (per `docs/architecture.md` §4.2 — TanStack Table for catalog grids):

- Product list: TanStack Table with columns (image thumbnail, name, SKU, brand, category, price, stock, status), search box, status filter, pagination.
- Product create/edit form: all fields from the data model, client-side validation mirroring the backend Zod schema (per root `CLAUDE.md`'s "no shared validation package" decision — this is `admin-app`'s own separate validation, not shared code), image upload widget with preview and the 1–8 count constraint surfaced in the UI, a brand selector, dynamic spec fields rendered from the product's category's `categorySpecification` definitions, and a variant editor (add/edit/deactivate) where each variant has its own SKU, attribute pairs, price, stock, and optional images.
- Category list: simple table (name, parent, product count), create/edit form, delete blocked with an inline error if `FR-CAT-024`'s guard rejects it.
- Brand list: simple table (name, logo, product count), create/edit form, delete blocked with an inline error if `FR-CAT-036`'s guard rejects it.

---

## 7. Out of Scope

- Product variants (size/color) are now first-class sellable child records (own SKU/price/stock) of a single parent product — but there is still no linking/grouping _between separate `Product` documents_. Two genuinely different products (even from the same brand) are never linked; a variant always belongs to exactly one parent product.
- Reviews and ratings.
- Wishlists / "save for later."
- Recommendation engine / "related products."
- Bulk import/export (CSV, etc.).
- Multi-warehouse / per-location inventory — a single global `stock` integer per product (or per variant, when variants exist) only.
- Category depth beyond two levels (parent + one level of children).
- Specification-axis inheritance from parent category to subcategory — `categorySpecification` definitions are scoped to the exact category a product is assigned to.
- R2 object cleanup on brand/product deletion — orphaned storage objects are not proactively deleted in this version (see §10 Resolved Decisions).
- Real RBAC enforcement on admin endpoints — protected by a temporary shared-secret header (`FR-CAT-030`–`032`) until v0.3 ships session/role-based auth.
- Cart/"add to cart" interaction — product detail page is read-only in this version (v0.4 territory).

---

## 8. Acceptance Criteria

- [ ] A guest can load the catalog listing page and see only `published` products, paginated.
- [ ] A guest can navigate to a category and see only products in that category (and its subcategories, if any).
- [ ] A guest can search a keyword and get correct, case-insensitive, partial-match results, including a correct empty-state for a no-match search.
- [ ] A guest can combine a price-range filter with sort (e.g., category + price ascending) and get correctly filtered, correctly ordered results.
- [ ] A product with `stock: 0` still appears in listings, marked out of stock, and is excluded when an "in stock only" filter is applied.
- [ ] A `draft` or `archived` product never appears on any public endpoint under any query combination — verified explicitly, not just by omission.
- [ ] An admin can create a product with all required fields; a request missing a required field, with a duplicate SKU, a non-positive price, or a negative/non-integer stock value is rejected with a validation error, not a 500.
- [ ] An admin can edit a product's status to `archived`; the product immediately disappears from all public endpoints while remaining visible in the admin grid.
- [ ] Deleting a product soft-deletes it (record persists with `status: archived`); no product is ever hard-deleted by this feature's endpoints.
- [ ] An admin can create a category, assign products to it, and see an accurate product count in the admin category list.
- [ ] Deleting a category with 0 products/subcategories succeeds; deleting one with either fails with a clear, actionable error message.
- [ ] Uploading a 9th image, or an image in an unsupported format, is rejected before reaching R2.
- [ ] A request to any admin endpoint with a missing or wrong `X-Admin-Key` header is rejected with 401 before any controller/service code runs.
- [ ] Price round-trips correctly as integer paise end-to-end (admin creates a product with `price.mrp: 49900` → buyer-facing detail/listing responses reflect the same `price.mrp`/`price.sellingPrice` with no float drift).
- [ ] `npm run test --workspace backend` includes integration tests (Supertest) covering at minimum: list/search/filter, product CRUD validation failures, category delete-guard, the draft/archived visibility boundary, and the admin shared-secret guard (missing/wrong/correct header) — mirroring the `health` module's existing test pattern.
- [ ] An admin can create a brand, assign products to it, and see an accurate product count (across all statuses) in the admin brand list; deleting a brand with ≥1 referencing product (any status) fails with a clear error.
- [ ] An admin can define a category's specification schema; creating a product with a spec value of the wrong type, an out-of-`options` enum value, a missing `required` spec, or an unknown spec key is rejected with a validation error, not a 500. Deleting that category cascades to remove its spec definitions.
- [ ] An admin can add two variants to the same product with different attribute combinations (e.g. Color=Red/Size=L and Color=Red/Size=M), each with its own SKU, price, and stock; adding a third variant with an attribute combination identical to an existing one is rejected.
- [ ] A buyer viewing a product with variants sees the first/lowest-priced active variant's price, stock, and images by default; selecting a different variant updates the displayed price, stock, and images (falling back to the parent product's images when the selected variant has none of its own).
- [ ] A buyer can filter a category listing by brand, by a `filterable` specification, and by a variant attribute name/value, individually and combined with existing price/sort filters.
- [ ] Requesting a presigned upload URL for a disallowed content-type is rejected before any URL is issued; a presigned URL used after 5 minutes is rejected by R2 itself; registering an image URL with an object key the backend never issued is rejected; registering more than 8 image URLs against a product is rejected.
- [ ] Creating a product with `mrp: 99900, discount: 10` computes and stores `sellingPrice: 89910`; attempting `discount: 100` is rejected (would force `sellingPrice: 0`).
- [ ] Submitting a `sellingPrice` value directly in a create/update request (product or variant) has no effect — it is always server-recomputed from `mrp`/`discount`.
- [ ] Archiving a product hides all of its variants from every buyer-facing endpoint, without needing to touch each variant's own `active` flag.

---

## 9. Dependencies

- **MongoDB Atlas** — first feature to require a live connection; `backend/src/config/db.ts`'s stub gets replaced. Per `SRS.md` §2.6, an Atlas cluster is assumed provisionable.
- **Cloudflare R2** — product/brand image storage (`FR-CAT-027`, `054`–`060`), S3-compatible object storage. Per `SRS.md` §2.6, assumed provisionable; needs an account ID, access key ID/secret, and bucket name in `backend/.env`.
- **MongoDB Atlas Search** — backs keyword search (`FR-CAT-004`) and spec/variant-attribute faceted filtering (`FR-CAT-045`, `FR-CAT-052`) per the already-decided architecture (`docs/architecture.md` §9). Needs to actually be provisioned/configured against the `products` collection as part of implementation, not just assumed.
- **Authentication (SRS v0.3)** — admin endpoints are protected only by the temporary shared-secret guard (`FR-CAT-030`–`032`) until v0.3 ships real session/role-based RBAC and replaces it.

---

## 10. Resolved Decisions

All open questions from the initial draft were resolved on 2026-07-22. Kept here as a record of what was asked and decided, rather than silently rewriting requirements without a trace.

1. **Admin endpoints before Authentication exists.** M2 ships before M3 (Authentication) per the fixed milestone order in `docs/milestone.md`. **Decision: accepted, with temporary protection** — admin endpoints are gated by a shared-secret header (`FR-CAT-030`–`032`), not left open, until v0.3 replaces it with real RBAC.
2. **Product variants.** **Decision: superseded by Decision #7 below** — the original "no variant-matrix/grouping feature" call is revisited now that the catalog needs to represent products with multiple sellable configurations (size/color).
3. **Category depth.** **Decision: two-level hierarchy approved** (parent + one level of children, no grandchildren) — matches the original proposal, §2's `FR-CAT-021` and the data model are unchanged.
4. **Currency/price precision.** **Decision: integer paise, now wrapped in a structured price object** — see Decision #9 below for the current shape.
5. **Brand.** New required top-level entity, own collection, full admin CRUD with delete-guard (counts products of any status), buyer-facing list + filter.
6. **Category-governed specifications, type-safe storage.** `categorySpecification` defines a per-category attribute schema; product spec values are strictly validated against it and stored in a type-matched field (`stringValue`/`numberValue`/`booleanValue`), never a generic `Mixed` — no free-form extra keys. Deleting a category cascades to delete its spec definitions.
7. **Product variants are real sellable items, not tags (supersedes Decision #2).** A variant is a full attribute combination with its own `sku`/`price`/`stock`/optional `images`, soft-deletable (`active` flag) like products. A product with 0 variants sells via its own sku/price/stock; a product with ≥1 active variants sells per-variant, with the base product's price/stock becoming "starting from" display fields, and variant images (falling back to the parent product's) shown when that variant is selected. This is a deliberate, load-bearing decision for Cart (v0.4)/Orders (v0.5): "add to cart" will reference a specific variant when one exists, not just a product.
8. **Image storage: Cloudflare R2 replaces Cloudinary.** Presigned direct-upload flow with a server-generated object key (`{purpose}/{uuid}.{ext}`, not entity-ID-keyed — avoids a chicken-and-egg problem for not-yet-created products/brands), 5-minute expiry, and ownership validation before an uploaded URL is accepted. The same endpoint serves both product images and brand logos. R2 objects are not proactively cleaned up on brand/product deletion in this version — accepted as a minor cost tradeoff at this scale, not a correctness gap.
9. **Price becomes a structured object.** `price` is `{ mrp, currency, discount, sellingPrice }` on both `products` and `productVariants`; `discount` is bounded 0–99 (not 0–100, which would force `sellingPrice: 0`); `sellingPrice` is always backend-computed, never accepted from the client. All price filtering/sorting/round-trip requirements operate on `sellingPrice`.
