// The health module needs no database access. This file exists to keep the
// module's shape (module/routes/controller/service/repo) consistent with
// DB-backed modules landing from M2 onward.
export {};
