# CLAUDE.md (backend)

This file covers `backend/` implementation detail only. Root-level architecture decisions live in the root [`CLAUDE.md`](../CLAUDE.md), [`AGENTS.md`](../AGENTS.md), and [`docs/architecture.md`](../docs/architecture.md) — nothing here overrides those.

## Current status

Module-based skeleton scaffolded (Issue #2 / M0.2): Express 5 + TypeScript, cross-cutting `src/config/`, `src/middleware/`, `src/utils/`, `src/externalService/`, `src/routes/` (aggregator), and one feature module, `src/modules/health/`. Vitest + Supertest are wired with passing tests. `npm run build|dev|test --workspace backend` all work. M2 core plumbing is in place (Issue #25 / M2.1): `src/config/db.ts` holds a live Mongoose connection (plus `disconnectDB()` for graceful shutdown), started from `src/index.ts`'s exported `startServer()` (refuses to call `app.listen()` and exits `1` if `connectDB()` rejects — see Startup below); `src/config/env.ts` validates `process.env` with a `zod` schema (first `zod` usage in this workspace); `src/utils/apiResponse.ts`'s `successResponse()` is the shared `{ success: true, data }` (+ optional `pagination`) envelope every later catalog controller uses; `src/middleware/adminAuth.ts` + `src/routes/admin.routes.ts` gate everything mounted under `/api/admin` behind an `X-Admin-Key` header check — see Admin routes below. Cloudflare R2 image uploads are in place (Issue #26 / M2.2): `src/externalService/r2.ts` is the first external-service client, `src/modules/uploads/` presigns direct-to-R2 uploads and tracks issued-but-unconsumed object keys in MongoDB with a TTL index, and `errorHandler.ts` now also translates a thrown `ZodError` into the standard error contract. A second, backend-proxied upload path (`POST /api/admin/uploads/direct`, `multer`-backed, SRS amendment `FR-CAT-097`–`100`) was added alongside the presigned path, and `errorHandler.ts` also translates a thrown `MulterError` — see R2 uploads below. Brand management is in place (Issue #27 / M2.3): `src/modules/product-catalog/features/brands/` is the first real catalog-entity module (model/repository/service/controller/routes/module, DB-backed) — see Brands below for the module itself and for the reusable conventions it introduces (`src/utils/slug.ts`, `:id` route validation, and a narrow cross-module repository exception) that categories (#28) and products (#31) are expected to copy. Category management is in place (Issue #28 / M2.4): `src/modules/product-catalog/features/categories/` adds a two-level category hierarchy (`FR-CAT-014`–`022`) with admin CRUD, a combined products-and-subcategories delete guard, and a public active-only list ordered by `sortOrder`/`name` with a computed SEO fallback — see Categories below. It also promotes two brands-era patterns to shared utilities on their second use (`src/utils/objectId.ts`, `src/utils/text.ts`) and fixes a real correctness bug discovered while building it: `brands.repository.ts`'s `updateById` now wraps its patch in `$set`, since MongoDB treats a plain (non-operator) update document as a full replacement, not a partial update. Category-governed specifications are in place (Issue #29 / M2.5): `src/modules/product-catalog/features/categorySpecifications/` adds a nested, one-per-category specification schema (`FR-CAT-030`–`035`) — read/define/update via `GET`/`PUT`/`PATCH` only, no `POST`/`DELETE` — with a guarded delete reaching one level deeper than anything before it (a single field inside a nested array), and resolves `categories.service.ts`'s previously-deferred cascade-delete comment — see Category Specifications below. Category-governed variant types are in place (Issue #30 / M2.6): `src/modules/product-catalog/features/categoryVariants/` adds a one-per-category list of variant axes (`FR-CAT-036`–`038`) with the identical `GET`/`PUT`/`PATCH` shape as specifications, but flat (no groups) and with **no in-use delete guard at all** — a deliberate asymmetry, since this definition drives admin form rendering only and is never enforced against stored variant attributes. Product core CRUD and pricing are in place (Issue #31 / M2.7): `src/modules/product-catalog/features/products/` graduates from a stub to the first full catalog-entity module built on top of brands/categories/specifications — `POST`/`PATCH`/`GET`/`GET`-list/`DELETE` plus a dedicated `PATCH .../stock` path (`FR-CAT-001`–`013`), a shared `src/utils/pricing.ts` computing `sellingPrice` server-side on every write (`FR-CAT-085`–`087`, reused by variants in #32), and the first paginated+sortable admin list this codebase has built — see Products below. Product variants are in place (Issue #32 / M2.8): the `variants` array on the product schema, stubbed since #31 to just `{ sku }`, is now the full embedded subdocument (`FR-CAT-039`–`044`) with its own `POST .../variants`/`PATCH .../variants/:variantId` endpoints, reusing `computeSellingPrice` and the create/update-time SKU cross-check `products.repository.ts` already had — see Product Variants below. Status update APIs are in place (Issue #33 / M2.9): a dedicated `PATCH .../status` lands on all three entities — `PATCH /api/admin/products/:id/status` (`FR-CAT-045`, a tri-state setter reusing a new shared `PRODUCT_STATUSES` const) and `PATCH /api/admin/categories/:id/status` / `PATCH /api/admin/brands/:id/status` (`FR-CAT-046`–`047`, boolean toggles) — each a thin pass-through that deliberately never touches its entity's own delete guard (`FR-CAT-048`), since deactivating and deleting are different operations with different preconditions — see Status Updates below. Admin search is in place (Issue #34 / M2.10): all three admin lists (`GET /api/admin/products`, `GET /api/admin/categories`, `GET /api/admin/brands`) gain a `search` query param (`FR-CAT-050`–`052`) plus a new shared `src/utils/text.ts`'s `escapeRegExp()`, and the product grid additionally gains a `status` query param that composes with `search` (`FR-CAT-053`) — see Admin Search below. Buyer browsing, search, and inventory visibility are in place (Issue #35 / M2.11): the first buyer-facing product endpoints — `GET /api/products` (paginated, `published`-only, `?q=` MongoDB Atlas Search), `GET /api/categories/:slug/products` (category + subcategories), `GET /api/products/:slug` (detail) — plus `GET /api/categories/search` (`FR-CAT-054`–`067`). A new shared `src/utils/availability.ts` derives the buyer-facing `availability` enum (`out_of_stock`/`low_stock`/`in_stock`) that every buyer response substitutes for raw `stock`/`lowStockThreshold` (`FR-CAT-095`–`096`), and this module's first use of Mongoose `.populate()` resolves a product's `brand`/`category` refs to `{_id, name, slug}` for buyer responses. The Atlas Search index itself is a real Atlas-cluster provisioning step this issue could not perform (local dev runs against a plain `mongodb://` instance, not Atlas) — see `backend/atlas-search/README.md` — see Buyer Browsing below for full detail. Buyer filtering, sorting, and card content are in place (Issue #36 / M2.12): the buyer listing endpoints (`GET /api/products`, `GET /api/categories/:slug/products`) gain price/brand/category/variant-attribute/filterable-specification/in-stock/on-sale filters plus a sort option (`FR-CAT-068`–`076`), composing with each other and with `?q=`; every list item also gains `cardSpecifications` — a category's first four `filterable` specification fields, in schema declaration order (`FR-CAT-091`–`092`). `app.ts` now sets `query parser: "extended"` (Express 5's default no longer supports the bracket-notation `?spec[RAM]=8GB` this issue's dynamic filters need) — see Buyer Filtering & Sorting below for full detail. Catalog modules were regrouped under `src/modules/product-catalog/features/` (chore, no issue number — a structural refactor, not a new feature): `brands`/`categories`/`categorySpecifications`/`categoryVariants`/`products` moved there as-is, while `health`/`uploads` stayed flat at `src/modules/<feature>/` since they're cross-cutting infra rather than catalog-domain entities; `__tests__/` was regrouped the same way, into `__tests__/product-catalog/<feature>/` — see "How to add a new module" above for the resulting convention. **M2 (Product Catalog) is complete** (Issues #25–#36) — and then amended post-completion by Issue #102 (SRS v0.2 amendment, backend-only): the product/variant model collapses to **variant-only** — a product carries no `sku`/`images`/`mrp`/`discount`/`sellingPrice`/`stock`/`lowStockThreshold` of its own, every sellable unit is a variant, and stock/inventory tracking (plus the derived `availability` enum) is removed system-wide, not moved — see Variant-Only Pricing below for full detail. Issue #104 (SRS v0.2 amendment, backend-only) adds a shared `src/utils/parseQuery.ts` and uses it to bring categories'/brands' admin lists up to pagination/sort parity with products' — both previously returned an unfiltered, unpaginated full array — while products' own sort wire format moves from a combined `?sort=-field` enum to two params, `?sortBy=`/`?orderBy=asc|desc|none`; `admin-app`'s consumption of the two newly-paginated endpoints is a known, accepted, documented gap left for a follow-up frontend issue — see Shared Admin List Query Parsing below for full detail. Issue #106 (dev-tooling only, no SRS/FR changes) adds an idempotent counterpart to `src/scripts/seed/run.ts`'s destructive full-reseed script: `src/scripts/seed/upsert.ts` (`npm run seed:upsert --workspace backend`) find-or-creates/updates-in-place across all five catalog models — brands, categories, `categorySpecifications`, `categoryVariants`, and products — safe to run repeatedly without wiping existing document ids; `run.ts`/`npm run seed` are unchanged and remain the default full-reset path — see Seeding below for full detail. Issue #121 (buyer-app-filed, backend-implemented) fixes a real persistence bug found while investigating the issue's own ask: `replaceVariants()` was resetting every variant's `createdAt` to "now" on every write (Mongoose's `$set`-based array replace re-stamps every element, not just the one being touched — confirmed empirically, and `{timestamps:false}` alone doesn't suppress it), fixed by routing that write through the raw MongoDB driver instead of Mongoose. The buyer "newest" sort (Home, Category, and Search's fallback) now orders by the primary variant's (lowest-price active variant's, same selection as `selectDefaultVariant()`) `createdAt` instead of the product's own top-level field — see Newest Sort By Primary Variant below for full detail. Buyer passwordless authentication is in place (Issue #139, redone): `src/lib/auth.ts` — a new `src/lib/` category for constructed third-party singletons — configures Better Auth (MongoDB adapter reusing the existing Mongoose connection, Google OAuth, Google One Tap, email-OTP) with a **bearer-token** session, not cookie-only: `buyer-app` (Vercel) and `backend` (Render) are separate domains with no shared parent domain, so a cross-site cookie doesn't reliably survive a `fetch` from one to the other (this is what broke buyer sign-in the first time this feature shipped and was reverted, PR #208). `src/middleware/betterAuthHandler.ts` bridges Better Auth's Fetch-API handler to this repo's `{success,data}`/`{success,code,message}` contract; `src/middleware/cors.ts` gained an `Authorization`-header allowance, a `set-auth-token` header exposure, and wildcard-origin matching for Vercel preview URLs (`src/utils/originMatch.ts`) — see Authentication (Buyer) below for full detail. Admin password + mandatory OTP sign-in is in place (Issue #140, redone): also reverted alongside #139's original attempt (PR #208) and left unredone through #139's own redo, `src/lib/auth.ts` now also configures Better Auth's `emailAndPassword` + `twoFactor` plugins for a two-step password-then-OTP admin challenge (`FR-AUTH-009`–`018`, `030`) — a correct password alone never establishes a session, reusing the identical `emailOTP`/`sendOtpEmail`/envelope-bridge/bearer-token machinery #139 already built, plus a new `rejectBuyerOnPasswordSignIn` hook and a `src/scripts/seed/createAdminUser.ts` helper (`npm run seed:admin`) for provisioning a real admin account outside of tests — see Authentication (Admin) below for full detail. Admin self-service password reset is in place (Issue #141 / M3.3): `src/lib/auth.ts` adds Better Auth's built-in reset-password support (`FR-AUTH-019`–`022`) — `POST /api/auth/request-password-reset` (Better Auth's real endpoint name; neither the SRS's `/forgot-password` nor the plausible `/forget-password` exist) and `POST /api/auth/reset-password` — plus a new `src/lib/passwordResetTokens.ts` tracking collection (mirroring `uploads.model.ts`'s `presignedUploads` precedent) that a `hooks.before` matcher uses to invalidate every session for the admin once a reset completes (`FR-AUTH-022`) — see Admin Password Reset below for full detail. Admin account provisioning is in place (Issue #142 / M3.4, `FR-AUTH-024`–`029`): a new `src/modules/authentication/adminUsers/` (`POST`/`GET`/`PATCH /api/admin/users`) lets a super-admin create, list, and update further admin accounts, gated by a new `src/middleware/requireRole.ts` — the first identity-aware session guard in this codebase, layered alongside the existing `X-Admin-Key` router guard rather than replacing it. A newly created admin is routed through #141's real `requestPasswordReset` flow, never an auto-established session or an exposed temporary password; deactivating an admin synchronously revokes their live sessions via a newly extracted `src/utils/sessions.ts`. `src/scripts/seed/admin.ts` (#140's hardcoded stopgap) is replaced by an env-var-driven `src/scripts/seed/superAdmin.ts` (`npm run seed:super-admin`) — see Admin Account Provisioning below for full detail. Issue #143 / M3.5 (`FR-AUTH-031`–`035`) replaces the temporary `X-Admin-Key` shared-secret guard (`adminAuth.ts`, live since #25/M2.1) with a real session+role guard — `src/middleware/rbac.ts` consolidates #142's `requireRole.ts` into one guard taking an array of allowed roles, wired onto every catalog route (products, categories, brands, categorySpecifications, categoryVariants, uploads) with no route/controller/service business logic changed. It also backfills `createdBy`/`updatedBy` (reserved-`null` since #27/#28/#31) from the authenticated admin's id on every brand/category/product write, via a new shared `src/utils/actor.ts`. See RBAC Guard & createdBy/updatedBy Backfill below for full detail. Issue #144 / M3.6 (`FR-AUTH-036`–`039`) adds a new `src/modules/authentication/account/` — session-scoped "my own account" endpoints, distinct from #142's admin-managing-other-admins: `GET`/`PATCH /api/account/profile` (buyer-only session, name/phone) and `POST /api/account/change-password` (admin-only session, current-password required). The real fix this issue needed came from a Better Auth behavior no prior issue had hit: a client's bearer token is `<rawToken>.<signature>`, but the `session` collection only ever stores the raw part before that `.` — every naive attempt to exclude "the session making this request" from a bulk revoke by comparing the full header value against `session.token` silently matched nothing and deleted every session for the admin, current one included, confirmed via real CI diagnostics before the fix. See Account Self-Service below for full detail. Abuse mitigation and the `FR-AUTH-045` error-code contract are in place (Issue #145 / M3.7): five independent Redis-backed rate limiters (`ioredis`/`rate-limiter-flexible`, this codebase's first Redis dependency) split across two layers — Better Auth's own native per-request limiter (`src/lib/auth.ts`'s `rateLimit` option, IP-only) and a new `enforceEmailRateLimits` `hooks.before` hook (per-email, for the three paths whose body carries one) — plus a new `enforceAccountNotDeactivated` hook making `ACCOUNT_DEACTIVATED` real for the first time and a `betterAuthHandler.ts` change stamping `OTP_REQUIRED` onto the password-only sign-in step's response. See Abuse Mitigation & Error Contract below for full detail. Issue #148 / M3.10 (`admin-app`-filed, backend-fixed alongside its own frontend sign-in flow) closes a real cross-site cookie gap found while wiring `admin-app` up to the two-factor flow above: `src/lib/auth.ts` gains an `advanced.defaultCookieAttributes` override (`sameSite: "none", secure: true`, gated on `BETTER_AUTH_URL` being `https://`) so the `twoFactor` plugin's pending-challenge cookie survives a real cross-domain deployment, matching the exact class of bug `bearer()` already fixed for the final session itself (Issue #139/#208) — see Authentication (Admin) below for full detail. Issue #244 / M3.16 (pure structural refactor, no SRS/FR changes) regroups the three authentication-related modules under one parent, mirroring the product-catalog regroup's own precedent: `src/modules/{auth,adminUsers,account}/` → `src/modules/authentication/{auth,adminUsers,account}/`, and `__tests__/{auth,adminUsers,account}/` → `__tests__/authentication/{auth,adminUsers,account}/` — one level shallower than product-catalog's shape (no intermediate `features/` folder for this grouping, since these three don't share one the way catalog's five entity modules do). No route, controller, service, or test-count change. Issue #257 / M3.19 (amended by Issue #267/M3.27) is in place: `src/lib/jwt.ts` (HS256 sign/verify, `sub`/`role`/`jti`/`iat`/`exp` claims, a fixed 90-day outer TTL bound) and `src/lib/session.ts` (a new `sessions` collection keyed by the JWT's own `jti` claim directly — no hashing, unlike an opaque bearer token, since the JWT's own signature is what prevents forgery — plus `issueSession`/`verifySessionToken`/`revokeSession`/`revokeAllSessionsForUser` and cookie/`Authorization: Bearer` dual-delivery helpers) together form the first piece of the Better Auth removal: a completely standalone, unit-tested primitive with zero route/`rbac.ts`/Better-Auth wiring yet. `verifySessionToken` checks the JWT's signature first (no DB hit on failure), then atomically checks-and-extends a live `sessions` row's rolling 30-day `expiresAt` in one `findOneAndUpdate`, returning a discriminated `{ok:false, reason:"invalid_token"|"session_expired"}` on failure — deliberately only two reasons, since a revoked session and a naturally-expired one are indistinguishable by design (the same way `passwordResetTokens.ts`'s `consumeResetToken` already collapses "never existed" and "already consumed"). `JWT_SECRET` is a new, additive env var alongside `BETTER_AUTH_SECRET` (which stays until Issue #261 removes it). Later issues (#258 buyer auth, #259 admin auth, #260 RBAC rewire) will wire this primitive into real sign-in flows. Issue #258 / M3.20 is in place: buyer sign-in (Google One Tap + email OTP) is now fully hand-rolled, built on #257's session engine, while Better Auth stays fully functional for every admin path — a coexistence-window swap, not a removal. `src/lib/googleAuth.ts` verifies a Google ID token directly against Google's public JWKS (`google-auth-library`), replacing the `google` social-provider plugin; `src/lib/otp.ts` adds a new shared `otps` collection (buyer-sign-in wired here, admin-2fa left for #259), fixed to `"123456"` in every environment per #242/M3.14's existing tradeoff. `src/modules/authentication/auth/` gains its first real internal structure — `auth.repository.ts` (raw-driver `users` access, mirroring `account.repository.ts`'s established convention, since Better Auth's adapter still owns that collection's schema during coexistence), `auth.service.ts`, and `auth.controller.ts` — and `auth.routes.ts` mounts 5 new buyer routes (`one-tap/callback`, OTP send/verify, `get-session`, `sign-out`) above the unchanged Better Auth catch-all. `src/middleware/rbac.ts` gains a minimal, additive dual-check (new engine first, Better Auth fallback) so `/api/account/profile` keeps working for a buyer signed in through either system — not #260's full rewire, just enough to avoid a regression during the migration window. `src/lib/rateLimit.ts` gains `consumeIpLimit`, replicating the two native Better-Auth IP rules (`FR-AUTH-043`/`044`) that stopped running once their paths were intercepted ahead of the catch-all. The full-page Google OAuth-redirect flow (`sign-in/social`/`callback/google`) was deliberately not rebuilt — buyer-app has no consumer for it. Issue #259 / M3.21 does the same swap for **admin** sign-in: `src/lib/password.ts` (bcrypt via `bcryptjs`) + `src/lib/adminChallenge.ts` (a signed `techcart_admin_2fa` pending-challenge cookie, `SameSite=None; Secure` cross-site) power a hand-rolled password-then-mandatory-OTP flow on #257's session engine + #258's `otps` collection, wire-compatible with `admin-app`'s existing paths/shapes/codes. Admin password + reset (`request-password-reset`/`reset-password`) and `POST /api/account/change-password` are all hand-rolled onto `users.passwordHash` + `passwordResetTokens`, removing the last Better Auth dependencies from those flows; `provisionAdminUser()` writes bcrypt straight to `users.passwordHash` (no more `auth.api.signUpEmail`/`account` collection). FR-AUTH-040/041 rate limits are re-enforced in-handler. `src/lib/auth.ts`'s admin plugin/hook config is now dead-but-present pending #260/#261; `JWT_SECRET` tightened to `.min(32)` (partial #267/M3.27, which stays open). See Authentication (Admin), Admin Password Reset, and Account Self-Service below. Issue #260 / M3.22 completes the runtime cut-over: `src/middleware/rbac.ts` resolves the session through the custom engine only (`extractSessionToken`/`verifySessionToken`), dropping #258's Better Auth `auth.api.getSession` fallback — it was rbac.ts's last consumer of `src/lib/auth.ts`, and since #259 no admin session is Better-Auth-backed anyway. `src/middleware/betterAuthHandler.ts` and the `router.all("/*splat", betterAuthHandler)` catch-all in `auth.routes.ts` are removed, so Better Auth's plugin HTTP surface (including the never-rebuilt full-page Google OAuth redirect) no longer responds; `getSessionHandler`/`signOutHandler` self-terminate for an unrecognized session instead of falling through (`get-session` → `200 {success:true,data:null}`, byte-compatible with the old bridge). `auth.service.ts`/`adminUsers.service.ts`/`account.service.ts` already sat on `@/lib/session`'s `revokeAllSessionsForUser` after #258/#259, so no session-revocation code moved here. After this issue nothing under `backend/src` imports `@/lib/auth` at runtime. Issue #261 / M3.23 (also **closing #267 / M3.27**, whose JWT-session design shipped back in #257) then finishes the removal: deletes `src/lib/auth.ts`, `src/utils/sessions.ts` (its `revokeSessionsForUser` was only called by `auth.ts`'s dead hook), and `src/utils/fetchHeaders.ts` (orphaned since #260); drops the `better-auth`/`@better-auth/*` packages and `rateLimit.ts`'s now-unused `nativeIpRateLimitStorage`; removes `BETTER_AUTH_SECRET` (`JWT_SECRET` is the sole token-signing secret) and renames `BETTER_AUTH_URL` → `APP_BASE_URL`; and reverts `src/index.ts` to a plain static `import app from "./app"` (the dynamic-import + CJS-unwrap dance existed only for `auth.ts`'s module-load `mongodbAdapter` freeze). `provisionAdminUser`/the seed scripts/`adminSession.ts` were already off Better Auth since #259, so only comment cleanup there. See RBAC Guard & createdBy/updatedBy Backfill and Startup below.

The buyer shopping cart is in place (Issues #150/#151 / M4.1–M4.2, `FR-CART-001`–`018`): a new flat `src/modules/cart/` module (`GET`/`POST`/`PATCH`/`DELETE /api/cart[/items[/:variantId]]`, all `rbac(["buyer"])`-guarded) — one lazily-created persistent cart per buyer (`carts` collection, unique index on `user`), variant-only line items, dedupe-by-variant on add, quantity 1–10 rejected-not-clamped. Every read resolves `sellingPrice`/`lineTotal`/`subtotal` **live** from the referenced embedded variant (never a stored snapshot), flags a line `unavailable` when its variant is deactivated / its product isn't `published` / its quantity is over-cap, keeps unavailable lines in `items` but out of `subtotal`, and reports an `itemCount` that includes them; the empty cart is a `200` shape, never a `404`. Two new lookups on `products.repository.ts` (`findByVariantId`/`findByVariantIds`) resolve an embedded variant by its own `_id` — `cart.service.ts` imports them via the same documented peer service→repository exception `brands.service.ts` already uses. `PublicProductListItem` also gains `defaultVariantId` (the lowest-price active variant) so a buyer-app product card can drive its add-to-cart / "Go to Cart" control. See Cart below for full detail.

## How to add a new module

1. Pick a location: a product-catalog domain entity module (governs a catalog resource — brands, categories, products, and their sub-resources) goes under `src/modules/product-catalog/features/<feature>/`; a cross-cutting/infra module with no catalog-domain meaning of its own (like `health`, `uploads`) stays flat at `src/modules/<feature>/`.
2. Create that `<feature>/` directory with:
   - `<feature>.routes.ts` — pure route definitions (method + path → controller)
   - `<feature>.controller.ts` — request/response shaping, calls the service
   - `<feature>.service.ts` — business logic, orchestrates the repository
   - `<feature>.repository.ts` — Mongoose queries only (skip if the module needs no DB access)
   - `<feature>.model.ts` — Mongoose schema (skip if the module owns no collection)
   - `<feature>.module.ts` — imports `<feature>.routes.ts`'s router, exports `{ path, router }`
   - `tests/` — colocated unit tests for this module
3. Wire it into `src/routes/index.ts`: import the module and add `router.use(<feature>Module.path, <feature>Module.router)`.
4. If the module needs end-to-end coverage, add a Supertest file under the workspace-root `__tests__/<feature>/` — nested under `__tests__/product-catalog/<feature>/` for a catalog module, flat for infra, mirroring the same split as step 1 (see `__tests__/health/health.api.test.ts` for the pattern).

`src/modules/health/` is the current worked reference for a module's internal shape — copy its shape regardless of which of the two locations above the new module lands in.

## Path aliases

`@/*` maps to `src/*` (`backend/tsconfig.json`'s `paths`). Use it for any import that would otherwise need `../` parent traversal (e.g. reaching `config/`, `middleware/`, `utils/`, `externalService/` from inside a module); same-directory or one-level-down-from-`src` imports stay relative. Works everywhere: `tsc` build (rewritten to relative paths by `tsc-alias`, run right after `tsc` in the `build` script), `tsx watch` dev server (native tsconfig-paths support), and Vitest via the `vite-tsconfig-paths` plugin in `vitest.config.ts` (there is no native `resolve.tsconfigPaths` option — an earlier version of this doc claimed there was, which was wrong and left `__tests__/health/health.api.test.ts` failing until Issue #8 fixed it) — one alias defined once in `tsconfig.json`.

## Error contract

- `src/utils/AppError.ts` — `class AppError extends Error { constructor(statusCode, code, message) }`
- `src/middleware/errorHandler.ts` — 4-arg Express error middleware. `AppError` instances → `{ success:false, code, message }` at `err.statusCode`; a `ZodError` (thrown directly from a controller, e.g. `schema.parse(req.body)`) → 400 `{ success:false, code:"VALIDATION_ERROR", errors }`, where `errors` is an object keyed by field path (`issue.path.join(".")`) with that field's own Zod message as the value — no `message` key on this response; a `MulterError` (thrown by the `multer` middleware itself, before a controller runs — e.g. an oversized file) → 400 `{ success:false, code, message }`, where `code` is `"FILE_TOO_LARGE"` for `LIMIT_FILE_SIZE` and `"UPLOAD_ERROR"` for every other Multer error code; anything else → always `console.error`'d server-side first, then 500 `{ success:false, code:"INTERNAL_ERROR", message:"Internal server error" }` — except when `env.NODE_ENV === "development"`, where the response instead includes the real `message`/`name`/`stack` for local debugging. Production, `test`, and any unrecognized `NODE_ENV` value all get the generic shape (fail-safe default) — `err.stack` is never leaked to a client outside development.
- `src/middleware/notFound.ts` — catch-all for unmatched routes → 404 `{ success:false, code:"NOT_FOUND", message:"Route not found" }`.

Both are mounted in `src/app.ts`, after all routes. Because Express 5 (already a dependency) auto-forwards a thrown or rejected error from an async route handler to this middleware, a controller can call `schema.parse(req.body)` directly with no manual `try`/`catch` — see `src/modules/uploads/uploads.controller.ts` for the pattern every later M2 admin endpoint should copy.

## Success envelope

`src/utils/apiResponse.ts`'s `successResponse(data)` / `successResponse(data, pagination)` is the one place every controller builds its success response from — mirrors the error contract's single-source-of-truth shape:

- Detail: `successResponse(data)` → `{ success: true, data }`, no `pagination` key at all.
- List: `successResponse(items, { page, limit, total, totalPages, hasNextPage })` → `{ success: true, data: items, pagination }`.

Built as two overloads returning a conditionally-constructed object literal (never `pagination: undefined`) specifically because `tsconfig.base.json` sets `exactOptionalPropertyTypes: true` — assigning `undefined` to an optional field is a type error there, and would violate the "omit entirely, never `null`/`{}`" rule anyway.

`health` predates this helper and still returns its own `{ success, code, message }` shape — left as-is; it isn't a catalog resource.

## Admin routes and auth

- **`src/middleware/adminAuth.ts` (the `X-Admin-Key` shared-secret guard) no longer exists** — replaced entirely by `src/middleware/rbac.ts` as of Issue #143/M3.5. See RBAC Guard & createdBy/updatedBy Backfill below for full detail.
- `src/routes/admin.routes.ts` — a plain aggregator router mounted at `/api/admin` in `src/routes/index.ts`; every admin feature module mounts under it (`adminRouter.use(<feature>Module.path, <feature>Module.router)`) and each module's own router now carries its own `rbac(...)` guard, applied once at that router's own top (`router.use(rbac([...roles]))`) — not a single router-wide guard anymore.
- Buyer-facing (non-admin) routes mount directly on the root aggregator router in `src/routes/index.ts`, same as `health` does today.

## CORS

`src/middleware/cors.ts` wraps the `cors` package and is mounted first in `src/app.ts`, ahead of the query-parser/`express.json()`/routes — pulled forward from the SRS v0.8 security-baseline bundle (`docs/architecture.md` §6 lists it alongside helmet/rate-limiting/session cookies, which stay deferred) since `buyer-app`/`admin-app` now deploy to different origins than `backend` and can't reach it at all without this.

- **Origins are env-driven, never hardcoded**: `env.CORS_ORIGINS` (`src/config/env.ts`) is a comma-separated string, split/trimmed into an array in `cors.ts`. Defaults to `http://localhost:3000,http://localhost:5173` (buyer-app/admin-app's local dev ports) so local dev needs no `.env` changes; Render's `render.yaml` declares the var `sync: false` (same pattern as `MONGODB_URI`/`ADMIN_API_KEY`/the R2 vars) — the real deployed Vercel URLs are typed into Render's dashboard by hand, not committed.
- **`credentials: true`** — anticipates the `httpOnly`/`secure`/`sameSite` session cookies `docs/architecture.md` already plans for v0.3 Authentication; harmless ahead of that since nothing currently sends cookies.
- **`methods`/`allowedHeaders` are the actual surface, not a wildcard**: `GET/POST/PUT/PATCH/DELETE` (every HTTP method any route in this codebase uses) and `Content-Type`/`Authorization` (the latter needed since every `/api/admin/*` route now resolves a session from the `Authorization: Bearer` header via `rbac.ts` — `X-Admin-Key` was dropped from this list when Issue #143 removed `adminAuth.ts` entirely).
- Test: `__tests__/cors/cors.api.test.ts` — flat, cross-cutting-infra layout matching `__tests__/admin-auth/`, not nested under `product-catalog/`.

## R2 uploads

- `src/externalService/r2.ts` — the first client under `externalService/`. Wraps `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner`: constructs an `S3Client` pointed at `https://{env.R2.ACCOUNT_ID}.r2.cloudflarestorage.com`, exports `createPresignedPutUrl(key, contentType)` returning a 5-minute presigned PUT URL, and `uploadObject(key, body, contentType)` — a plain `PutObjectCommand` sent via `client.send()`, used by the direct-upload path below. Presigning is a local signature computation — no network call — so it's fully unit-testable with dummy credentials; only actually uploading (through the presigned URL, or via `uploadObject`) touches the network.
- `src/modules/uploads/` — `POST /presign` (mounted as `/api/admin/uploads/presign`) and `POST /direct` (mounted as `/api/admin/uploads/direct`), the module's own model/repository/service pattern, plus reusable entity-agnostic exports from `uploads.service.ts` that later modules import directly rather than duplicating:
  - `POST /direct` (SRS amendment `FR-CAT-097`–`100`, the one deliberate exception to `FR-CAT-077`'s "backend never receives raw image bytes") accepts the image itself as `multipart/form-data` (a `file` field plus a `purpose` field), parsed in-memory by `multer` (`uploads.routes.ts`, `multer.memoryStorage()`, `limits.fileSize` capped at `MAX_DIRECT_UPLOAD_BYTES` — 5 MB), then uploads the buffer to R2 itself via `issueDirectUpload()` in `uploads.service.ts`. Same content-type allow-list as presign, same `{purpose}/{uuid}.{ext}` object key format, same `presignedUploads` tracking record — a product/brand/category registration consumes a direct-upload key identically to a presigned one, since `consumeImageKeys` doesn't distinguish which path produced the key.
  - Exists specifically for clients that can't perform a direct browser-to-R2 PUT (server-side tooling, scripts, classic form submission); the presigned path (`FR-CAT-077`–`081`) remains the default/preferred path for browser clients.
  - `consumeImageKeys(keys)` — validates each object key was issued by a prior presign call and not already consumed (`FR-CAT-082`); called by product/brand/category create-and-update paths once they exist.
  - `validateImageCount(images, { min, max })` — bounds check (`FR-CAT-083`); callers pass `{min:1,max:8}` for products, `{min:0,max:2}` for variants.
  - `normalizeImages(images)` — ensures exactly one `isPrimary: true`, auto-promoting the first image if none is marked (`FR-CAT-084`). Pure function, no I/O.
  - This module intentionally doesn't expose a `/products/:id/images`-style registration endpoint — brands/categories/products don't exist as collections yet. It builds the shared pieces; #27 (brands), #28 (categories), and #31 (products/variants) wire them into their own create/update controllers.
- Issued-but-unconsumed object keys are tracked in a `presignedUploads` MongoDB collection (`uploads.model.ts`), not in memory — a TTL index on `expiresAt` auto-deletes stale entries, and the choice avoids the correctness gaps an in-memory store would have (lost on restart, not shared across instances). `uploads.repository.ts`'s `consumeByKey` is an atomic `findOneAndDelete`; not-found collapses "never issued" and "already consumed" into one rejection, matching `FR-CAT-082`'s wording without needing to distinguish the two.
- New env vars are namespaced under `env.R2` in code even though the underlying `.env` entries are flat (env files can't nest keys) — see `src/config/env.ts`: `R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET_NAME`/`R2_PUBLIC_URL_BASE` parse as flat fields, then get reshaped into `env.R2.ACCOUNT_ID` etc. before export. `R2_PUBLIC_URL_BASE` is the browser-accessible bucket URL (custom domain or `r2.dev`) used to build a registered image's stored `url`; it's distinct from the R2 API endpoint used to sign uploads, which is derived from `R2_ACCOUNT_ID`.

## Brands

`src/modules/product-catalog/features/brands/` (Issue #27 / M2.3, `FR-CAT-023`–`029`) is the first module with a real Mongoose collection beyond `uploads`' tracking one. Scope is deliberately just issue #27's own checklist — create/update/list-with-count/get/guarded-delete plus a public active-only list — not the status-toggle endpoint (`FR-CAT-047`, owned by #33) or admin search (`FR-CAT-052`, owned by #34); the SRS's endpoint table lists those near brands, but the project's issue breakdown defers them until brands+categories+products all exist, matching how categories (#28) is split too.

- **Two mount points, one module**: `brands.module.ts` exports `brandsAdminModule` (`path: "/brands"`, mounted under `adminRouter` → `/api/admin/brands`, guarded) and `brandsPublicModule` (`path: "/api/brands"`, mounted directly on the root router in `routes/index.ts`, unguarded) — the first module needing both an admin and a buyer-facing surface.
- **`src/utils/slug.ts`** (new, shared): `slugify(name)` + `generateUniqueSlug(name, slugExists)`, taking an injected `slugExists` checker rather than querying Mongoose directly, so it's DB-agnostic and reusable by categories/products. Suffixes collisions `-2`, `-3`, ... A brand's slug is generated once at create time and never touched on update (`FR-CAT-025` only lists name/logo/description as updatable).
- **`src/modules/product-catalog/features/products/` stub**: issue #31 (the real product module) doesn't exist yet, but `FR-CAT-026` (admin list's product count) and `FR-CAT-028` (delete guard) both need to query products. `products.model.ts` is a deliberately partial schema (`brand`, `status`, timestamps only — extend, don't replace, when #31 lands), and `products.repository.ts` exposes only `countByBrand(id)` / `countByBrandIds(ids)`, no service/controller/routes.
- **Cross-module repository import — the one layering exception**: `brands.service.ts` imports `countByBrand`/`countByBrandIds` directly from `@/modules/products/products.repository` rather than `brands.repository.ts` reaching into the `Product` model itself, or `brands.service.ts` skipping the repository layer entirely. Every module's repository still stays scoped to its own model; this is a peer service-to-repository import, narrow and commented where it happens.
- **`:id` route validation**: neither `health` nor `uploads` has an `:id` route, and `errorHandler.ts` has no `CastError` handling, so an invalid ObjectId string would otherwise fall through to a generic 500. `src/utils/objectId.ts`'s `parseObjectId()` checks `mongoose.isValidObjectId()` first and throws `AppError(400, "INVALID_ID", ...)` — note it also has to account for `req.params.id`'s real type, `string | string[] | undefined` (`ParamsDictionary`'s index signature under `noUncheckedIndexedAccess: true`), not just `string`. Originally inlined in `brands.controller.ts`; extracted to the shared util on categories (#28) becoming its second consumer — see Categories below.
- **`buildPublicUrl(objectKey)`**: extracted from `uploads.service.ts` (previously inlined twice) so brands' logo-URL construction, and later categories'/products' image-URL construction, doesn't duplicate the `${env.R2.PUBLIC_URL_BASE}/${objectKey}` interpolation a third and fourth time.
- **Known, accepted races (documented, not fixed)**: delete-guard TOCTOU (a product could be created between the count-check and the delete) and slug-uniqueness TOCTOU (two concurrent creates with the same name could both pass `slugExists` before either inserts, surfacing as a Mongo `E11000` → generic 500). No transactions or test-DB exist anywhere in this codebase yet; categories' `FR-CAT-019` guard will have the identical gap.
- Test layout follows the established split: `src/modules/product-catalog/features/brands/tests/brands.service.test.ts` (unit, repository/uploads-service mocked) and `__tests__/product-catalog/brands/brands.api.test.ts` (Supertest, same mocking, exercises the real Express app end-to-end).

## Categories

`src/modules/product-catalog/features/categories/` (Issue #28 / M2.4, `FR-CAT-014`–`022`) mirrors brands' layering almost exactly, but is the first module with a **hierarchy** (`parentCategory`, at most one level deep) and a **two-reason delete guard**. Scope is strictly issue #28's own checklist, same deferral as brands: no status-toggle (`FR-CAT-046`) or admin search (`FR-CAT-051`) — both owned by #33/#34, which explicitly cover brands+categories+products together.

- **`categories.description` exists even though the SRS's own schema table omits it** — added deliberately so `metaDescription`'s fallback (`FR-CAT-022`, "same behaviour as `FR-CAT-012`," which truncates a description) has something to truncate from; confirmed with the user before implementation.
- **List endpoints return a flat array with a `parentCategory` ref, not a nested tree** — despite the SRS endpoint table's "category tree" wording, matching brands' flat-array precedent; confirmed with the user since no consumer (buyer-app's catalog browsing, #35) exists yet to demand a specific nested shape.
- **Hierarchy validation — one shared `validateParentCategory(parentId, currentId?)` helper**, called from both create and update: `null` parentId is always valid; a `currentId` equal to `parentId` is a self-reference (`400 INVALID_PARENT_CATEGORY`); a parent that doesn't resolve is `404 PARENT_CATEGORY_NOT_FOUND`; a parent that itself already has a parent is `400 PARENT_CATEGORY_TOO_DEEP` (3-level chain from below). The **update-only** branch (only reachable when `currentId` is given): if the category being updated currently has subcategories of its own, assigning it a parent is `400 CATEGORY_HAS_SUBCATEGORIES` (3-level chain from above — parent → this category → its existing children). `createCategory` never hits this branch; a brand-new category can't have children yet.
- **`parentCategory` is the first tri-state field in this codebase**: absent (untouched), explicit `null` (promote to root), or an id (set/change parent) — every brands PATCH field is "if present, set" with no clear-to-null case. Validated in Zod as `objectIdString.nullable().optional()` on the update schema only, converted to `Types.ObjectId | null | undefined` in the controller before reaching the service, which distinguishes `undefined` from `null` explicitly under `exactOptionalPropertyTypes: true`.
- **`src/modules/product-catalog/features/products/products.repository.ts` grows `countByCategory`/`countByCategoryIds`**, identical shape to `countByBrand`/`countByBrandIds`; `products.model.ts` grows a required `category` field + index alongside `brand`, same extension pattern brands used first.
- **`categories.repository.ts` adds `countByParent(parentId)`** beyond brands' shape — a same-collection query (`Category.countDocuments({ parentCategory: parentId })`), used by both the update-time hierarchy guard and the delete guard. Not a cross-module exception like `countByBrand`/`countByCategory` — this one stays in-module because it queries `categories` against itself.
- **Delete guard — one combined `409 CATEGORY_IN_USE`**, not two separate throws: `Promise.all([countByCategory(id), countByParent(id)])`, then a message naming whichever of "N product(s)" / "N subcategory(ies)" actually blocks (both if both do). Matches this codebase's existing house style of reporting every blocking reason in one response (`errorHandler.ts`'s `ZodError` translation already does this for validation failures).
- **Cascade delete is deliberately not implemented**: `FR-CAT-019`'s "cascade-delete that category's specification and variant-type documents" has nothing to cascade to yet — #29/#30 don't exist. `deleteCategory` has a comment noting this is intentional, not an oversight, extend once they land.
- **Two shared utilities promoted on their second use**: `src/utils/objectId.ts`'s `parseObjectId()` (extracted out of `brands.controller.ts`, which now imports it too — no behavior change, brand tests stayed green) and `src/utils/text.ts`'s `truncate(text, maxLength = 160)` (word-boundary-safe, ellipsis-appended; reusable by products' identical `FR-CAT-012` fallback). Both follow the same "extract on second use" instinct that produced `buildPublicUrl` during brands.
- **A real bug found and fixed while building this**: `brands.repository.ts`'s `updateById` called `Brand.findByIdAndUpdate(id, patch, {new:true})` with a plain object — MongoDB treats an update document with no `$`-operator keys as a full **replacement**, not a partial update, silently dropping every field not in `patch` (including `slug`, which has no default). Both `brands.repository.ts` and `categories.repository.ts`'s `updateById` now wrap the patch in `{ $set: patch }`. No test caught this originally because every existing test mocks the repository layer entirely — worth remembering when writing repository-adjacent code, since this codebase has no real-DB test coverage to catch this class of bug.
- **A second, category-specific TOCTOU** beyond brands' delete-guard race: two concurrent `PATCH`es could each pass the has-no-children check on the same category before either write lands, both successfully assigning it a parent and producing a 3-level hierarchy despite each request validating individually. Documented in `categories.service.ts`, not fixed — no transactions or test-DB exist anywhere in this codebase.
- Test layout matches brands: `src/modules/product-catalog/features/categories/tests/categories.service.test.ts` (unit) and `__tests__/product-catalog/categories/categories.api.test.ts` (Supertest).

## Category Specifications

`src/modules/product-catalog/features/categorySpecifications/` (Issue #29 / M2.5, `FR-CAT-030`–`035`) governs what specification fields products in a category can carry, one document per category. Structurally unlike brands/categories: no `slug`, no `status`, no buyer-facing endpoint of its own, and its data is nested (groups of fields, not a flat document).

- **`GET`/`PUT`/`PATCH` only — no `POST`/`DELETE`**, matching the SRS endpoint table exactly (and the sibling `categoryVariants` resource, #30, has the identical shape). Deleting a group or field happens _through_ `PATCH`, not a separate route — the issue body's looser "POST/PATCH/DELETE routes" phrasing is not literal, same as how brands/categories already treated the SRS endpoint table as more authoritative than an issue body's phrasing in the opposite direction.
- **First single-mount-point module**: `categorySpecificationsAdminModule` only, no public counterpart — zero buyer-facing surface. Path is `/categories/:id/specifications`, a param segment mid-path rather than at the end, mounted directly on `adminRouter`; `categories.admin.routes.ts` is untouched (its own exact-match routes don't collide).
- **`Router({ mergeParams: true })` is required** — the one new, easy-to-miss bug this module introduces. Express routers don't inherit parent path params by default; without `mergeParams`, every handler's `req.params.id` would be `undefined` and every request would 400 `INVALID_ID`. Brands/categories never hit this since their `:id` was always declared inside their own router, never in a parent mount prefix. Copy this for any future module mounted the same way.
- **`GET` returns a synthetic empty default before any `PUT` has happened** — `{category, specificationGroups: []}`, no 404 (the `:id` is a _category_ id, already validated to exist; the sub-resource just hasn't been created yet). The response also drops `_id`/`createdAt`/`updatedAt` entirely, uniformly for both states — a deliberate, first-of-its-kind deviation from brands/categories' admin reads (which return the full record including those fields).
- **`PUT` is a full-replace upsert**; **`PATCH` takes one discriminated-union operation per call** (`renameGroup`/`deleteGroup`/`updateField`/`deleteField`) applied to the in-memory array and persisted through the _same_ full-replace repository function `PUT` uses — MongoDB positional-array update operators are brittle at this nesting depth for a low-traffic admin tool. Per FR-CAT-031's verbs ("update **or delete**," never "create"), `PATCH` only ever targets something that already exists; new groups/fields are added via `PUT`.
- **Deleting a group is guarded the same way deleting a field is**, even though `FR-CAT-031`'s guard sentence only names field-level deletion — resolved as an interpretive gap, not scope creep: it mirrors `FR-CAT-019`'s category-delete guard already being "blocked by what's nested beneath it," and group-deletion isn't optional scope the way brands'/categories' status-toggle and search were (deferred to #33/#34) — it's a mandatory operation whose safe-vs-unsafe behavior needed picking. `deleteGroup` iterates the group's fields through the same `countBySpecificationField` `deleteField` uses, and rejects (`409 SPECIFICATION_FIELD_IN_USE`, one code shared by both operations) naming every blocking field and its count.
- **Resolves `categories.service.ts`'s previously-deferred cascade comment**: `deleteCategory` now calls `categorySpecifications.service.ts`'s `deleteForCategory` before its own `deleteById` — safe without its own guard, since `deleteCategory`'s existing `productCount === 0` check already guarantees no product references any field in that category's schema. Variant-type cascade still waits for #30.
- **One deliberate exception to "cross-module calls go through the peer's service when one exists"**: `categorySpecifications.service.ts` validates category existence via `categories.repository.ts`'s `findById` directly, not `categories.service.ts`'s `getCategoryById` — going through the service would create a two-module circular import, since `categories.service.ts` already imports _from_ `categorySpecifications.service.ts` for the cascade. The dependency stays one-directional (categories → categorySpecifications for cascade; categorySpecifications → categories.repository for existence checks), keeping the module graph acyclic. `categorySpecifications.service.ts` throws its own `CATEGORY_NOT_FOUND` rather than reuse categories' internal helper.
- **Products stub grows a `specifications` field**, the third stub extension after `brand` (#27) and `category` (#28): `{ groupName, values: [{ name, value }] }[]`, where `value: string | number | boolean` — not just `string`, since the SRS's own worked example stores a number, a string, and a boolean in this same array. `products.repository.ts` gains `countBySpecificationField(categoryId, groupName, name)`; the `category` filter in that query is load-bearing, not defensive — two categories can each define a same-named field, and a product's specs are only ever valid against its own category's schema (`FR-CAT-034`).
- **Structural validation lives in Zod, not service-level `AppError`s**: `options` required when `type: "enum"`, `filterable` rejected when `type: "text"` are pure shape rules with no DB dependency, expressed as `.refine()` on the field schema (surfacing as `VALIDATION_ERROR`). Only rules needing the current stored document (rename collisions, not-found lookups, in-use guards) are service-level `AppError`s: `SPECIFICATION_GROUP_NOT_FOUND`, `SPECIFICATION_FIELD_NOT_FOUND`, `DUPLICATE_SPECIFICATION_GROUP`, `DUPLICATE_SPECIFICATION_FIELD`, `SPECIFICATION_FIELD_IN_USE`.
- Test layout matches brands/categories: `src/modules/product-catalog/features/categorySpecifications/tests/categorySpecifications.service.test.ts` (unit) and `__tests__/product-catalog/categorySpecifications/categorySpecifications.api.test.ts` (Supertest) — the latter also implicitly verifies `mergeParams: true` works, since a missing/broken mount would fail every test with `INVALID_ID`.

## Category Variant Types

`src/modules/product-catalog/features/categoryVariants/` (Issue #30 / M2.6, `FR-CAT-036`–`038`) governs what variant axes (e.g. `Color`, `Size`) the admin product-variant editor renders a control for, one document per category. Copies categorySpecifications' shape almost exactly, with one deliberate divergence.

- **Named `categoryVariants` (module folder, model, collection), not the issue body's `categoryVariantTypes`** — the SRS's own `categoryVariants` collection table (and its schema/index section, and a forward-reference comment already left in `categorySpecifications.model.ts` during #29) settled this name first; same "endpoint table/schema table over issue-body phrasing" call as categorySpecifications made for its own routes. The HTTP path is still `/variant-types` (`/api/admin/categories/:id/variant-types`), matching the SRS endpoint table.
- **Flat, not nested**: unlike `specificationGroups` (groups of fields), this document holds one flat `variants: VariantAxis[]` array — there's no group concept for variant axes. `VariantAxis` is `{ name, code, type: text|select|color|number (default select), required, options?: {label,value}[] }`.
- **`GET`/`PUT`/`PATCH` only — no `POST`/`DELETE`**, identical reasoning to categorySpecifications: deleting an axis happens through `PATCH`, not a separate route.
- **`Router({ mergeParams: true })`**, same requirement and same reason as categorySpecifications' router — mounted mid-path at `/categories/:id/variant-types`.
- **`GET` returns a synthetic empty default before any `PUT`** — `{category, variants: []}`, no 404 — identical to categorySpecifications' `GET` behavior.
- **`PUT` is a full-replace upsert; `PATCH` takes one discriminated-union operation** (`updateAxis` | `deleteAxis`) applied in-memory and persisted through the same full-replace repository function `PUT` uses. No `renameGroup`-equivalent op exists since there's no group layer; `updateAxis` alone covers renaming an axis (including its `code`), the same way categorySpecifications' `updateField` alone covers renaming a field.
- **Matched by `code`, not `name`**: an axis has both a display `name` and a `code`; `PATCH` operations key off `code`. Chosen over `name` specifically because `FR-CAT-037` makes it safe — this definition is never enforced against stored variant attributes, so renaming an axis (or its code) can't orphan anything downstream the way renaming a specification field could.
- **The one real behavioral divergence from categorySpecifications: `deleteAxis` (and `PATCH` generally) carries no in-use guard of any kind** — `FR-CAT-037` is explicit the definition drives admin form rendering only, never enforced against stored variant attributes, so deletion always succeeds even while products hold variants using that axis. No `products.repository.ts` query backs this module at all, unlike categorySpecifications' `countBySpecificationField`.
- **`options` structural validation mirrors categorySpecifications' `enum`/`text` split**: required (non-empty) when `type` is `select` or `color`, forbidden when `type` is `text` or `number` — expressed as two `.refine()`s on the Zod axis schema, not custom Mongoose validators, same split as categorySpecifications.
- **Resolves `categories.service.ts`'s remaining deferred cascade comment**: `deleteCategory` now also calls `categoryVariants.service.ts`'s `deleteForCategory` (alongside categorySpecifications' own, from #29) before its own `deleteById`. Needs no guard of its own to be safe — this document was never gated by product references in the first place.
- No `products.model.ts` stub growth this issue — variant axes are purely a rendering guide, so nothing on the product document references them.
- Test layout matches categorySpecifications: `src/modules/product-catalog/features/categoryVariants/tests/categoryVariants.service.test.ts` (unit) and `__tests__/product-catalog/categoryVariants/categoryVariants.api.test.ts` (Supertest).

## Products

`src/modules/product-catalog/features/products/` (Issue #31 / M2.7, `FR-CAT-001`–`013`, `FR-CAT-085`–`087`) is "the spine of M2" — the first module that's a real consumer of brands, categories, and categorySpecifications rather than just a stub they query. Scope is strictly issue #31's own checklist: no embedded-variant CRUD (`FR-CAT-039`–`044`, #32), no status-transition endpoint (`FR-CAT-045`, `PATCH .../status`, #33), no admin search (`FR-CAT-050`, #34), and no buyer-facing surface at all (`FR-CAT-054`–`056`, #35) — `products.module.ts` exports only `productsAdminModule`.

- **`sku` is immutable after create, exactly like `slug`** — `FR-CAT-004`'s editable-field list omits it, so `UpdateProductInput` has no `sku` field at all (a type-level guarantee, not just a runtime check); a client-submitted `sku` in a `PATCH` body is silently ignored by Zod's default "strip unknown keys" behavior, the same mechanism that makes a client-submitted `sellingPrice` have no effect.
- **`src/utils/pricing.ts`** (new, shared): `computeSellingPrice(mrp, discount)` implements `FR-CAT-087`'s formula (`mrp - floor(mrp * discount / 100)`) as a pure function with no I/O, deliberately placed outside the module (like `slug.ts`/`text.ts`) since #32's variant subdocuments will call the identical formula — a peer module importing into `products/` would be backwards.
- **SKU uniqueness needs two indexes plus an application-level check, not one** (`FR-CAT-003`): a unique index on `sku` and a separate unique multikey index on `variants.sku` (`productSchema.index({ "variants.sku": 1 }, { unique: true })`) can't jointly express "unique across both a scalar field and an array field on the same collection." `products.repository.ts`'s `skuInUse(sku, excludeId?)` runs `{ $or: [{ sku }, { "variants.sku": sku }] }` as the missing cross-check. `variants` was a deliberate partial stub at this point — only `{ sku: string }`, just enough to establish the index ahead of any variant-creation endpoint (an empty array contributes zero index entries, so declaring the index was safe before any variant existed); #32 fills in the rest of the subdocument — see Product Variants below.
- **Specifications validated via a peer service-to-service call in the opposite direction of the categories→categorySpecifications cascade**: `products.service.ts` imports `validateProductSpecifications` from `categorySpecifications.service.ts` (new export, alongside the existing `SpecPatchOperation` machinery). Safe without creating a cycle — `categorySpecifications.service.ts` only imports `products.repository.ts` (not `products.service.ts`), so the module graph stays acyclic: `products.service → categorySpecifications.service → products.repository`. The validator matches submitted `{groupName, name}` pairs against the category's schema, checks every `required` field is present, rejects unknown field names, and checks each value's runtime type against the field's declared `type` (`enum` also checks membership in `options`) — reporting every offending field in one `400 SPECIFICATION_VALIDATION_FAILED`, not just the first, matching this codebase's established "name every blocking reason" house style (`categories`' combined `CATEGORY_IN_USE`, `categorySpecifications`' combined field list).
- **`FR-CAT-034`'s re-validation runs whenever `category` _or_ `specifications` changes on update, not only when both do**: `updateProduct` fetches the existing product first specifically so it can fall back to the _other_ field's current stored value — moving `category` alone re-validates the product's existing (unchanged) specifications against the new schema; changing `specifications` alone re-validates the new values against the current (unchanged) category.
- **First module to actually consume `uploads.service.ts`'s `validateImageCount`/`normalizeImages`/`consumeImageKeys`** with real bounds (`{min:1, max:8}`, `FR-CAT-083`) — brands/categories only ever handled at most one image each, so this is the first `1–8` array case those functions were written for back in #26. Image-count bounds are enforced once, in the service via `validateImageCount` (surfacing `IMAGE_COUNT_OUT_OF_BOUNDS`), not duplicated as a Zod range check — `products.controller.ts`'s Zod schema validates each image's _shape_ only, so there's exactly one authoritative source for the bounds error rather than two competing error codes for the same rule.
- **The first paginated, sortable admin list in this codebase** (`FR-CAT-005`) — brands/categories/categorySpecifications all return their full list unpaginated. `GET /api/admin/products` uses `apiResponse.ts`'s existing `successResponse(items, pagination)` overload (defined since #25/M2.1, unused until now) for the first time. Query params: `page`/`limit` (Zod-clamped: `limit` max `100`, both integers ≥ 1 — an oversized `limit` is rejected with `VALIDATION_ERROR`, not silently clamped, an interpretive call since the SRS only specifies this behavior for _buyer_-facing pagination), and `sort` — an allow-listed enum (`createdAt`/`name`/`mrp`/`stock`, each with an optional `-` prefix for descending; default `-createdAt`) rather than free-form field/order params, since the SRS names no specific sort keys for the admin grid beyond "sortable." No `search` query param — that's `FR-CAT-050`'s admin-search scope (#34), same deferral brands/categories already made.
- **`lowStock` list filter** (`FR-CAT-011`, presence-based — `?lowStock=true`) is the one query filter beyond pagination/sort this issue adds, since the field is explicitly in `FR-CAT-001`–`013`'s range and would otherwise ship unused; implemented as `$expr: { $lte: ["$stock", "$lowStockThreshold"] }` in `products.repository.ts`'s `listPaginated`, since comparing two fields of the same document can't be a plain query-operator filter.
- **`DELETE` is an unconditional status flip to `archived`, not a guarded delete** — unlike brands/categories, nothing downstream references products yet (Orders is v0.5), so there's no in-use count to check. A nonexistent id `404`s (`PRODUCT_NOT_FOUND`) rather than silently succeeding — a deliberate difference from brands'/categories' guarded deletes, which silently succeed on a nonexistent id because their guard count is naturally zero either way; a plain status flip has no such "vacuous success" to fall back on.
- **`DUPLICATE_SKU` is `400`, not `409`** — modeled as an input-validation-style rejection (like categorySpecifications'/categoryVariants' `400 DUPLICATE_*` codes for a payload-level name collision), not a `409`-style "blocked by something that already depends on this" guard (`BRAND_IN_USE`, `CATEGORY_IN_USE`, `SPECIFICATION_FIELD_IN_USE`) — the two express different situations even though both are "already in use."
- **Brand/category reference validation reuses `brands.service.ts`'s `getBrandById`/`categories.service.ts`'s `getCategoryById` directly** (both already throw `BRAND_NOT_FOUND`/`CATEGORY_NOT_FOUND`) rather than re-implementing existence checks against their repositories — the first module to validate a foreign reference this way; safe for the same acyclic-graph reason as the specifications validator, since neither brands' nor categories' service imports `products.service.ts` (only `products.repository.ts`, for their own delete-guard counts).
- Test layout matches the established split: `src/modules/product-catalog/features/products/tests/products.service.test.ts` (unit, all five peer modules — `products.repository`, `uploads.service`, `brands.service`, `categories.service`, `categorySpecifications.service` — mocked) and `__tests__/product-catalog/products/products.api.test.ts` (Supertest; `uploads.service` is a _partial_ mock via `importOriginal` here, since the real Express app also boots `uploads.routes.ts`, which reads `UPLOAD_PURPOSES` off the same module at import time — a full replacement mock would crash route registration, not just this module's own tests).

## Product Variants

Issue #32 / M2.8 (`FR-CAT-039`–`044`) fills in the `variants` array `products.model.ts` stubbed to just `{ sku }` back in #31 — the full embedded subdocument (`attributes`, `images`, `mrp`/`discount`/`sellingPrice`, `stock`, `weight?`, `active`) — and adds `POST /api/admin/products/:id/variants` / `PATCH /api/admin/products/:id/variants/:variantId` to `products.admin.routes.ts`. No new module: variants live entirely inside `products.service.ts`/`.controller.ts`/`.repository.ts`, since they're embedded, not a standalone collection.

- **Both endpoints return the full updated product**, not a variant on its own — there's no standalone variant resource to `GET` independently; the response shape matches create/update's.
- **`products.repository.ts` grows `replaceVariants(id, variants)`**, a full-array replace — same reasoning as categorySpecifications'/categoryVariants' `replaceGroups`/`replaceAxes`: MongoDB positional array-update operators are brittle at this nesting depth for a low-traffic admin tool, so `products.service.ts` loads the product, mutates a plain in-memory copy of `variants`, and persists the whole array in one write. A new variant's `_id` is assigned explicitly via `new Types.ObjectId()` in the service rather than relied on from Mongoose's update-cast subdocument defaulting, which is unreliable for a `$set`-based array replacement (unlike `.push()` on a live document).
- **A variant's `sku` is editable, unlike the parent product's own** — `FR-CAT-004`'s editable-field list explicitly excludes the product's `sku`, but `FR-CAT-040`'s "admin can update a variant" names no such exclusion, so `UpdateVariantInput.sku` is present and optional, re-validated identically to create whenever it changes. A deliberate asymmetry, not an oversight — documented here specifically because it's easy to assume it should mirror the immutable product `sku`.
- **The SKU cross-check needs a three-part check, not just `skuInUse`** — `skuInUse(sku, excludeId)` excludes the _entire_ product document by id, so it correctly rules out every _other_ product's own `sku` and variants, but says nothing about collisions within the same product (the parent's own `sku`, or a sibling variant's `sku`) since those live inside the excluded document. `assertVariantSkuAvailable()` checks those two cases locally against the already-`findById`-fetched product before falling through to `skuInUse` for the "everywhere else" case — all three share one `400 DUPLICATE_SKU`.
- **Attribute-set duplication (`FR-CAT-041`) is checked via a canonical, order-independent key**: `attributeSetKey()` sorts each variant's `name=value` pairs and joins them, so `[Color=Red, Size=L]` and `[Size=L, Color=Red]` collide identically. Checked against every _other_ variant on the product regardless of `active` — the issue's own task list is explicit that both active and inactive variants count, unlike some of this codebase's guards that only consider live state.
- **Variant images reuse the product's own `resolveImages` shape but with a bespoke `resolveVariantImages` bound**: `{min: 1, max: 2}` only engages once at least one image is submitted (`FR-CAT-064`'s "0 or 1-2") — an empty array is valid and short-circuits before `validateImageCount` is even called, unlike the parent product's `images`, which is always required to be 1-8.
- **Deactivation is just `active: false` on the same `PATCH`**, not a separate endpoint or a `DELETE` — `FR-CAT-040` doesn't split it out, and a variant is never hard-removed from the document regardless of `active`'s value; the array length is unchanged either way.
- **`FR-CAT-043` (purchasability) and `FR-CAT-044` (visibility inheritance) are read-time, buyer-facing rules with no code added in #32 itself** — there's no buyer endpoint yet (`#35`) to compute "which unit is purchasable" or filter variants by the parent's `status` against. #32's job is limited to storing the data faithfully (`active`, per-variant `images`/pricing/`stock`) so `#35` can apply both rules once it exists — same deferral shape as `#30`'s `FR-CAT-038` (admin-editor rendering needed no backend code either).
- Tests are added to the existing files, not new ones — `addVariant`/`updateVariant` `describe` blocks in `products.service.test.ts`, and matching `POST`/`PATCH .../variants` blocks in `products.api.test.ts` — since this is an extension of the products module, not a new one.

## Variant-Only Pricing (Issue #102, SRS v0.2 amendment)

Issue #102 collapses the "zero-variant products sell on their own sku/price/stock, one-or-more-variant products sell per variant" model `#31`/`#32` originally shipped (`FR-CAT-043`) down to **variant-only**: `products.model.ts`'s `ProductDocument` drops `sku`/`images`/`mrp`/`discount`/`sellingPrice`/`stock`/`lowStockThreshold` entirely — a product is now pure metadata (name/description/brand/category/specifications/SEO/`isFeatured`). Stock/inventory tracking and the derived buyer-facing `availability` enum (`src/utils/availability.ts`, `FR-CAT-095`/`096`) are removed **system-wide**, not moved to the variant — this system tracks no inventory quantity at all, admin or buyer-facing.

- **A product needs ≥1 active variant to publish** (`FR-CAT-043`) — `updateProductStatus()` now does an extra `findById` + `variants.some(v => v.active)` check only when the target status is `"published"`, throwing a new `400 PRODUCT_HAS_NO_VARIANTS` otherwise. `"draft"`/`"archived"` transitions are unaffected — this is a publish-time guard, not a standing invariant enforced on every variant write, so deactivating the last active variant on an already-published product isn't separately blocked (a documented, accepted gap, same "TOCTOU documented, not fixed" treatment this module already gives other races).
- **Variant `images` is now schema-`required`, 1–2, unconditionally** (no more `default: []` on the schema, no more `resolveVariantImages`' empty-array short-circuit) — since the product no longer has any images of its own, there's no parent gallery left for a zero-image variant to fall back to (`FR-CAT-064`'s old last sentence is gone).
- **`skuInUse()` and `assertVariantSkuAvailable()` lose their product-`sku` half** — `FR-CAT-003`'s "shared namespace" collapses to `variants.sku` alone, now the sole SKU-uniqueness source (index and application-level check both). The `variants.sku` unique multikey index is technically sufficient on its own now; the application-level `skuInUse` check stays anyway, for a friendly `400 DUPLICATE_SKU` instead of a raw Mongo `E11000` surfacing as an uncaught `500`.
- **Buyer-facing list/detail mappers (`toPublicListItem`/`toPublicDetail`) source image and price from `selectDefaultVariant()` unconditionally** — no more `defaultVariant ? … : product.…` fallback branch, since there's no product-level value left to fall back to. `PublicProductListItem`'s `mrp`/`discount`/`sellingPrice`/`primaryImage` and `PublicProductDetail`'s `mrp`/`discount`/`sellingPrice`/`defaultVariantId` are all now optional, genuinely absent (not `null`/`0`) on the one edge case above (a published product that ends up with zero active variants). `PublicProductDetail` also drops its old top-level `sku`/`images` — both stay per-variant only (`variants[].sku`/`.images`), since a multi-variant product has no single unambiguous value for either.
- **`buildMatchStage()`'s price-range/on-sale filters move from top-level `sellingPrice`/`discount` fields into one `$elemMatch` on `variants`** (alongside `active: true`) — a single variant must satisfy price range **and** on-sale together when both are requested, not two different variants independently. `buildSearchFilters()`'s Atlas-only variant-attribute `embeddedDocument` filter gains the identical price/on-sale clauses inside its own `compound.filter` when combined with an attribute filter, so the plain and Atlas query paths stay semantically consistent.
- **Price sorting (`price_asc`/`price_desc`) is no longer a plain `.sort({sellingPrice})`** — there's no top-level field to sort by. Both `listPublicPaginated()` and `searchPublicPaginated()` gain a `priceSortStages()` case: `$addFields` a computed `sortPrice` (`$min` of each product's active variants' `sellingPrice` — the "starting from" price, same key for both directions, just the order flipped) then `$sort` on it. `listPublicPaginated()` branches into a small aggregation (`$match` → `priceSortStages` → `$facet`) only for this case; every other sort keeps the original plain `.find().sort()` path. A new shared `runFacetedAggregate()` helper (pull `$facet` items/count apart, then `Product.populate()` manually) is reused by this new branch and by `searchPublicPaginated()`'s pre-existing aggregation tail.
- **Admin list**: `?lowStock=` and the `mrp`/`stock` sort options are gone (`ProductSortField` is now just `"createdAt" | "name"`); `search`'s SKU half now targets `variants.sku` instead of the removed `sku` field.
- **`PATCH /api/admin/products/:id/stock` is deleted outright** — `updateStockSchema`/`updateStockHandler`/`updateStock()` (service) all removed, along with the route.
- **`backend/src/scripts/seed/generate.ts`** (local dev catalog seeding, predates this doc's own coverage) updated to the new shape — no product-level `sku`/`images`/`mrp`/`discount`/`stock`/`lowStockThreshold`, no variant `stock`.
- No index-field changes to `backend/atlas-search/products-search-index.json` — `sku`/`stock`/`sellingPrice` were never mapped fields there to begin with (only `name`/`description`/`variants.*`/`specifications.*` are).

## Status Updates

Issue #33 / M2.9 (`FR-CAT-045`–`049`) adds a dedicated `PATCH .../status` to each of the three entities that had been deliberately deferring one since brands (#27) and categories (#28) first drew the "status-toggle is #33's job" line. No new modules — one new route/handler/service-function trio per existing module.

- **Products get a tri-state setter, brands/categories get a boolean toggle** — matching each entity's own `status` type, not a uniform shape. `products.model.ts` exports a new `PRODUCT_STATUSES = ["draft", "published", "archived"] as const`, used both by the Mongoose schema's `enum` and by `products.controller.ts`'s `z.enum(PRODUCT_STATUSES)`, so the two can't drift apart — the first time this codebase has extracted a shared enum-of-truth like this (the schema previously just inlined the three literals).
- **`products.service.ts`'s `deleteProduct` is refactored to call the new `updateProductStatus(id, "archived")`** rather than duplicating the same `updateById(id, {status: "archived"})` call — `DELETE` is now just `updateProductStatus` under a delete-shaped name, not two independent code paths that happen to do the same write.
- **`UpdateBrandDoc`/`UpdateCategoryDoc` grow a `status?: boolean` field** (`Partial<CreateBrandDoc> & {status?: boolean}` / same shape for categories) — `status` was never part of either entity's `Create*Doc` (both are always created active), so this couldn't reuse the existing `Partial<>` alone, same pattern `UpdateProductDoc` already used for its own `status` since #31.
- **Deactivating never touches, checks, or bypasses the entity's own delete guard** (`FR-CAT-048`) — `updateBrandStatus`/`updateCategoryStatus` are both a plain `updateById(id, {status})` with nothing else, no `countByBrand`/`countByCategory` call anywhere in the function. This is the literal reading of "does not bypass the guard": the guard is simply never in this code path's way, since deactivating and deleting remain two entirely different operations with different preconditions — a brand/category with products can always be deactivated, only never _deleted_ while products reference it.
- **`FR-CAT-049`'s "a product in a deactivated category/brand stays reachable by its own slug but drops from that category's/brand's buyer-facing listings" needs no code in #33** — there's no buyer product-listing endpoint yet (`#35`) for a deactivated category/brand to disappear from. `listCategoriesForPublic`/`listBrandsForPublic` (both already filter `status: true`, since #27/#28) already give #35 everything it needs to read; #33 only had to add the write path.
- Test layout matches each entity's own existing split: a new `describe("updateProductStatus", ...)` / `describe("updateCategoryStatus", ...)` / `describe("updateBrandStatus", ...)` block in each module's existing `tests/*.service.test.ts`, and a matching `PATCH .../status` `describe` block in each module's existing `__tests__/*.api.test.ts` — no new test files, mirroring how #32 extended products' existing files rather than creating new ones.

## Admin Search

Issue #34 / M2.10 (`FR-CAT-050`–`053`) adds a `?search=` query param to all three existing admin list endpoints. No new modules, same "extend, don't create" shape #32/#33 already established — one new repository/service/controller parameter per module, reusing each list's existing unfiltered/paginated query.

- **`src/utils/text.ts` grows `escapeRegExp(text)`** (new, shared) — escapes every regex metacharacter so user-submitted search text is matched literally rather than interpreted as a pattern. All three modules' search clauses build on it; this is deliberately a plain MongoDB regex query, not Atlas Search, matching the SRS's decision to confine Atlas Search to buyer-facing paths (`FR-CAT-050`'s own wording) and keep all three admin searches on one mechanism.
- **Categories/brands (`FR-CAT-051`/`052`): a single case-insensitive, unanchored regex on `name`.** `categories.repository.ts`'s and `brands.repository.ts`'s existing unfiltered `list()` each grow an optional `search?: string` parameter, threaded straight through from `listCategoriesForAdmin(search?)`/`listBrandsForAdmin(search?)` to a new `?search=` param on `listCategoriesQuerySchema`/`listBrandsQuerySchema` in each controller. No status filter was added to either — the issue's own test criteria only asks for a status filter on the product grid, not categories/brands.
- **Products (`FR-CAT-050`): a two-part `$or` — name is the identical unanchored case-insensitive regex, sku is anchored at the start (`^`) with no case-insensitive flag**, so pasting a full sku returns exactly that product (exact match) while a partial sku also matches as a prefix. Composed into `products.repository.ts`'s existing `listPaginated` query object as a sibling key alongside `lowStock`'s `$expr` and the new `status` field filter — MongoDB allows all three on the same query object with no `$and` needed, since each occupies a distinct key.
- **`FR-CAT-053`'s status filter is product-only, not a third search-composing feature added to categories/brands.** `products.repository.ts`'s `ProductListFilter` grows `status?: ProductStatus`, `products.controller.ts`'s `listQuerySchema` grows `status: z.enum(PRODUCT_STATUSES).optional()`, and search stays over all statuses by default (the grid's existing all-statuses admin visibility rule, unchanged since #31) unless `status` is also present — the two filters are independent query keys, not mutually exclusive, so a request narrows by whichever of them it supplies.
- Test layout matches #33's precedent: new cases added to each module's existing `tests/*.service.test.ts` / `__tests__/*.api.test.ts` files (no new test files), plus a new `describe("escapeRegExp", ...)` block in `src/utils/tests/text.test.ts`. As with every prior list endpoint in this codebase, the repository layer itself stays mocked in tests — the regex-construction logic is exercised only via `escapeRegExp`'s own unit tests and by each layer above the repository asserting the right filter object reaches it.

## Shared Admin List Query Parsing

Issue #104 (SRS v0.2 amendment) extracts a shared `src/utils/parseQuery.ts` and uses it to bring categories'/brands' admin lists up to pagination/sort parity with products' — both had returned an unfiltered, unpaginated full array via the 1-arg `successResponse(data)` overload since #27/#28. Products' own sort wire format changes from a combined `?sort=-field` enum to two params, matching the new shared shape. No new modules — one new repository/service/controller signature change per module, same "extend, don't create" shape #32–#36 already established.

- **`parseQuery.ts` is pure and deliberately non-throwing** — `errorHandler.ts`'s `ZodError` branch is the only code path in this codebase producing `VALIDATION_ERROR` (with a field-keyed `errors` object); a hand-thrown `AppError(400,"VALIDATION_ERROR",...)` from inside a shared util would produce a different response shape for the identical code. So each controller's own Zod schema (`z.enum(<SORT_FIELDS>)` for `sortBy`, `z.enum(["asc","desc","none"])` for `orderBy`, `.max(100)` for `limit`) is what rejects bad input, exactly like products' pre-#104 `sort` enum already did — `parseQuery(search, pagination, sortBy, orderBy, field, filter?)` only ever shapes already-valid input into `{filter, sort?, page, limit}`; `field` (the caller's sortable-field allow-list) is consulted only as a redundant, unreachable-once-Zod-has-run defensive guard and to type the returned `sort`.
- **The returned `sort` shape, `{field, order}`, deliberately mirrors `ProductListSort` exactly** — `products.repository.ts`'s `listPaginated` needed no shape change to its `sort` parameter, only widening it to `| undefined` (`orderBy:"none"` means no `.sort()` call at all, not a fallback default).
- **Products' call passes `search: undefined`** — its search behavior (name: unanchored case-insensitive regex; sku: prefix-anchored, no case-insensitive flag) is asymmetric across fields and doesn't fit `parseQuery`'s generic same-regex-every-field merge, so that logic stays exactly as-is, untouched, inside `products.repository.ts`'s `listPaginated`. Only `sortBy`/`orderBy`/`field`/`pagination` are exercised for products' call; `PRODUCT_SORT_FIELDS = ["createdAt", "name"] as const` (new, exported from `products.repository.ts`) replaces the controller-local `SORT_VALUES`/`parseSort` as the single source of truth. Default `sortBy:"createdAt"`/`orderBy:"desc"` reproduces the old `-createdAt` default exactly.
- **Categories/brands _do_ use the generic `search`** (single field `["name"]`, functionally identical to their existing single-field regex match) — `CATEGORY_SORT_FIELDS = ["name", "sortOrder", "createdAt"] as const` and `BRAND_SORT_FIELDS = ["name", "createdAt"] as const` (new, exported from each repository; brands has no `sortOrder` field to sort by). `orderBy` defaults to `"none"` with no `sortBy` default on both — a deliberately conservative choice for a genuinely new capability, preserving each endpoint's pre-#104 "no guaranteed order" behavior when a caller sends nothing.
- **`categories.repository.ts`'s and `brands.repository.ts`'s `list()` both change shape**: from `list(search?: string): Promise<Record[]>` to `list(filter: QueryFilter<Document>, sort, page): Promise<{items, total}>` (`QueryFilter` is Mongoose's own exported type, not a `parseQuery.ts` addition) — filter (built by `parseQuery`, name search folded in) and sort now arrive pre-built from the controller, the same layering `products.repository.ts` already used. Confirmed each had exactly one caller (`listCategoriesForAdmin`/`listBrandsForAdmin`) before changing the signature. `listCategoriesForAdmin`/`listBrandsForAdmin` (service) now return `{items, pagination}` instead of a plain array, and their controllers switch to the 2-arg `successResponse(items, pagination)` overload.
- **`src/utils/apiResponse.ts` grows `buildPagination(page, limit, total): Pagination`** — extracted from `products.service.ts`, which had this exact formula duplicated twice already (a private buyer-facing copy and an inline copy in `listProductsForAdmin`) before this issue would have added a third and fourth for categories/brands. All four call sites now share the one implementation.
- **Known, accepted, documented gap: `admin-app` truncation risk.** `admin-app`'s `categoriesApi.ts`/`brandsApi.ts`/`CategoryList.tsx` currently expect the _entire_ unpaginated array back from `GET /api/admin/categories`/`GET /api/admin/brands` — now that both paginate with a default `limit: 20` (same default as products, for consistency across all three admin lists — a deliberate choice over a defensively-high default), `admin-app` will silently render only the first 20 categories/brands until a follow-up frontend issue updates it to actually page through results. This mirrors #102's own precedent of shipping a backend contract change ahead of its frontend consumer, except this one is a silent data-truncation risk on a screen that already exists and is in use today, not just a shape change on an unbuilt screen — flagged here prominently rather than left implicit.
- Test layout matches #34's precedent: new cases added to each module's existing `tests/*.service.test.ts` / `__tests__/*.api.test.ts` files (no new test files), plus a new `src/utils/tests/parseQuery.test.ts` (one `describe("parseQuery", ...)` block, mirroring `text.test.ts`'s per-behavior `it` style).

## Seeding

`backend/src/scripts/seed/` has two entry points sharing the same `data.ts`/`generate.ts` foundation, Issue #106 adding the second:

- **`npm run seed --workspace backend`** (`run.ts`, unchanged) — destructive, full reset: `deleteMany`s `Brand`/`Category`/`Product` and reinserts everything from `data.ts`'s static templates. Never touches `categorySpecifications`/`categoryVariants` at all.
- **`npm run seed:upsert --workspace backend`** (`upsert.ts`, new) — idempotent, safe to re-run: find-or-creates/updates-in-place across all five catalog models, matched by each model's own unique index (`slug` for brands/categories/products, `category` for `categorySpecifications`/`categoryVariants`). No `deleteMany` anywhere. Re-running twice produces the same document ids and no duplicates — only embedded `variants[]._id`s regenerate each time (variants are always fully rebuilt, not diffed).
- **`generate.ts` gained three exports Issue #106 introduced**: `buildProductFields` (extracted from `createProductBuilder`'s closure — the pure "fields from an already-decided slug/sku" builder, shared by both entry points), `buildProductFieldsDeterministic` (the upsert path's slug/sku source — `slugify(name)` directly, no `Set`-based dedup, safe because every product template's name is unique across `data.ts`), and `buildVariantAxesForKind` (derives a category's `categoryVariants` axis definitions from the same `kind` that already drives `buildVariantList`, reusing its existing `STORAGE_PAIRS`/`CONFIG_PAIRS`/`COLOR_PAIRS`/`COLOR_TRIOS` constants).
- **`data.ts` gained `CategoryNode.specificationGroups`** — one `"Technical Specifications"` group per leaf category (the 9 nodes that carry `kind`+`products`), authored for the first time by this issue since nothing seeded `categorySpecifications` before.
- **Deliberate boundary: seeded products still carry `specifications: []`**, exactly as before Issue #106 — this issue seeds category-level specification/variant-axis _schema_ (the same thing the admin UI's `PUT .../specifications` / `PUT .../variant-types` define), not per-product specification _values_. Matching each product's specs to its category's schema is a materially larger follow-on task, out of scope here.
- `upsert.ts`'s functions (`upsertBrand`/`upsertBrands`, `upsertCategory`/`upsertCategories`, `upsertCategorySpecifications`, `upsertCategoryVariants`, `upsertProduct`/`upsertProducts`) are all exported for standalone reuse, not only reachable via the CLI entry point.
- No new Vitest tests — this area of the codebase has zero prior test coverage (`data.ts`/`generate.ts`/`run.ts` were never covered either), and the issue's own test criteria is manual/CLI-level (run twice, confirm no duplicates; confirm `npm run seed` still works).

## Newest Sort By Primary Variant (Issue #121)

Issue #121 was filed against `buyer-app` (observed as inconsistent Home Page card ordering) but its actual fix is entirely here — the buyer-facing `?sort=newest` default has no product-level field to sort by reliably since `#102` made products variant-only.

- **The real bug, found while investigating**: `products.repository.ts`'s `replaceVariants(id, variants)` persists the _entire_ `variants` array in one `$set` every time `addVariant`/`updateVariant` runs. Empirically confirmed against a real product: every variant's `createdAt` was identical and matched the product's own `updatedAt` from the most recent variant write — Mongoose's subdocument-array casting re-stamps `createdAt`/`updatedAt` on every element that arrives without one, every time the whole array is `$set`, regardless of whether that element was actually touched. Also empirically confirmed: passing `{timestamps:false}` to `Product.findByIdAndUpdate` suppresses only the _document's_ own auto-timestamp bump, not the subdocument-array schema's own casting-time defaulting — it doesn't fix this on its own.
- **Fix: `replaceVariants` now goes through the raw MongoDB driver** (`Product.collection.updateOne(...)`, then a plain `findById`), deliberately bypassing Mongoose's schema casting entirely — verified empirically (a disposable test product, `create → add two variants → inspect → delete`) that this persists exactly what the caller supplies, nothing more. `addVariant`/`updateVariant` (`products.service.ts`) are the caller: a new variant gets fresh `createdAt`/`updatedAt`; an edited variant keeps its original `createdAt` but bumps `updatedAt`; untouched sibling variants are left exactly as fetched. `ProductVariant`'s TS type (`products.model.ts`) gained explicit `createdAt: Date`/`updatedAt: Date` fields to support this — both were always present at runtime via the subdocument schema's own `{timestamps:true}`, just untyped until now.
- **"Primary variant" has no prior definition in this codebase** (unlike images, variants have no `isPrimary` flag) — defined identically to the already-established `selectDefaultVariant()` (`products.service.ts`): the lowest-`sellingPrice` _active_ variant. A new shared `newestSortStages()` (`products.repository.ts`, mirroring `priceSortStages()`'s existing `$addFields`-then-`$sort` shape) reimplements that same selection rule in aggregation syntax via `$sortArray`/`$first` (MongoDB ≥5.2; confirmed 8.0 on this Atlas cluster), extracting that variant's `createdAt` into a computed `sortCreatedAt` field to sort on. A product with zero active variants gets a missing `sortCreatedAt` and sorts last under `-1` — not specially handled, consistent with how this codebase already treats that documented edge case elsewhere (e.g. `toPublicListItem`'s absent price/image fields).
- **Wired into both `listPublicPaginated`'s non-price-sort branch and `searchPublicPaginated`'s newest-fallback branch** — fixing only the former (Home Page's own literal path) would have left Search's "newest" fallback ordering the same products differently for the identical underlying reason, an inconsistency judged worse than the small extra diff of sharing the fix. `listPublicPaginated` is now always aggregation-based (the old plain `.find().sort({createdAt:-1})` path is gone) — both price sorts and newest now need a computed-per-product sort key derived from `variants`, since there's no top-level field left to sort by directly.
- **Deliberately out of scope**: the admin list's own `?sortBy=createdAt` (still the product's own top-level field) and exposing `createdAt` on the buyer API response (nothing renders it — this only ever affected sort order, not a displayed value) — both per an explicit scoping decision, not an oversight.
- Test coverage: two new cases in `products.service.test.ts`'s existing `addVariant`/`updateVariant` `describe` blocks, asserting the exact variant array passed to the (mocked) `replaceVariants` — a new variant's fresh/matching `createdAt`/`updatedAt`, an edited variant's preserved `createdAt` alongside a bumped `updatedAt`, and an untouched sibling's timestamps left exactly as they were. No dedicated `newestSortStages()` test — same as `priceSortStages()`, this codebase has no existing precedent for testing aggregation pipeline shape/output directly (the repository layer stays mocked everywhere else too); manually verified end-to-end instead, against the real database and running API, before merge.

## Buyer Browsing

Issue #35 / M2.11 (`FR-CAT-054`–`067`, `FR-CAT-095`–`096`) is the first buyer-facing surface for products — everything before this issue was admin-only (`productsAdminModule` was the only export from `products.module.ts`). No new module: buyer routes/handlers/service functions extend the existing `products`/`categories` modules, same "extend, don't create" shape #32–#34 already established, plus two new shared utilities.

- **`src/utils/availability.ts`** (new, shared): `deriveAvailability(stock, lowStockThreshold)` implements `FR-CAT-096`'s per-unit formula (`0` → `out_of_stock`; `0 < stock ≤ lowStockThreshold` → `low_stock`, inclusive; else `in_stock`) and `bestAvailability(states)` picks the best of a list (`in_stock` > `low_stock` > `out_of_stock`). Every buyer response substitutes this enum for the raw `stock`/`lowStockThreshold` fields it's never allowed to leak (`FR-CAT-095`). **Variants carry no `lowStockThreshold` of their own** — every per-unit calculation in this module, whether the unit is the product itself or one of its variants, reuses the _parent product's_ single threshold, since the schema has nowhere else to store one; a deliberate interpretive call, documented at each call site.
- **First use of Mongoose `.populate()` in this codebase**: every prior admin read was content with a raw `brand`/`category` ObjectId ref; buyer responses need the referenced document's own `name`/`slug`. `products.repository.ts` exports a `PopulatedRef` (`{_id, name, slug}`) and a `PublicProductDoc` type (`ProductRecord` with `brand`/`category` replaced by `PopulatedRef`), and every buyer-facing repository query chains `.populate("brand", "name slug").populate("category", "name slug")` before `.lean()`.
- **Two query shapes for listing, not one**: `listPublicPaginated(filter, page)` is a plain `.find({status:"published", ...})` — used for the flat `GET /api/products` (no `?q=`) and the category-scoped `GET /api/categories/:slug/products`. `searchPublicPaginated(q, filter, page)` (`FR-CAT-065`) is a genuinely different aggregation pipeline — `$search` must lead an aggregation, incompatible with layering onto a plain `.find()` — using `$facet` to run the paginated slice and the total count in one round trip, then `Product.populate()` on the raw aggregate result (aggregation doesn't run schema-level populate automatically the way `.find().populate()` does).
- **The Atlas Search index this depends on is a real Atlas-cluster provisioning step, not something this issue's code can create** — `backend/.env`'s local `MONGODB_URI` points at a plain `mongodb://localhost` instance, and Atlas Search is an Atlas-cluster-only feature (it doesn't exist against local/self-hosted MongoDB at all, Community Edition included). `backend/atlas-search/products-search-index.json` + `backend/atlas-search/README.md` check in the index definition (named `products_search`, `dynamic:false` over `name`/`description`) and the manual steps to apply it via the Atlas Console or CLI once pointed at a real cluster. Until then, `?q=` requests error at the database level — a known, accepted gap, same "documented, not fixed" treatment this codebase already gives its other infra-dependent gaps (e.g. brands'/categories' delete-guard TOCTOU races).
- **`GET /api/categories/:slug/products` (`FR-CAT-055`) is wired from `categories.public.routes.ts`, but its controller (`listProductsByCategorySlugHandler`) lives in `products.controller.ts`** — the response is product data, not category data, so the controller stays with the data it returns; only the route's _wiring_ crosses modules, imported directly into `categories.public.routes.ts`. Category resolution itself (`categories.service.ts`'s new `getActiveCategoryBySlug`/`listActiveSubcategoryIds`) is active-only and 404s as `CATEGORY_NOT_FOUND` on a miss — a deactivated category's slug shouldn't resolve to a buyer-visible product listing either, and categories are at most two levels deep, so "direct active children" already covers the whole subtree.
- **`GET /api/categories/search` (`FR-CAT-066`)** reuses `categories.repository.ts`'s `listActive(search?)` (already extended with an optional search param during #34) via a new `searchPublicCategoriesHandler`, distinct from the existing `listPublicCategoriesHandler` only in that `q` is _required_ here (this route exists specifically to search) versus optional on the plain list. Deliberately still a plain regex query, not Atlas Search — `FR-CAT-066`'s own wording: `categories` is too small a collection to warrant a second search index.
- **Buyer pagination clamps an oversized `limit` instead of rejecting it (`FR-CAT-057`)** — the opposite of the admin list's `.max(100)` → `VALIDATION_ERROR`. `products.controller.ts`'s buyer query schemas have no `.max()` at all; a new `clampLimit()` does `Math.min(limit, PUBLIC_PAGE_SIZE_MAX)` after parsing instead. `PUBLIC_PAGE_SIZE_DEFAULT`/`MAX` are `24`/`48` — the SRS's own worked JSON example uses `limit: 24`; no maximum is specified anywhere, so `48` (2×) is this issue's own interpretive choice.
- **List item shape follows the SRS's own worked example, minus `cardSpecifications`** — `FR-CAT-091`/`092` (card content: primary image, name, `sellingPrice`, and — on category cards — the first four `filterable` specifications in schema order) are explicitly **not** this issue's scope; the GitHub issue body's own `Implements` line names `FR-CAT-054`–`067`/`095`–`096` only. `PublicProductListItem` carries `_id, name, slug, brand{_id,name,slug}, primaryImage?, mrp, discount, sellingPrice, availability, isFeatured` — everything the worked example shows except `cardSpecifications`, which is `#36`'s job.
- **List-view vs. detail-view availability are deliberately different computations**, both reachable from `FR-CAT-096`'s single sentence: list items use `bestAvailability()` across every _active_ variant (`FR-CAT-096`'s literal "product-level availability is the best state across active variants" — one badge per card). The detail page's _initially-displayed_ availability (`FR-CAT-064`) is narrower: the state of whichever single unit is currently selected — the default variant if one exists, else the product's own stock — not a rolled-up best-of-everything value, since `FR-CAT-064` describes a value that changes as the buyer picks a different variant, not a static badge.
- **`FR-CAT-064`'s default-variant selection**: the lowest-`sellingPrice` _active_ variant (`selectDefaultVariant()`). Inactive variants are excluded from selection **and** from the buyer-facing `variants[]` array entirely — an interpretive call, since nothing in `FR-CAT-040` states whether an inactive variant is buyer-visible at all, and there's no reason to surface one the buyer can't select. When at least one active variant exists, the detail response's top-level `mrp`/`discount`/`sellingPrice`/`availability` reflect that default variant, not the parent product's own stored fields — matching `FR-CAT-064`'s "defaults to... and updates the displayed price/availability/images" literally. `defaultVariantId` is present only when a default variant exists; omitted (not `null`) otherwise, same `exactOptionalPropertyTypes: true` convention every other optional field in this codebase follows.
- **A variant with no images of its own falls back to the parent's images (`FR-CAT-064`'s last sentence)**, resolved server-side in `toPublicVariant()` so buyer-app consumers never duplicate the fallback themselves.
- **`FR-CAT-012`'s meta-description fallback is implemented for products for the first time here** — the field has existed on the schema since #31, but nothing read it until this issue built products' first buyer-facing read path. Identical formula to categories' own fallback (`metaTitle ?? name`, `metaDescription ?? truncate(description)`) via the same `src/utils/text.ts`; a product's `description` is always required (unlike categories', which is optional), so there's no further name fallback needed on that side.
- **`FR-CAT-063`: the detail page's `specifications` field is a verbatim pass-through of every stored group/value**, filterable or not — unlike list items, the detail response draws no distinction; filtering _which_ specs are filterable is `#36`'s concern (`FR-CAT-072`), not a detail-page display rule.
- **A new shared `src/utils/routeParams.ts`'s `parseSlugParam()`** mirrors `objectId.ts`'s `parseObjectId()` for this codebase's first `:slug`-based routes — same reasoning (validate the route param's shape before it reaches the service, and the same `string | string[] | undefined` real type under `noUncheckedIndexedAccess: true`), but a malformed/missing slug is `400 INVALID_SLUG`, not `404` — `404` stays reserved for a well-formed slug that simply doesn't match any record, so a client can tell "you sent garbage" apart from "that product doesn't exist."
- Test layout: new buyer-facing `describe` blocks added to each module's existing `tests/*.service.test.ts` / `__tests__/*.api.test.ts` files (no new test files, same #33/#34 precedent), plus a new `src/utils/tests/availability.test.ts`. The repository layer stays mocked in every test, same as every prior list endpoint — the Atlas Search aggregation pipeline's actual query shape is therefore unverified against a real Atlas cluster by this test suite, same "documented gap" as the index provisioning itself.

## Buyer Filtering & Sorting

Issue #36 / M2.12 (`FR-CAT-068`–`076`, `FR-CAT-091`–`092`) is the last M2 issue — it extends #35's two buyer listing endpoints (`GET /api/products`, `GET /api/categories/:slug/products`) with a full filter/sort surface and adds `cardSpecifications` to every list item. No new modules, same "extend, don't create" shape every M2 issue since #32 has followed.

- **Two query paths, same split #35 already drew, just with a richer boundary**: `buildMatchStage()` (new, `products.repository.ts`) builds the plain-MongoDB side of the filter — category/brand/price-range/in-stock/on-sale — shared by both `listPublicPaginated` and `searchPublicPaginated`'s post-`$search` `$match` stage. Variant-attribute (`FR-CAT-071`) and filterable-specification (`FR-CAT-072`) filters are Atlas-only (`buildSearchFilters()`), since matching "name X and value Y on the _same_ array element" needs `embeddedDocument`/`embeddedDocuments` operators Atlas Search provides and a plain query's `$elemMatch` can only approximate with more code — **submitting either one now forces the Atlas Search path even with no `?q=` at all**, a new routing condition beyond #35's original "only `?q=` triggers Atlas Search."
- **`in_stock` (`FR-CAT-073`)**: no variants → the product's own `stock > 0`; with variants → at least one _active_ variant has `stock > 0`. Expressed as a two-branch `$or` in `buildMatchStage()`, mirroring `FR-CAT-096`'s own availability rule (best state across active variants) but as a boolean gate rather than an enum.
- **Price sorting always targets `sellingPrice`, never `mrp`** (`FR-CAT-075`) — `sortSpec()`'s `price_asc`/`price_desc` cases. `"relevance"` only means something alongside `$search`'s own score order; both the plain `.find()` path and an Atlas query with no `q` (attribute/spec filters only, nothing to rank by) fall back to `newest` instead of trying to sort by a score that was never computed.
- **`?category=<slug>` is new on the _flat_ `GET /api/products`** (`FR-CAT-070`), resolved via the identical `getActiveCategoryBySlug`/`listActiveSubcategoryIds` pair the dedicated nested route already used — the two aren't duplicated, `listPublicProductsCore()` (new, `products.service.ts`) is the one implementation both `listPublicProducts`/`listPublicProductsByCategorySlug` now funnel into, differing only in whether `categorySlug` arrives via a query param or is already fixed by the route.
- **Filterable-specification filter validation is category-scoped, not global** (`FR-CAT-072`/`035`): `categorySpecifications.service.ts`'s new `getFilterableFieldsByCategory(categoryId)` is only consulted when the listing resolves to exactly one category (the nested route, or `?category=` on the flat one) — an unscoped, catalog-wide listing has no single schema to validate against, so a spec filter there is matched literally rather than rejected, a deliberate simplification (documented at `resolveSpecFilters()`) rather than a security gap: the filter can only ever narrow results, never expose more than a plain query already could. When a category _is_ known, an unfilterable or nonexistent field name, or a value/range shape mismatched to the field's actual type (`enum`/`boolean` → value match, `number` → range, per `FR-CAT-035`), is rejected as `400 INVALID_SPECIFICATION_FILTER`.
- **Spec filters matched by name alone, not `groupName` + name** — same simplification `categorySpecifications.service.ts`'s card-field lookup (below) already makes: the buyer-facing filter rail has no reason to expose group structure, and categorySpecifications' own duplicate-name guard is scoped to a single group, not the whole category, so this accepts a rare same-name-different-group collision rather than complicating the filter shape to rule it out.
- **`variantAttribute` is never validated against `categoryVariants`' axis definitions** — consistent with #30's own established rule that this definition drives admin form rendering only and is never enforced against stored variant attributes; a filter naming an axis that was never defined (or has since been renamed) just matches nothing, same as any other filter with no matches (`FR-CAT-058`).
- **`cardSpecifications` (`FR-CAT-091`/`092`) is always present on every list item**, even as `[]` on a home/all-products listing or a category with no filterable fields — per the SRS, home and category views share one response shape, and it's `buyer-app`'s job to ignore the field on the home grid rather than the API omitting it contextually. `categorySpecifications.service.ts`'s new `getCardFieldsByCategoryIds()` batch-fetches the first four filterable fields (in schema declaration order) for every distinct category on a result page in one query — `products.service.ts`'s `attachCardSpecifications()` — then `buildCardSpecifications()` looks up the _product's own_ value for each; a field the product carries no value for is skipped entirely, never padded with a placeholder, matching "a product carrying fewer than four filterable specifications renders only those it has."
- **`unit` is explicitly `null`, not omitted, on a `cardSpecifications` entry with no unit** — the one field in this codebase that breaks from the usual "omit an absent optional field" convention, chosen to match the SRS's own worked JSON example precisely (`"unit": null`) rather than this module's default.
- **A real, pre-existing bug found and fixed while building this**: Express 5's default query parser (`"simple"`, Node's built-in `querystring`) doesn't support the bracket notation (`?spec[RAM]=8GB`, `?spec[ScreenSize][min]=6`) this issue's dynamic specification filters need — Express 4's `"extended"` (`qs`-based) default was dropped, and `qs` is no longer bundled as a dependency. `src/app.ts` now calls `app.set("query parser", "extended")`, with `qs`/`@types/qs` added as explicit `backend` dependencies (previously only present transitively). No prior route needed structured/nested query params, so nothing else in this codebase depended on the old default.
- **The Atlas Search index definition (`backend/atlas-search/products-search-index.json`) grows two `embeddedDocuments`-typed fields**, `variants` and `specifications`, alongside #35's original `name`/`description` — see `backend/atlas-search/README.md` for the updated field-by-field explanation. Same unprovisioned-cluster caveat as #35: this repo has no Atlas access, so the index mapping and the `embeddedDocument` query operators `buildSearchFilters()` builds against it are this codebase's best-faith translation of Atlas Search's documented syntax, not verified against a live cluster.
- Test layout matches #35's precedent: new cases added to each module's existing `tests/*.service.test.ts` / `__tests__/*.api.test.ts` files (no new test files) plus new `describe` blocks in `categorySpecifications.service.test.ts` for the two new lookups. Same repository-stays-mocked caveat as #35 — the Atlas Search filter-clause construction is exercised only via the service/controller layers asserting the right filter object reaches the (mocked) repository call, never against a real index.

## Cart

`src/modules/cart/` (Issues #150/#151 / M4.1–M4.2, `FR-CART-001`–`018`) is the buyer shopping cart — the first M4 module and the first `src/modules/<feature>/` flat module since `uploads` (it's neither a catalog entity nor cross-cutting infra; it's its own commerce domain, and a single module, so it doesn't get a `product-catalog/`-style `features/` grouping). Standard model/repository/service/controller/routes/module shape.

- **Single mount point, buyer-only**: `cart.module.ts` exports `{ path: "/api/cart", router }`, mounted directly on the root router in `src/routes/index.ts` (after `express.json()`, next to `account`), **not** under `/api/admin`. `cart.routes.ts` applies `router.use(rbac(["buyer"]))` once at the top — every endpoint 401s (`UNAUTHENTICATED`) without a buyer session, 403s (`FORBIDDEN`) for any non-buyer role, including super-admin (`FR-CART-002`, SRS v0.4 §7: nobody manages another person's cart, there is no admin surface at all).
- **`carts` collection**: `{ user: ObjectId (ref User, required, unique), items: [{ variant: ObjectId, quantity: Number 1-10 }] }`, `{ timestamps: true }`. The `unique` on `user` is the Baseline NFR's required index (no sparse/guest variant — every cart belongs to a real buyer). **No price/availability/`expiresAt` is stored** — all derived live at read time.
- **`MAX_QUANTITY_PER_VARIANT = 10`** (`cart.model.ts`) is the single source of truth, reused by the Zod schemas and the add-accumulation check. `FR-CART-005`'s cap is a real business rule (bulk-buy/scalping mitigation): a request outside 1–10 is **rejected outright** (`VALIDATION_ERROR` for the literal body value, `QUANTITY_OUT_OF_RANGE` for a combined line that would exceed 10), never clamped.
- **Repository**: `findByUser` (plain read, no creation — `GET`), `getOrCreateByUser` (atomic `findOneAndUpdate` upsert — lazy creation on first add, `FR-CART-001`, race-safe against the unique index), `replaceItems` (full-array `$set` replace — same low-traffic reasoning as `categorySpecifications`' `replaceGroups`; the service mutates a plain in-memory copy of `items` and persists the whole array).
- **Variant resolution — two new methods on `products.repository.ts`**: `findByVariantId(id)` / `findByVariantIds(ids)` — `Product.findOne/find({ "variants._id": … })`. There's no standalone variant collection, so this is a query into the embedded array. Neither filters on `status`/`active` — the cart deliberately keeps a since-deactivated line, flagging it rather than dropping it, so the caller needs the full document. `cart.service.ts` imports these directly from `products.repository` (the documented peer service→repository cross-module exception `brands.service.ts` established with `countByBrand`; graph stays acyclic — products repo imports nothing from cart).
- **`buildCartResponse(cart | null)`** is the one place every response is shaped (SRS v0.4 §5). `null` (or an empty `items`) → `{ items: [], itemCount: 0, subtotal: 0 }` (`id` included only when a doc exists) — `200`, never `404` (`FR-CART-016`). Otherwise: bulk-fetch parent products, index every variant by `_id.toString()`, then per line derive `unavailable = !resolved || product.status !== "published" || !variant.active || quantity > MAX` (`FR-CART-012`/`013`), `sellingPrice` from the live variant, `lineTotal = unavailable ? 0 : sellingPrice * quantity`. `subtotal` = Σ `lineTotal` (unavailable lines already 0, `FR-CART-011`); `itemCount` = Σ `quantity` across **all** lines incl. unavailable (`FR-CART-017`) — the two deliberately tell different stories (badge vs. what checkout charges). `data` never carries a `pagination` key (`FR-CART-018`).
- **Line `variant` sub-object**: `{ id, sku, product: { id, name, slug }, attributes, primaryImage }` — `primaryImage` via the `images.find(isPrimary) ?? images[0] ?? null` rule (same as `products.service.ts`'s private `getPrimaryImage`; a trivial one-liner, replicated locally rather than exported). A line whose variant no longer resolves to any product at all (parent hard-deleted — rare, since delete is a soft status flip) gets an `orphanVariantView` with empty strings and is always `unavailable`.
- **Error codes**: `VARIANT_NOT_FOUND` (400, `addItem` — the `variantId` matches no variant, `FR-CART-009`), `QUANTITY_OUT_OF_RANGE` (400, `addItem` — combined line quantity > 10), `CART_ITEM_NOT_FOUND` (404, `updateItem`/`removeItem` — the variant isn't a line in this buyer's cart, or the buyer has no cart). `updateItem` with `quantity: 0` removes the line (`FR-CART-006`, equivalent to `DELETE .../items/:variantId`). `clearCart` is safe to call with no cart doc.
- **`PublicProductListItem` gains `defaultVariantId`** (`products.service.ts`'s `toPublicListItem`) — the lowest-price active variant's id (already computed there via `selectDefaultVariant`), so a `buyer-app` product card can POST that variant and detect the "already in cart" state without a per-card call (`FR-CART-021`). Absent on the same zero-active-variant edge case the price fields are.
- **Tests**: `src/modules/cart/tests/cart.service.test.ts` (unit — `cart.repository` + the two `products.repository` lookups mocked) and `__tests__/shopping-cart/cart/cart.api.test.ts` (Supertest, **real DB end to end** via `bootstrapMemoryMongo`/`signInBuyer` — unlike the catalog admin suites this one seeds real `Product` docs and exercises real cart persistence + live resolution, since that's the actual behavior under test). Plus a `defaultVariantId` assertion added to `products.api.test.ts` and the exact-match list-item expectation in `categories.api.test.ts` updated.
- Postman: `docs/postman/shopping-cart/cart.api.md` (+ `docs/postman/README.md` index).

## Orders (Issues #154–#159, #121, SRS v0.5)

> **Doc-sync gap**: this section was written during M6/Payments' own status-sync pass, after finding M5's own doc PR (`b000b31`) had updated only `docs/milestone.md`. It's a good-faith summary at the level of detail directly verified against the real source (models, routes, the state machine) — treat it as accurate but not as exhaustively cross-checked against every M5 commit as the sections above are for M2–M4.

`backend/src/modules/addresses/` (Issue #154, `FR-ORD-028`–`033`) is a small, standalone buyer address book — `{user, fullName, phone, line1, line2?, city, state, pincode, isDefault}`, no reference back to `orders` at all; checkout copies these fields onto an order as an immutable snapshot rather than referencing the document live.

`backend/src/modules/orders/` (Issues #155–#159) is the checkout/order-lifecycle module:

- **Checkout (`POST /api/orders`, Issue #155)** reuses `cart.service.ts`'s own `getCart()` for pricing/availability re-validation — calling it fresh at the top of checkout *is* the commit-time re-validation `FR-ORD-025` asks for, since `getCart()` already resolves everything live on every call; an unavailable line is silently dropped (reported back as `droppedItems`, not an error) rather than blocking the whole checkout. Each surviving line is snapshotted into an `OrderItemSnapshot` (`{product:{id,name,slug}, variant:{id,sku,attributes,image}, unitPrice, quantity, lineTotal}`) — a frozen copy, never a live reference, so a later catalog change (rename, reprice, archive) can't alter an existing order. The shipping address is snapshotted the same way, from either a saved `addressId` or an inline `shippingAddress`. `orderNumber.ts`'s `allocateOrderNumber()` is this codebase's first sequential-counter idiom (`Counter` collection, atomic `$inc` under an upsert, `TC-<year>-<seq>` format) — every other entity uses `ObjectId` as its primary key; the sequence is global and never resets per year.
- **State lifecycle (`orders.stateMachine.ts`, Issue #156, `FR-ORD-008`)**: `ORDER_STATUSES = ["pending_payment","paid","processing","shipped","delivered","cancelled","refunded"]`. `pending_payment → paid → processing → shipped → delivered`; `cancelled` reachable only from `pending_payment`/`paid`; `refunded` only from `paid` or later. `assertTransition(from, to)` throws `409 INVALID_ORDER_TRANSITION` on an illegal move; `orders.service.ts`'s `transitionOrder(orderId, toStatus, options?)` is the **single write path** for every status change anywhere in this codebase — validates via `assertTransition`, appends to `statusHistory`, enqueues a notification. `markOrderPaid(orderId, paymentId)` already existed here, calling `transitionOrder(orderId, "paid")`, but was deliberately left unwired until M6/Payments' `verifyPayment`/webhook handlers finally called it. Same issue adds a BullMQ-backed auto-cancel sweep (`src/lib/queue.ts`, this codebase's first BullMQ usage) for orders stuck in `pending_payment` past a timeout.
- **Buyer routes (Issue #157)**: `GET /api/orders` (paginated history), `GET /api/orders/:id` (ownership-filtered detail), `POST /api/orders/:id/cancel` (self-cancel, legal only from `pending_payment`/`paid` per the state machine) — all `rbac(["buyer"])`, mounted on `orders.routes.ts`.
- **Admin routes (Issue #158)**: `GET /api/admin/orders[/:id]` (list/detail, no ownership filter), `PATCH /api/admin/orders/:id/status` (advance via `transitionOrder`, optionally setting `trackingReference` on the transition into `shipped`), `POST /api/admin/orders/:id/cancel` (admin cancel with a required reason) — all `rbac(ORDER_ADMIN_ROLES)` (`order-manager`/`super-admin`; a `catalog-manager` session is rejected `403`, the reciprocal of the boundary v0.3 drew around catalog routes), mounted on `orders.admin.routes.ts`.
- **Notification emails (Issue #159)**: `orders.notifications.ts` sends an email on exactly four transitions — `paid`/`shipped`/`delivered`/`cancelled` (`NOTIFIABLE_STATUSES`) — via new `sendOrderConfirmationEmail`/`sendOrderStatusEmail` exports on `externalService/mailer.ts`; every other transition (e.g. `processing`) is silently skipped, enqueued through the same BullMQ queue Issue #156 introduced rather than sent synchronously from the request.

## Payments (Issues #164–#170, SRS v0.6)

SRS v0.6's Razorpay integration — payment initiation, client-side verification, a webhook as the source of truth, admin refunds, and a retry/error-contract hardening pass. Built entirely against dummy Razorpay credentials (`env.RAZORPAY.{KEY_ID,KEY_SECRET,WEBHOOK_SECRET}`, required in `env.ts`); real end-to-end verification against Razorpay's test-mode API is deferred to the user configuring real keys.

- **`src/externalService/razorpay.ts`** (Issue #164) — the third external-service client, alongside `r2.ts`/`mailer.ts`: `createRazorpayOrder`/`createRazorpayRefund` (real network calls via the `razorpay` SDK, mocked wholesale in every test) and `verifyPaymentSignature`/`verifyWebhookSignature` (pure `crypto.createHmac` + `crypto.timingSafeEqual` comparisons, zero network — exercised for real in tests against the dummy secrets).
- **`src/modules/payments/`** (Issue #164) — a new flat module, `payments` collection: one document per Razorpay order-attempt against a given order (`{order, razorpayOrderId, razorpayPaymentId?, razorpaySignature?, amount, currency, status, refunds[], webhookEvents[]}`, `PAYMENT_STATUSES = ["created","authorized","captured","failed","refunded","partially_refunded"]`). **`payments.amount`/`refunds[].amount` are integer paise — the one place in this codebase that isn't whole rupees** (`orders.totalAmount` and every other money field are); the conversion (`Math.round(order.totalAmount * 100)`) happens exactly once, in `initiatePayment`.
- **`POST /api/orders/:id/payment`** (`rbac(["buyer"])`, mounted on `orders.routes.ts` since both new endpoints are order-scoped and that router already carries the buyer/ownership guard they need identically, not a separate payments router) mints a Razorpay order for a `pending_payment` order and returns `{razorpayOrderId, amount, currency, keyId}` — never the secret. Idempotent: a re-call while the latest attempt is still non-`failed` reuses it (no duplicate Razorpay order); a re-call after a `failed` attempt mints a fresh one (`FR-PAY-011` retry).
- **`POST /api/orders/:id/payment/verify`** (`rbac(["buyer"])`) verifies the Checkout widget's success callback against a genuinely computed HMAC signature. Valid → persists `razorpayPaymentId`/`razorpaySignature`, sets the payment `captured`, calls `markOrderPaid(orderId, razorpayPaymentId)` — finally wiring the reserved seam M5's `orders.service.ts` left unconnected. Invalid → sets the payment `failed`, throws `400 PAYMENT_VERIFICATION_FAILED`; the order stays `pending_payment` so the buyer can retry.
- **`src/modules/webhooks/`** (Issue #166, new, minimal module) — `POST /api/webhooks/razorpay`, mounted in `routes/index.ts` **ahead of** the shared `express.json()` (mirroring the `authModule` precedent) with its own route-scoped `express.raw({type:"application/json"})`, since signature verification needs the exact signed bytes. **No `rbac()` at all** — authenticated by signature alone (`FR-PAY-024`). Missing signature header → `400 MISSING_WEBHOOK_SIGNATURE`; mismatched → `400 INVALID_WEBHOOK_SIGNATURE`. Handles `payment.captured` (mirrors the verify endpoint's success path) and acknowledges (`200`) any other event or one referencing an order it has no matching payment for. **Idempotent**: each processed event's id is appended to the matching payment's `webhookEvents[]` (unique multikey index) — a redelivered event is a no-op `200`.
  - **A real bug found and fixed by Issue #168's own error-envelope audit**: `handleRazorpayWebhookHandler` originally did `(req.body as Buffer).toString("utf8")` unconditionally — but `express.raw()` only populates `req.body` as a `Buffer` when the request's `Content-Type` matches `application/json`; a request with no matching content-type (the exact shape of the "missing signature" test case) leaves `req.body` `undefined`, so `.toString()` threw a `TypeError` before `handleRazorpayWebhookEvent`'s own missing-signature guard ever ran — surfacing as an uncaught `500` instead of the intended `400`. Fixed with `Buffer.isBuffer(req.body) ? req.body.toString("utf8") : ""`.
- **`POST /api/admin/orders/:id/refund`** (Issue #167, `rbac(ORDER_ADMIN_ROLES)`) — body `{amount?, reason}`, `amount` optional integer paise (omitted = full remaining balance). Validates the payment is `captured`/`partially_refunded` and the requested amount doesn't exceed what's left, calls `createRazorpayRefund`, appends to `payments.refunds[]`, updates `payments.status` to `refunded`/`partially_refunded`. Calls `transitionOrder(orderId, "refunded", {note: reason})` **only on a full refund** — a partial refund leaves the order's own status untouched, since the state machine has no "partially refunded" order status; reuses the state machine's already-correct `paid`/`processing`/`shipped`/`delivered` → `refunded` edges with zero changes needed there.
- **Issue #168 (polish/audit, no new endpoints)**: `getPaymentSummary(orderId)`/`getPaymentSummariesByOrders(orderIds)` (new, `payments.service.ts`) resolve a lightweight `PaymentSummary` (`{status, amount, razorpayPaymentId?}`, `null` if no attempt yet) attached to every buyer/admin order read (`getOrderHandler`, `getAdminOrderHandler`, `listAdminOrdersHandler`) — wired at the **controller** layer specifically to avoid a service-to-service circular import between `orders` and `payments`. Confirms and tests the retry-after-failure path (`FR-PAY-011`). Also fixed the webhook `req.body` bug documented above.
- **A genuine, twice-hit test-infrastructure bug found while getting all 5 payments Supertest suites green in CI**: a partial mock — `vi.mock("@/externalService/razorpay", async (importOriginal) => ({...await importOriginal(), createRazorpayOrder: vi.fn()}))` — forces eager evaluation of the real module (and transitively `@/config/env`) the moment the test file's own static import runs, which happens **before** `bootstrapMemoryMongo()` has set the real `MONGODB_URI`, freezing it on `vitest.config.ts`'s placeholder (`ECONNREFUSED ::1:27017`) — the identical bug class `orders.service.ts`'s own dynamic-import comments already document hitting twice before. Fixed per-file: `paymentInitiation.api.test.ts` (needs no real crypto) switched to a full mock; `paymentVerification`/`paymentWebhook`/`refunds`/`paymentPolish` (which do need `verifyPaymentSignature`/`verifyWebhookSignature` exercised for real) switched to full mocks that **reimplement** those two functions' HMAC logic directly against `node:crypto` and the same dummy test secrets, rather than importing the real module at all.
- Test layout: `src/modules/payments/tests/payments.service.test.ts` (unit) + `src/externalService/tests/razorpay.test.ts` (unit, the pure-crypto functions) + `backend/__tests__/payments/{paymentInitiation,paymentVerification,paymentWebhook,refunds,paymentPolish}/*.api.test.ts` (real-DB Supertest, same `mongodb-memory-server`/`bootstrapMemoryMongo` harness every M3+ admin suite uses).
- Postman: not yet written as a dedicated `docs/postman/payments/` folder — a follow-up, per this repo's own Issue #224-established convention that every new/updated endpoint gets its Postman doc in the same PR (not done here; flagged as a known gap, not silently skipped).

**M6 (Payments) is complete** — Issues #164 through #170 are all merged.

## Dashboard (Issues #171–#173, SRS v0.7)

A read-only aggregation layer introducing **no new primary collection** — every endpoint reads and aggregates data already owned by `orders`/`payments`/`products`/`categories`/`brands`. New flat `backend/src/modules/dashboard/` (not nested under `orders`/`payments`/`product-catalog` — a cross-cutting read layer, matching `webhooks/`'s own precedent of a small standalone module that delegates into other modules' repositories rather than owning data).

- **`src/lib/cache.ts`** (Issue #171, new) — a 60s TTL `getOrSetCache(key, ttlSeconds, compute)` helper, mirroring `rateLimit.ts`'s own client-construction pattern exactly: its own independently-constructed `Redis` client (`env.REDIS_URL && !isTestEnv ? new Redis(...) : null`), an in-memory `Map` fallback otherwise. TTL-only invalidation (`FR-DASH-014`) — nothing ever explicitly busts a key; no order/payment write anywhere in this codebase touches this cache.
- **`src/utils/dateRange.ts`** (new) — `resolveDateRange(from?, to?)`: both omitted defaults to the last 30 days (`FR-DASH-002`); a reversed range or one spanning more than a year is rejected outright (`400 INVALID_DATE_RANGE`/`RANGE_TOO_LARGE`), never clamped (`FR-DASH-019`). `resolveBucket(range)`: `<=31` days buckets by day, otherwise by week (`FR-DASH-005`). `generateBucketKeys(range, bucket)` enumerates every bucket key between `from`/`to` (including an ISO-week computation matching MongoDB's own `$dateToString` `"%G-W%V"` format exactly) so the service layer can zero-fill gaps the aggregation itself only returns matched rows for (`FR-DASH-018`).
- **`payments.model.ts` gains a `capturedAt?: Date` field**, set inside `markCaptured()` alongside the existing `status`/`razorpayPaymentId` `$set` — distinct from `updatedAt`, which a later `addRefund()`'s own `$set` re-bumps, which would otherwise make a date-range-scoped revenue figure inaccurate once a payment has since been refunded.
- **`GET /api/admin/dashboard/summary`** (`rbac(ORDER_ADMIN_ROLES)`) → `{range, totalOrders, totalRevenue, ordersByStatus}` — `totalRevenue` is captured payments in range minus refunds processed in range (`orders.repository.ts`'s new `countAndRevenueInRange`, `payments.repository.ts`'s new `sumCapturedInRange`/`sumRefundsInRange`), net of both, in rupees (`FR-DASH-001`/`003`/`004`).
- **`GET /api/admin/dashboard/sales`** → `{range, bucket, series:[{date,revenue,orders}]}` — bucketed by each order's own `createdAt` (not `payments.capturedAt` — a distinct, order-volume-over-time view from the summary's payments-based net-revenue figure), zero-filled for every bucket in the range (`orders.repository.ts`'s new `salesOverTimeInRange`, `FR-DASH-005`/`018`).
- **`GET /api/admin/dashboard/top-products`** → `{range, products:[{productId,name,slug,unitsSold,revenue}]}` — top 10 by revenue within the range, tiebroken by units sold, sourced from each order's own frozen `OrderItemSnapshot` (never a live product lookup, matching every other order-history read in this codebase) via `orders.repository.ts`'s new `topProductsInRange` (`FR-DASH-006`/`017`).
- **`GET /api/admin/dashboard/catalog-summary`** (Issue #172, `rbac(CATALOG_ADMIN_ROLES)` — the reciprocal role boundary from the three sales routes above, so `dashboard.routes.ts` moved from one router-wide `rbac()` guard to a per-route guard) → `{totalProducts, productsByStatus, totalCategories, activeCategories, totalBrands, activeBrands}` — live counts via new `products.repository.ts`'s `countByStatusGroups()` and `categories.repository.ts`'s/`brands.repository.ts`'s `countTotalsByStatus()`, no denormalized counter anywhere (`FR-DASH-007`/`020`/`021`). **Deliberately no `outOfStockCount` field at all** — SRS v0.10 (Inventory Management) isn't built yet; this ships the other counts now and omits the field entirely (not zero-filled) per the issue's own explicit scope boundary, not range-scoped at all (a live snapshot).
- **`GET /api/account/dashboard`** (Issue #173, `rbac(["buyer"])`, added to the existing `account` module rather than a new one — a session-scoped "my own account" read, same shape as `GET /api/account/profile`) → `{profile, recentOrders, lifetimeOrderCount, lifetimeAmountSpent}` — `account.service.ts`'s new `getAccountDashboard(userId)` composes the existing `getProfile`, `orders.service.ts`'s existing `listOrdersForBuyer(userId, 1, 5)` for the 5 most recent orders, and a new `orders.repository.ts`'s `lifetimeStatsForUser(userId)` (count + sum `totalAmount` for non-`cancelled`/`pending_payment` orders) whose returned `orderIds` feed a new `payments.repository.ts`'s `sumRefundsForOrders(orderIds)` to net refunds out of `lifetimeAmountSpent` — the identical net-of-refunds treatment `getSalesSummary` gives the admin dashboard's own revenue figure. Cached per-user via #171's `getOrSetCache` (`dashboard:account:${userId}`, 60s TTL). A brand-new buyer with zero orders gets a `200` empty-state shape (`recentOrders: []`, both counts `0`), never an error (`FR-DASH-010`–`012`).
- **Money units**: every dashboard figure is whole rupees, matching every other feature in this codebase except `payments` itself — SRS `FR-DASH-016`'s claim that dashboard responses are paise is treated as doc drift (the same category of stale-SRS-claim gap found during M6): any figure sourced from `payments.amount`/`refunds[].amount` (paise) is divided by 100 once, at the aggregation boundary (`dashboard.service.ts`'s `paiseToRupees`).
- Test layout: `src/utils/tests/dateRange.test.ts` + `src/lib/tests/cache.test.ts` (unit, pure logic), `__tests__/dashboardSales/salesDashboard.api.test.ts` + `__tests__/dashboardCatalog/catalogSummary.api.test.ts` + `__tests__/dashboardAccount/accountDashboard.api.test.ts` (real-DB Supertest, same `mongodb-memory-server`/`bootstrapMemoryMongo` harness every M3+ admin suite uses) — covering role-boundary rejections in both directions, revenue-net-of-refunds correctness, zero-filled series gaps, top-products tiebreak ordering, the catalog summary's explicit absence of `outOfStockCount`, and the buyer dashboard's empty-state shape for a brand-new buyer.
- **A real env-freeze test bug found and fixed while writing the sales-dashboard Supertest suite** — the identical bug class M6's own payments suites already hit twice: a static top-level `import { resetDashboardCache } from "@/lib/cache"` forces `@/config/env` to evaluate (since `cache.ts` itself imports `env` at module load) before `bootstrapMemoryMongo()`'s `beforeAll` has set the real `MONGODB_URI`, freezing it on `vitest.config.ts`'s placeholder for the whole test file and breaking every subsequent `connectDB()` call with `ECONNREFUSED`. Fixed by dynamically importing `resetDashboardCache` inside `beforeEach`, after the real `MONGODB_URI` is already set.

**M7 (Dashboard) is complete** — Issues #171 through #173 are all merged (backend half; #174/#175 are `admin-app`/`buyer-app`).

## Inventory (Issues #189–#190, SRS v0.10)

A fixed 2–3 warehouse set with stock tracked per `(variant, warehouse)`, reinstating the concept Issue #102 removed system-wide when it stripped every `stock`/`lowStockThreshold` field off `products`/its variants entirely. **M10 (Inventory Management) is complete** — Issues #189 and #190 (backend) are merged; #191/#192 are `admin-app`/`buyer-app`.

- **New flat `src/modules/inventory/`** (Issue #189, not nested under `product-catalog/` — a cross-cutting stock layer that reads/writes `products`/`carts` but owns its own two collections, matching `webhooks/`'s/`dashboard/`'s own precedent for a small standalone module) owns **two** collections in one module — a deliberate departure from the one-collection-per-module norm every catalog module before it followed, justified because an `inventory` row is meaningless without its `warehouse` and neither has an independent lifecycle:
  - `warehouses.model.ts` — `{name, code, active}`, `{timestamps:true}`. `POST`/`GET /api/admin/warehouses` (`rbac(CATALOG_ADMIN_ROLES)`) — **create and list only, no edit/delete/status-toggle at all**, matching the SRS's own small-fixed-set scope; `code` is unique, rejecting a duplicate with `400 DUPLICATE_WAREHOUSE_CODE`.
  - `inventory.model.ts` — `{productId, variantId, warehouseId, stock}`, unique compound index on `{variantId, warehouseId}` (`FR-INV-002`'s "no unassigned stock, no duplicate row" guarantee). `GET /api/admin/inventory?warehouseId=&search=&page=&limit=` (`rbac(CATALOG_ADMIN_ROLES)`) — a paginated table joining product name / variant sku / warehouse name via three small batched lookups (mirroring `attachCardSpecifications`'s per-page enrichment shape, not a heavier aggregation `$lookup` pipeline); `search` matches product name (regex) or variant sku (exact-or-prefix), the identical asymmetric convention `products.repository.ts`'s own admin search established. `PATCH /api/admin/inventory/:inventoryId` (`{stock}`) is the one stock-write endpoint — a negative value is rejected with `400 NEGATIVE_STOCK_REJECTED` at the **service** layer (not Zod's own `.min(0)`, which would only produce a generic `VALIDATION_ERROR`, not this SRS-named code) before the `$set`.
- **Self-healing row backfill, no migration script** — the same "no real production data pre-launch" reasoning every other catalog module in this codebase has used to skip a one-off migration. `inventory.service.ts`'s `ensureInventoryRowsForVariant(productId, variantId)` / `ensureInventoryRowsForWarehouse(warehouseId)` upsert a `stock: 0` row for every `(variant, warehouse)` pair, called from the two points that can create a gap: `products.service.ts`'s `addVariant` (a new variant needs a row per existing warehouse) and the new `warehouses.service.ts`'s `createWarehouse` (a new warehouse needs a row per existing variant, across every product). **The first service→service cross-module call in this codebase** (`products.service.ts` calling into `inventory.service.ts` directly) — every prior cross-module dependency in this repo was peer service→repository (`brands.service.ts`'s `countByBrand` precedent); documented inline as a deliberate exception, justified because this is a genuine side-effecting business action ("a new variant needs a stock row"), not a read.
- **Buyer-facing availability reinstated** (`FR-INV-007`/`008`) — `products.service.ts` gains a new batched `attachAvailability(items, docs)` (list) plus an inlined per-variant equivalent for `toPublicDetail`'s `variants[]`, both backed by `inventory.repository.ts`'s `sumStockByVariantIds(variantIds): Map<string, number>` (one `$group` aggregate per result page, same "bulk map, default missing key to 0" shape as `countByBrandIds`/`countByCategoryIds`). A list item's single top-level `availability` is the best state (`in_stock` beats `out_of_stock`) across every **active** variant's summed stock; a detail's `variants[]` gets a per-variant `availability`, each variant's own summed stock across warehouses. Buyer availability is always 2-state — no `low_stock`, and warehouse detail is never exposed to a buyer.
- **`?inStock=true` buyer filter reinstated** (also removed by #102) — `listPublicProducts`/`listPublicProductsByCategorySlug` call `inventory.repository.ts`'s new `listVariantIdsWithStock()` only when requested, folding the result into `buildVariantElemMatch`'s existing single `$elemMatch` alongside price/on-sale (the file's own established "AND within one elemMatch" design) — no aggregation `$lookup` needed, the existing plain `.find()` path stays a plain `.find()`.
- **Cart-time stock allocation** (Issue #190, `FR-INV-009`–`011`) — `cart.model.ts`'s `CartItem` gains a **required** `warehouse: ObjectId` field: a line is allocated to exactly **one** warehouse, never split across warehouses, and never exposed to the buyer in any cart read (`buildCartResponse`/`buildLineView` are unchanged — inventory stock is a mutation-time gate, not a read-time `unavailable` flag).
  - **`addItem`** — a brand-new line calls `inventory.service.ts`'s `allocateNewLine(variantId, quantity)`, which shops every **active** warehouse in creation order via a sequential atomic `findOneAndUpdate({variantId, warehouseId, stock:{$gte:quantity}}, {$inc:{stock:-quantity}})` per warehouse — each attempt is atomic, so trying them in order is race-safe (only one attempt can ever succeed for a given decrement) — stopping at the first that succeeds and recording its `warehouseId` on the line. No warehouse fits → `409 INSUFFICIENT_STOCK`, naming the max single-warehouse stock available for that variant (this same path also satisfies `FR-INV-008`: a variant with zero total stock always fails here, no separate check needed). Combining into an **already-existing** line (the buyer clicks add-to-cart again for a variant already in the cart) is treated as an increase, not a fresh allocation — see the next bullet.
  - **`updateItem`** (and `addItem`'s combine-into-existing-line branch) — re-checks the delta **only against the line's already-recorded warehouse** via `allocateAdditionalStock`, never re-shopping other warehouses; the SRS explicitly disallows splitting one line's fulfillment across warehouses. An increase that the recorded warehouse can't cover is `409 INSUFFICIENT_STOCK` naming that one warehouse's current stock; a decrease (including a `quantity: 0` removal) calls `restoreStock`, an atomic increment that always succeeds.
  - **`removeItem`** restores the full line quantity to its recorded warehouse before filtering it out; **`clearCart`** restores every remaining line's quantity the same way (`Promise.all`) before wiping the array — a direct extension of the same restore call, needed so a full cart-clear can't permanently leak decremented stock with no way back.
  - **13 pre-existing Supertest fixtures across orders/payments/dashboards** (`orderStatus`, `buyerOrders`, `orderNotifications`, `checkout`, `adminOrders`, `salesDashboard`, `accountDashboard`, all 5 `payments/*` suites) had all incidentally used the real `POST /api/cart/items` endpoint as setup scaffolding — every one of them would now be rejected as out-of-stock, since a freshly-seeded `Product` has no `inventory` rows at all. Fixed with one shared `seedTestWarehouseStock(productId, variantIds, stock = 999)` helper on `__tests__/testHelpers/adminSession.ts` (find-or-create a `"TEST-WH"` warehouse, `Inventory.insertMany` the rows) rather than 13 bespoke fixes.
  - `orders.service.ts`'s checkout re-persist of dropped/unavailable cart lines now re-fetches the raw cart record (`cart.repository.ts`'s `findByUser`) instead of reconstructing items from the buyer-facing `CartLineView`, since that view carries no `warehouse` field to reconstruct from (warehouse is never exposed to the buyer).
- **New error codes**: `NEGATIVE_STOCK_REJECTED` (400), `INSUFFICIENT_STOCK` (409 — a conflict with current state, matching `BRAND_IN_USE`-style guard codes' own status choice, not 400), `INVENTORY_ROW_NOT_FOUND` (404), `DUPLICATE_WAREHOUSE_CODE` (400).
- **Deliberately out of scope** (per the SRS's own §7/§10, not oversights): no adjustment audit log, low-stock thresholds/alerts, suppliers, purchase orders, barcode scanning, bulk/CSV import, a cart-expiry stock-restoration job (`FR-INV-012`), order-time (vs. cart-time) deduction, cross-warehouse split fulfillment, per-warehouse admin permissions, or support beyond a handful of warehouses. `dashboard`'s `catalog-summary` endpoint (Issue #172/M7.2) still has no `outOfStockCount` field — that remains a follow-up now that this module exists, not done as part of M10.
- Tests: `src/modules/inventory/tests/{inventory,warehouses}.service.test.ts` (unit, repository mocked — negative-stock rejection, row-backfill idempotency, availability aggregation math), `__tests__/inventory/{warehouses,inventoryTable,buyerAvailability}.api.test.ts` (real-DB Supertest) for Issue #189; `src/modules/cart/tests/cart.service.test.ts` (extended, `inventory.service` mocked — allocate/restore call shapes on all four cart operations) and `__tests__/shopping-cart/cart/cart.api.test.ts` (extended — real allocation/restoration, `INSUFFICIENT_STOCK` on over-request, warehouse-pinned re-checks on a repeat add) for Issue #190.

## Authentication (Buyer)

> **Superseded by Issue #258 / M3.20 (custom implementation) and Issue #261 / M3.23 (Better Auth removed entirely).** Buyer sign-in (Google One Tap + email OTP) is hand-rolled in `src/modules/authentication/auth/` on #257's session engine (`src/lib/session.ts`) — `src/lib/googleAuth.ts` verifies the Google ID token against Google's JWKS directly; `src/lib/otp.ts` owns the `otps` collection; per-IP/per-email rate limits are enforced in-handler via `src/lib/rateLimit.ts`. `src/lib/auth.ts`, `betterAuthHandler.ts`, the `bearer`/`emailOTP`/`oneTap` plugins, and the `better-auth` packages are all gone. The bearer-token cross-domain mechanism (`set-auth-token` response header, `Authorization: Bearer` on the way back, `cors.ts`'s `exposedHeaders`) carried over unchanged — `session.ts`'s cookie/Bearer dual-delivery is the same shape. The rest of this section is historical (#139's original Better Auth build).

Issue #139 (`FR-AUTH-001`–`008`, `046`) — buyer sign-in via Google OAuth, Google One Tap, and email OTP, all resolving to one `users` collection by email. This is a **redo**: it shipped once (PR #202), was reverted the same day alongside two sibling admin-auth issues (#140/#141, PR #208) because login broke on deployed previews, then redone here with a fix. Scope is strictly buyer auth — no admin password/OTP sign-in, no `X-Admin-Key` replacement (both still #140/#141/#143, untouched by this issue).

- **Root cause of the original break, and why this redo uses a bearer token, not cookies alone**: `buyer-app` deploys to Vercel, `backend` deploys to Render (`docs/architecture.md` §7) — genuinely separate domains, no shared parent domain, no proxy. Better Auth's default session is a `sameSite=lax` cookie; a cross-site `fetch` from Vercel to Render won't reliably carry it back (and Safari blocks third-party/cross-site cookies outright regardless of `sameSite`), so this passed CI and worked locally while being broken in every real deployed environment. Fixed by adding Better Auth's official `bearer` plugin (`src/lib/auth.ts`) — additive alongside the existing cookie, not a replacement: sign-in returns the session token in a `set-auth-token` response header, and a client sends it back as `Authorization: Bearer <token>`. Documented as an SRS amendment, `FR-AUTH-046` (`docs/srs/features/0.3-authentication.md`), since the spec's original `FR-AUTH-015` said a session was "never a client-readable or client-storable token" — inert for buyer auth specifically since `#140` (admin, same cross-domain shape) hasn't been redone yet, but flagged there for when it is.
- **`src/lib/` is a new top-level directory** — this codebase's first constructed-third-party-singleton category, distinct from `src/config/`'s env/DB bootstrap and `src/externalService/`'s thin async-function SDK wrappers. `auth.ts` exports one `betterAuth(...)` instance (`mongodbAdapter` reusing `mongoose.connection.db`/`getClient()`, `transaction: false` — a confirmed, still-open upstream bug, [better-auth#10925](https://github.com/better-auth/better-auth/issues/10925), not a workaround for anything this feature's account-creation flows actually need).
- **`src/middleware/betterAuthHandler.ts` (deleted by Issue #260/M3.22, along with its `router.all("/*splat", ...)` catch-all — nothing routes through Better Auth's HTTP handler any more; the rest of this bullet is historical)** bridged Better Auth's native Fetch-API `Request`/`Response` handler to Express and reshaped JSON responses to `{success:true,data}`/`{success:false,code,message}`. Wired in via **`src/modules/authentication/auth/`** (`auth.routes.ts` + `auth.module.ts`, exporting `{path:"/api/auth", router}`) — the same module shape `health`/`brands`/`categories`/`products` use, mounted through `src/routes/index.ts` like every other route, not a special case in `app.ts`. The one real constraint this forces: Better Auth needs the raw, unparsed request body, so `authModule` is registered **first** in `routes/index.ts`, ahead of that file's own `router.use(express.json())` — `express.json()` moved out of `app.ts` and into `routes/index.ts` specifically so the auth module could sit ahead of it while every other module still gets JSON parsing; `app.ts` itself is back to knowing nothing about auth. Forwards every response header except `set-cookie` (handled separately, since a single `Headers` entry can hold multiple `Set-Cookie` values `res.setHeader` would clobber) and `content-type`/`content-length` (set by `res.json()`/`res.send()` themselves) — not just `Set-Cookie` alone, which is what the original (reverted) version of this bridge did, silently dropping `set-auth-token` and making the whole bearer-token mechanism invisible until deployed.
- **`src/middleware/cors.ts`** gained `"Authorization"` in `allowedHeaders` (the Bearer header needs to survive preflight) and `exposedHeaders: ["set-auth-token"]` (without it, a browser receives the header but cross-origin `fetch(...).headers.get(...)` can't read it — the second silent-failure point in this mechanism). `origin` changed from a plain array to a matcher function (`src/utils/originMatch.ts`'s `matchesOrigin`) supporting a `*` wildcard segment per `CORS_ORIGINS` entry, so an operator can put e.g. `https://tech-cart-buyer-app-*.vercel.app` into Render's `CORS_ORIGINS` for staging to match Vercel's per-branch preview URLs (which change every deploy and can't be listed as static literals) — deliberately scoped to this project's own Vercel URL prefix, never a bare `*.vercel.app`, which would trust every Vercel-hosted app on the internet. Existing literal (non-wildcard) entries keep matching exactly.
- **`src/externalService/mailer.ts`** — the second client under `externalService/`, same pattern as `r2.ts`: a `nodemailer` transport constructed once, two exported async functions (`sendOtpEmail`, `sendPasswordResetEmail`). Originally `resend.ts` (Resend), renamed and rewritten by Issue #242/M3.14 — TechCart doesn't own a verified sending domain, which Resend requires, so Mailtrap (no domain verification needed) is now the sole email provider in every environment, dev and production alike, with no `NODE_ENV` branch. `env.MAILTRAP.{HOST,PORT,USER,PASS,FROM_EMAIL}` are all required fields in `env.ts`.
- **Buyer sign-in OTP is fixed to a default value (`"123456"`) in every environment, including production (Issue #242/M3.14)** — `emailOTP({...})`'s new `generateOTP: () => "123456"` override, so manual sign-in testing never needs real inbox access. **Deliberate, explicit security tradeoff**: a fixed OTP is a known, guessable constant, not a real secret — acceptable only at this pre-launch stage, not a posture to carry into a real production launch (documented again in `docs/srs/features/0.3-authentication.md`'s amendment note). `storeOTP: "hashed"` still hashes this fixed value deterministically, so the stored/verified code and the emailed code stay identical.
- **`additionalFields`**: `role` (default `"buyer"`, `input:false` — blocks a client-supplied role, `FR-AUTH-004`), `status`, `phone`, `lastSignInAt` (`input:false`, stamped via `databaseHooks.session.create.after`).
- **`FR-AUTH-006` (reject a buyer sign-in on an email already registered as non-buyer)** is enforced two ways: `user.validateUserInfo` re-validates on every OAuth/One Tap sign-in (fresh provider identity each time); a `hooks.before` matcher (`rejectAdminEmailOnReturningOtpSignIn`) closes the one gap `validateUserInfo` doesn't cover — a _returning_ email-OTP sign-in for an already-existing account isn't re-validated through that hook, per Better Auth's own docs.
- **Known, documented limitation, out of scope for this backend-only issue**: Google OAuth is a full-page browser redirect, not a `fetch` call — a redirect response's headers (including `set-auth-token`) aren't readable by frontend JS the way a `fetch` response's are. One Tap and email-OTP are both direct JSON `fetch` calls and get the bearer token cleanly; whichever future issue wires `buyer-app`'s actual sign-in UI needs a specific technique (e.g. a token-exchange step after the redirect lands) for the OAuth path specifically. Pre-existing to the cross-domain premise, not introduced by this redo.
- **Google Cloud Console configuration (Authorized JavaScript origins vs. redirect URIs) is a separate, real operational requirement — not covered by this file's own env vars alone** (Issue #243/M3.15, filed after a real user hit `Error 400: origin_mismatch`) — see `docs/architecture.md` §7.1 for the exact origins/URIs to register and why the two Console fields serve different parts of this flow.
- **Route paths are Better Auth's own plugin-generated paths, confirmed against the installed `better-auth@1.7.1`** (not the SRS endpoint table's illustrative names, per that table's own footnote): `POST /api/auth/one-tap/callback`, `POST /api/auth/sign-in/social`, `GET /api/auth/callback/google`, `POST /api/auth/email-otp/send-verification-otp`, `POST /api/auth/sign-in/email-otp`, `GET /api/auth/get-session` (not `/api/auth/session`).
- **Test**: `__tests__/authentication/auth/auth.api.test.ts` — this codebase's first real-database test suite (`mongodb-memory-server`'s standalone `MongoMemoryServer`, not a replica set — sufficient since `transaction: false` means nothing here needs replica-set-only transaction support), needed because account-dedup/reject logic can't be trusted against a mocked adapter. Covers OAuth/One Tap/OTP account creation, cross-method dedup by email, no-client-supplied-role, OTP reuse/expiry rejection, and admin-email rejection — plus two tests specific to this redo's actual fix: one signs in and calls a _separate_ request with only `Authorization: Bearer <token>` and no cookies at all (the direct regression test for the bug that got this feature reverted — none of the other tests would have caught it, since Supertest's plain `request(app)` doesn't persist cookies across calls either way), and one asserts `Access-Control-Expose-Headers` actually includes `set-auth-token` when a request carries an `Origin` header.
- **CI**: `.github/workflows/ci.yml`'s `backend` test-matrix leg caches `~/.cache/mongodb-binaries` (`actions/cache`), since `mongodb-memory-server` downloads its own MongoDB binary on first use.

## Authentication (Admin)

> **Superseded by Issue #259 / M3.21 (custom implementation); Better Auth fully removed in #261 / M3.23.** Better Auth's `emailAndPassword` + `twoFactor` plugins are replaced by a hand-rolled password-then-OTP challenge on #257's session engine + #258's `otps` collection, wire-compatible with `admin-app`'s existing flow (no frontend change — #263/M3.25 only re-verifies). #260/M3.22 removed the Better Auth catch-all + `betterAuthHandler.ts`; #261/M3.23 deleted the `src/lib/auth.ts` instance and the `better-auth` packages. The `src/lib/password.ts` / `src/lib/adminChallenge.ts` / `src/lib/rateLimit.ts` machinery below is the whole story now.
>
> - **`src/lib/password.ts`** — `hashPassword`/`verifyPassword` (`bcryptjs`, cost 12). Admins are the only role with a password (`users.passwordHash`).
> - **`src/lib/adminChallenge.ts`** — the "which admin is mid-sign-in" state between the password step and the OTP steps: a 10-minute HS256 JWT (`JWT_SECRET`, `typ: "admin-2fa"`) delivered as the `httpOnly` cookie `techcart_admin_2fa`. `SameSite=None; Secure` gate mirrors `session.ts`'s `isCrossSiteDeployment` exactly — the hand-rolled equivalent of #148/M3.10's fix for Better Auth's own pending-challenge cookie. `readAdminChallenge`/`issueAdminChallenge`/`clearAdminChallenge`.
> - **Flow** (`auth.service.ts` / `auth.controller.ts`): `POST /api/auth/sign-in/email` verifies `users.passwordHash`, sets the challenge cookie, returns `200 {data:{code:"OTP_REQUIRED"}}` — **no session, no OTP sent yet**. `POST /api/auth/two-factor/send-otp` (challenge cookie only, no body) mints + emails the OTP (`requestOtp(email, "admin-2fa")` → fixed `"123456"`, symmetric — this removes #242/M3.14's admin-only asymmetry and #254/M3.18's 3-vs-10-min expiry mismatch by construction); used for the initial send **and** "Resend" (admin-app calls it right after the password step). `POST /api/auth/two-factor/verify-otp` (`{code}`) verifies, `issueSession`, sets the session cookie + `set-auth-token` header, clears the challenge cookie, returns `200 {data:{user}}`.
> - **Error codes** (unchanged from what `admin-app`'s `describeAuthError.ts` keys on): `INVALID_EMAIL_OR_PASSWORD` (401 — unknown email / wrong password / buyer-role account, all identical), `ACCOUNT_DEACTIVATED` (403, checked ahead of the password), `INVALID_TWO_FACTOR_COOKIE` (401 — missing/expired challenge), `INVALID_CODE` (401 — wrong or reused OTP), `OTP_HAS_EXPIRED` (401), `RATE_LIMITED` (429).
> - **Rate limits** (FR-AUTH-040/041) re-enforced in-handler via `rateLimit.ts`'s new IP groups `admin-signin` (5/15m), `admin-otp-verify` (5/15m), `admin-otp-resend` (3/10m) + the existing `admin-signin` email group — since shadowing the routes stops Better Auth's native limiter (same gap #258 hit).
> - **`provisionAdminUser()`** (`createAdminUser.ts`) rewritten: bcrypt-hash straight into `users.passwordHash` via the raw driver, no `auth.api.signUpEmail`, no `account` collection. `superAdmin.ts`/`seedUsers.ts` inherit it unchanged.
> - **`#267/M3.27` is closed** (by #261/M3.23) — its JWT-session design shipped in #257, and its last two items landed with #261: `BETTER_AUTH_SECRET` removed (`JWT_SECRET` is the only token-signing secret), and `src/utils/sessions.ts` deleted (revocation runs through `src/lib/session.ts`'s `revokeAllSessionsForUser`, which keys by `_id`/`userId`).
> - Test: `__tests__/authentication/auth/admin-sign-in.api.test.ts` rewritten against the custom flow (real `mongodb-memory-server`); `src/lib/tests/{password,adminChallenge}.test.ts` new; `cross-site-cookies.api.test.ts` now asserts the `techcart_admin_2fa` cookie carries `SameSite=None; Secure`.

Issue #140 / M3.2 (`FR-AUTH-009`–`018`, `030`) — admin sign-in as a two-step password-then-mandatory-OTP challenge, distinct from buyer's single-call passwordless flow. Also a **redo**: it shipped once (PR #205) alongside #139's first attempt, was reverted in the same PR #208 for the identical cross-domain cookie reason, and stayed unredone through #139's own redo (#209) since `admin-app`'s cross-domain shape needed its own verification. No new module — extends `src/lib/auth.ts` and adds a `src/scripts/seed/` helper; every new route rides the existing `/api/auth/*` catch-all and envelope bridge with zero bridge changes.

- **Built on Better Auth's own `emailAndPassword` + `twoFactor` plugins**, not a hand-rolled challenge token — `POST /api/auth/sign-in/email` (password step) returns no session when the account has `twoFactorEnabled: true`, just a short-lived signed state; `POST /api/auth/two-factor/send-otp` triggers the OTP email (reusing the identical `sendOtpEmail`/`emailOTP` plugin instance #139 already wired); `POST /api/auth/two-factor/verify-otp` is what actually establishes the session. All three paths, plus `GET /api/auth/get-session`/`POST /api/auth/sign-out`, were confirmed against a real CI run — see `__tests__/authentication/auth/admin-sign-in.api.test.ts`'s own header comment for the one surprise found there (the two-factor OTP `verification` record's `identifier` isn't a simple "contains-the-email" string the way buyer's emailOTP one is).
- **`emailAndPassword.disableSignUp` was tried first and rejected outright** — it blocks `auth.api.signUpEmail` server-side too, not just the public `POST /sign-up/email` route, which broke the admin-seeding helper below (confirmed via a real CI failure: `APIError: Email and password sign up is not enabled`). Sign-up stays enabled instead; the safety property it would have provided comes from the next point.
- **`rejectBuyerOnPasswordSignIn` (new `hooks.before` matcher, alongside #139's `rejectAdminEmailOnReturningOtpSignIn`)** refuses any `role:"buyer"` account at the `/sign-in/email` step, using the identical generic `INVALID_EMAIL_OR_PASSWORD` error a wrong password gets (`FR-AUTH-010`'s no-enumeration rule extended to this case too). This is what actually keeps a publicly self-registered password credential inert — buyers can create one via the now-enabled sign-up route, but can never sign in with it, since `additionalFields.role` stays `input:false` and every self-registered account defaults to `"buyer"`.
- **Bearer-token support needed zero new code** — `bearer()` is registered once, globally, in the same `betterAuth(...)` instance; any session it establishes (buyer or admin) gets a `set-auth-token` header automatically. `admin-app` is cross-domain from `backend` exactly like `buyer-app` is, so this issue inherits #139's fix for free rather than needing its own `FR-AUTH-046`-style amendment.
- **`src/scripts/seed/createAdminUser.ts`'s `provisionAdminUser()`** — shared by the CLI seed script (`npm run seed:admin --workspace backend` at the time this issue shipped; superseded by `npm run seed:super-admin`, see Admin Account Provisioning below) and the test suite, so "create a Better-Auth-compatible password credential" logic exists once. Creates the account via `auth.api.signUpEmail` (server-side, so the password hash matches what real sign-in verifies against — never hand-rolled), then patches `role`/`twoFactorEnabled`/`emailVerified` via the raw MongoDB driver, the same convention `auth.ts`'s own hooks already use. **Not** `FR-AUTH-025`'s full admin-provisioning API — that stayed a separate issue, since built as #142.
- **This issue's own `admin.ts` seed script (hardcoded super-admin email/password, no env vars — a deliberate simplification, no `.env` setup needed to exercise the flow locally) was superseded and deleted by Issue #142**, replaced by an env-var-driven `superAdmin.ts` — see Admin Account Provisioning below.
- **Deliberately untouched, at the time this issue shipped**: `src/middleware/adminAuth.ts`/`X-Admin-Key` and every `/api/admin/*` route still used the old guard — swapping it for a session+role check was a separate follow-up issue, since completed as Issue #143/M3.5 (see RBAC Guard & createdBy/updatedBy Backfill below). Rate limiting (`FR-AUTH-040`/`041`, Upstash Redis) is still explicitly deferred — no Redis dependency exists in this codebase yet.
- **A real cross-site cookie gap, found and fixed while building `admin-app`'s own sign-in flow (Issue #148/M3.10)**: the two-factor flow above establishes the _final_ session via the already-proven bearer-token mechanism, but the _intermediate_ step — Better Auth's own `twoFactor` plugin cookie carrying "which pending sign-in does this OTP belong to" between `POST /sign-in/email` and `POST /two-factor/verify-otp` — is a real cookie with no bearer-token equivalent, and it inherited `sameSite: "lax"` from Better Auth's own cookie defaults (confirmed by reading the installed package's `cookies/index.mjs`, `createCookieGetter`). `admin-app` (Vercel) is cross-domain from `backend` (Render) exactly like `buyer-app` is, and a `SameSite=Lax` cookie is never sent on a cross-site request — so this step would have silently failed (`INVALID_TWO_FACTOR_COOKIE`) in any real deployment, despite passing Supertest (its `agent` resends cookies unconditionally, with no browser-style SameSite enforcement) and local dev (`localhost:5173`→`localhost:4000` is same-site by spec — port doesn't affect "site"). Fixed with one `advanced.defaultCookieAttributes` override in `src/lib/auth.ts`, gated on `env.BETTER_AUTH_URL.startsWith("https://")` (mirroring Better Auth's own internal `useSecureCookies` protocol-sniff) — a no-op in dev/test (where `BETTER_AUTH_URL` is `http://`), so no existing test needed to change, including this section's own `sameSite=lax` cookie-flags assertion below. `admin-app`'s `baseQuery.ts` also gained `credentials: "include"` to actually send/receive this cookie cross-site during that brief window — see `admin-app/AGENTS.md`'s "Session-based sign-in and route guard" section for the frontend half. Test: new `__tests__/authentication/auth/cross-site-cookies.api.test.ts`, forcing `BETTER_AUTH_URL` to an `https://` value via `vi.resetModules()` + a fresh dynamic import, asserting the two-factor cookie's `Set-Cookie` header actually carries `SameSite=None; Secure`.
- **Admin 2FA OTP is fixed to a default value (`"123456"`) in every environment, including production (Issue #242/M3.14)** — the `twoFactor` plugin's `otpOptions` has **no equivalent override** to `emailOTP`'s `generateOTP` above anywhere in its public API (confirmed by reading the installed `better-auth@1.7.1` source: `OTPOptions` is only `period`/`digits`/`sendOTP`/`allowedAttempts`/`storeOTP`, and its OTP generation — `generateRandomString(opts.digits, "0-9")` — is hardcoded with no injection point; `sendOTP` only ever _receives_ the already-generated code to deliver it). The closest documented, supported fix instead: a new `databaseHooks.verification.create.before` hook (`src/lib/auth.ts`), scoped to `identifier.startsWith("2fa-otp-")` (the same core `verification` collection/identifier shape this section's own header comment already flags as a surprise), forces every write's `value` to `"123456:0"` (this codebase's `twoFactor` config sets no `storeOTP` override, so it defaults to `"plain"`/unhashed). **Deliberate, documented asymmetry**: this makes admin 2FA _verification_ always accept `"123456"`, satisfying the actual goal (no inbox access needed for manual testing), but the code _emailed_ by `otpOptions.sendOTP` above is still the real, random `generateRandomString()` output — decorative, safe to ignore, since `"123456"` always verifies regardless. Same explicit, pre-launch-only security tradeoff as the buyer-side override — see `docs/srs/features/0.3-authentication.md`'s amendment note.
- Test: `__tests__/authentication/auth/admin-sign-in.api.test.ts` (kept separate from `auth.api.test.ts` — the flow shape differs materially from buyer's single-call methods) — happy path, wrong-password/unknown-email identical error, password-alone-insufficient, wrong/expired/reused OTP (all now submitting the fixed `"123456"` value rather than the real emailed code, per the asymmetry above), sign-out invalidation, cookie flags (`httpOnly`/`secure`/`sameSite=lax`), the bearer-token round-trip, and buyer-role rejection.

## Admin Password Reset

> **Superseded by Issue #259 / M3.21.** Both endpoints are now hand-rolled (`auth.service.ts`), on the same `passwordResetTokens` collection #141's `sendResetPassword` callback already recorded into. Better Auth's `emailAndPassword` reset config went with the rest of `src/lib/auth.ts` in #261/M3.23.
>
> - `POST /api/auth/request-password-reset` — looks up the user; for a non-buyer, `recordResetToken` + `sendPasswordResetEmail(email, <origin>/reset-password?token=…)`; **always** returns the identical `200` regardless of whether the email is registered (FR-AUTH-019). Rate-limited per IP + email (`admin-forgot-password`, 3/hour). The reset-URL origin is `CORS_ORIGINS[0]` — provisional, since no admin reset UI exists yet.
> - `POST /api/auth/reset-password` (`{token, newPassword}`) — `consumeResetToken` (now `expiresAt`-guarded: an expired-but-not-yet-TTL-collected token is rejected → `400 INVALID_RESET_TOKEN`), bcrypt-write `users.passwordHash`, then `revokeAllSessionsForUser(userId)` (FR-AUTH-022).
> - `adminUsers.service.ts`'s `createAdminUser` (#142) now calls a new internal, non-rate-limited `sendAdminPasswordResetLink(email)` instead of `auth.api.requestPasswordReset`.
> - Test: `admin-password-reset.api.test.ts` updated — token still read from `passwordresettokens`, new password signs in via the custom flow, expiry checked against that collection.

Issue #141 / M3.3 (`FR-AUTH-019`–`022`) — self-service recovery for an admin who forgets the password #140 introduced. Built on the same `emailAndPassword` plugin's own reset-password support, not hand-rolled token logic; extends `src/lib/auth.ts` again, plus one new small tracking collection. No `X-Admin-Key`/provisioning changes.

- **Real endpoint is `POST /api/auth/request-password-reset` (`requestPasswordReset` in `auth.api`), not `/forgot-password`** — neither the SRS/issue's illustrative name nor the plausible-sounding `/forget-password` exist in `better-auth@1.7.1`; found via a temporary diagnostic logging `Object.keys(auth.api)` after both guesses 404'd against real CI. `POST /api/auth/reset-password` matched as originally written.
- **`sendResetPassword` only actually sends an email for a non-buyer account** (re-queried via the raw driver, same pattern `rejectBuyerOnPasswordSignIn` already uses) — Better Auth's own response to the caller is identical either way, so `FR-AUTH-019`'s no-enumeration guarantee holds regardless of what this callback does; buyers just never receive a pointless reset link for a credential type they don't use for sign-in.
- **`src/lib/passwordResetTokens.ts`** (new): a small Mongoose model + `recordResetToken`/`consumeResetToken`, mirroring `uploads.model.ts`'s `presignedUploads` collection precedent exactly (DB-backed with a TTL index, not an in-process `Map`, so it survives restarts and works across instances) — `sendResetPassword` records `{token, userId}` at send time; `FR-AUTH-022`'s session-invalidation logic (below) consumes it.
- **Session invalidation (`FR-AUTH-022`) runs in `hooks.before`, not `after`** — an `after`-hook version was tried first (matching the same `ctx.path`/`ctx.body.token` pattern the working `before` hooks use) but confirmed via real CI to never actually delete a session; `ctx.body` in an `after` hook apparently isn't the original request body the way it reliably is in `before`. Runs before the real handler instead, resolving the affected userId via `consumeResetToken` and deleting every matching `session` document — a documented, accepted tradeoff (a token-valid-but-otherwise-rejected request, e.g. a rejected weak password, would revoke sessions slightly early) not chased further since CI is the only ground truth available here.
- **The delete query matches `session.userId` as both a plain string and an `ObjectId`** — a string-only match found zero documents in real CI even though the hook was confirmed running; this file's own `databaseHooks.session.create.after` already had precedent for exactly this ambiguity (converting `session.userId` to `ObjectId` to query `users` by `_id`), suggesting the `session` collection's own stored `userId` field isn't consistently the same type as the string ids arrive as at the API boundary.
- **The test's reset token is read back from `passwordResetTokens` directly, not parsed from the emailed URL** — Better Auth's reset `url` doesn't carry the token as a `?token=` query param the way the test originally assumed; reading it from this repo's own tracking collection sidesteps guessing Better Auth's URL format entirely.
- Test: `__tests__/authentication/auth/admin-password-reset.api.test.ts` (kept separate from `admin-sign-in.api.test.ts` — same "materially different flow shape" precedent) — registered-admin/registered-buyer/unregistered response equality (asserting `200` explicitly, not just equality between calls — two matching 404s would otherwise have passed the equality check trivially, which is exactly how the wrong endpoint path slipped past it twice), valid reset + new-password sign-in, expired/reused token rejection, session invalidation after reset.

## Admin Account Provisioning

Issue #142 / M3.4 (`FR-AUTH-024`–`029`) — the real admin-provisioning story #140's own `createAdminUser.ts` helper deliberately stopped short of: a super-admin can now create, list, and update further admin accounts over REST, and the CLI seed script is env-var-driven instead of hardcoded. New module (`src/modules/authentication/adminUsers/`) plus one new middleware; no changes to `X-Admin-Key`/`adminAuth.ts`.

- **`src/middleware/requireRole.ts`** (new) — this codebase's first identity-aware admin guard. Calls `auth.api.getSession({headers})` (via a new, shared `src/utils/fetchHeaders.ts`'s `buildFetchHeaders(req)`, extracted from `betterAuthHandler.ts`'s own inline header-conversion loop on its second use), 401s (`UNAUTHENTICATED`) with no session, 403s (`FORBIDDEN`) if `session.user.role` doesn't match the required role, otherwise attaches `req.adminUser = {id, role}` and calls `next()`. Mounted once at the top of `adminUsers.routes.ts` (`router.use(requireRole("super-admin"))`), the same "guard applied once, at mount" convention `adminAuth.ts` already established — **layered alongside** the existing router-level `X-Admin-Key` check, not a replacement; every other `/api/admin/*` route is untouched and still relies on `X-Admin-Key` alone. `auth.api.getSession({headers})`'s call shape worked against real CI on the first attempt — no iteration needed, unlike #140's `twoFactor` config or #141's `requestPasswordReset` endpoint name.
- **`src/modules/authentication/adminUsers/`** — raw MongoDB driver against the `users` collection (`adminUsers.repository.ts`), not a new Mongoose model, matching `auth.ts`'s own established reasoning for staying off Mongoose for this collection. `list()`/`findById()`/`findByEmail()` all scope to `role: {$in: ["catalog-manager","order-manager","super-admin"]}` — this module is the admin roster, never a general user directory, so a buyer account is structurally invisible to it.
  - **`createAdminUser(input)`** (service) generates a random, never-logged, never-returned temporary password (`crypto.randomBytes(24).toString("base64url")`), calls #140's `provisionAdminUser()` to create the account with the requested role and `twoFactorEnabled: true` — identical shape to every other admin account in this codebase — then immediately calls `auth.api.requestPasswordReset({body:{email}})`, reusing #141's real, already-proven flow end to end. The new admin's actual first sign-in still goes through #140's full password+OTP challenge with no bypass (`FR-AUTH-029`) — proven in the test suite by completing a real reset-password round trip and confirming the password step alone still doesn't establish a session.
  - **`listAdminUsers`** — pagination/search/role/status filtering via the shared `src/utils/parseQuery.ts` (#104), reused as-is with no changes needed.
  - **`updateAdminUser(id, patch, requesterId)`** — `400 CANNOT_MODIFY_OWN_ACCOUNT` if `id === requesterId` (`FR-AUTH-026`/`028`: an admin can never change their own role or deactivate themselves through this endpoint, whichever field the patch touches). On `status: false`, synchronously revokes every live session for that admin in the same request, via a newly extracted `src/utils/sessions.ts`'s `revokeSessionsForUser(userId)` — the identical string/`ObjectId` dual-match `deleteMany` logic #141's `auth.ts` hook already had, pulled out on its second use (this codebase's established "extract on second use" convention) and imported back into `auth.ts` too, replacing that inline copy with no behavior change.
  - **A real bug found and fixed while building this**: `adminUsers.repository.ts`'s `list()` originally spread the caller's own `filter` first and the module's `role: {$in: ADMIN_ROLES}` restriction second — since both use the same `role` key, the second spread silently clobbered a caller-supplied `?role=` filter every time, so `GET /api/admin/users?role=catalog-manager` actually returned admins of every role. Caught by the test suite (not a mock — this module's tests hit a real database), fixed by spreading the restriction first so a caller's own (already Zod-validated, already-a-subset-of-`ADMIN_ROLES`) role filter wins.
  - No `DELETE` route (`FR-AUTH-027`'s "an admin can be deactivated" doesn't extend to hard-deletion) — deactivation via `PATCH .../status: false` is the only removal path, consistent with brands'/categories'/products' own status-vs-delete distinction (Status Updates above).
- **`src/scripts/seed/superAdmin.ts`** (new) replaces `src/scripts/seed/admin.ts` (Issue #140's hardcoded stopgap, explicitly flagged in its own header comment as something to "rotate or remove entirely once real admin provisioning... exists") — reads `SUPER_ADMIN_EMAIL`/`SUPER_ADMIN_NAME`/`SUPER_ADMIN_PASSWORD` from `process.env` directly (CLI-only ad hoc config, not routed through the global `env.ts` schema, matching every other seed script's own inputs). Calls `provisionAdminUser()` directly — idempotent, safe to run repeatedly. `package.json`'s `seed:admin` script is renamed `seed:super-admin`. **Issue #241/M3.13**: the original version fell back to `admin.ts`'s own literal defaults (`admin@techcart.dev` / `TechCart@Admin123`) when any of the three vars was unset, so local dev needed zero `.env` changes — reconsidered as a real risk for a _production_-bootstrap script (a well-known default credential silently provisioned), so all three are now required with no fallback: the script exits non-zero naming whichever var(s) are missing, matching `env.ts`'s own fail-fast convention.
- **`src/scripts/seed/seedUsers.ts`** (new, Issue #245/M3.17) — `npm run seed:users`, a second, deliberately different seed script alongside `superAdmin.ts`: idempotent local-dev/test _sample_ data, not a production-bootstrap tool. Upserts 3 buyer fixtures directly against the `users` collection via the raw MongoDB driver (buyers are passwordless — no Better Auth credential to create, unlike admins), plus one `catalog-manager` and one `order-manager` sample account via the existing `provisionAdminUser()` (already idempotent as-is). Its two sample admins' credentials are deliberately hardcoded, not env-var-driven — unlike `superAdmin.ts` (a real bootstrap script), this one is never used for a real deployment, matching `data.ts`'s own hardcoded catalog-template precedent.
- Test: `__tests__/authentication/adminUsers/adminUsers.api.test.ts` — real-DB Supertest suite (same `mongodb-memory-server` pattern as `__tests__/authentication/auth/*`, needed for the same reason: this module's session/role logic can't be trusted against a mocked adapter) — create catalog-manager/order-manager with 2FA and no sign-in bypass proven via a full reset-password round trip, list pagination/search/role filter, role update, deactivate + live-session-invalidation (confirmed via a bearer token rejected on the very next `get-session` call), self-account rejection, role-guard rejection (no session and a non-super-admin session, both), and seed idempotency (`provisionAdminUser` run twice creates no duplicate).

## RBAC Guard & createdBy/updatedBy Backfill

Issue #143 / M3.5 (`FR-AUTH-031`–`035`) finishes the swap Product Catalog's `FR-CAT-090` always promised: `src/middleware/adminAuth.ts` (the temporary `X-Admin-Key` shared secret, live since #25/M2.1) is removed entirely and replaced by a real session+role guard at the identical mount points — no catalog route/controller/service business logic changed. Also backfills `createdBy`/`updatedBy` (reserved-`null` since #27/#28/#31) from the authenticated admin's id.

- **`src/middleware/rbac.ts` consolidates #142's `requireRole.ts`**, generalized from a single required role to an allow-list array — `rbac(roles: readonly AdminRole[])` returns middleware doing the identical `auth.api.getSession({headers: buildFetchHeaders(req)})` resolution `requireRole.ts` already proved working against real CI, 401s (`UNAUTHENTICATED`) with no session, 403s (`FORBIDDEN`, naming every allowed role) if the session's role isn't in the list, else attaches `req.user = {id, role}` and calls `next()`. `requireRole.ts` is deleted; `req.adminUser` is renamed `req.user` everywhere (its one prior call site, `adminUsers.controller.ts`, now goes through the new shared `requireActorId` below). `rbac.ts` also exports `CATALOG_ADMIN_ROLES = ["catalog-manager", "super-admin"] as const`, co-located so every catalog route file imports one shared constant instead of repeating the literal array.
  - **Issue #258/M3.20** added a minimal, additive dual-check ahead of the Better Auth call — the custom session engine first, Better Auth as fallback — so `/api/account/profile` kept working for a buyer signed in through the new flow during the migration window.
  - **Issue #260/M3.22** removes that fallback: `rbac.ts` now calls `extractSessionToken` / `verifySessionToken` from `@/lib/session` only. No token, or a token that fails verification (`invalid_token` or `session_expired`, deliberately not distinguished) → `401 UNAUTHENTICATED`; a verified session whose `role` isn't in the allow-list → `403 FORBIDDEN`; else `req.user = {id, role}` and `next()`. The role-check logic, `CATALOG_ADMIN_ROLES`, and the `Role = AdminRole | "buyer"` widening (#144) are unchanged. This was `rbac.ts`'s last import of `@/lib/auth`.
- **Guard placement moves from the router-wide `adminRouter.use(adminAuth)` to one `router.use(rbac([...]))` per module's own router** — `adminUsers.routes.ts`'s own guard (`router.use(requireRole("super-admin"))`, #142) already used this "guard applied once, at this router's own mount" shape; every other admin module (`brands.admin.routes.ts`, `categories.admin.routes.ts`, `products.admin.routes.ts`, `categorySpecifications.routes.ts`, `categoryVariants.routes.ts`, `uploads.routes.ts`) now adds `router.use(rbac(CATALOG_ADMIN_ROLES))` the same way; `adminUsers.routes.ts` itself switches to `rbac(["super-admin"])`. `src/routes/admin.routes.ts` is now a plain aggregator with no guard of its own.
- **`ADMIN_API_KEY` is removed from every surface it touched**: `src/config/env.ts`'s zod schema, `src/middleware/cors.ts`'s `allowedHeaders` (now `Content-Type`/`Authorization` only), `.env.example`, root `render.yaml`'s Render env var declaration, and `vitest.config.ts`'s injected test env.
- **New shared `src/utils/actor.ts`'s `requireActorId(req): string`** generalizes `adminUsers.controller.ts`'s own local `requesterId(req)` helper (#142) on its second/third/... use — every controller handler that writes a brand/category/product now calls `requireActorId(req)` and passes the result as a new trailing `userId: string` parameter through the service layer into the repository layer, which converts it to a `Types.ObjectId` once, at the write.
- **`createdBy`/`updatedBy` stamped on every write to brands/categories/products** — create (`createdBy`), update, status-toggle, and (products only) variant add/update (`updatedBy`, always) — `categorySpecifications`/`categoryVariants` have no such fields on their schemas at all and are out of scope. `CreateBrandDoc`/`CreateCategoryDoc`/`CreateProductDoc` (repository-layer write types) gain a required `createdBy: Types.ObjectId`; `UpdateBrandDoc`/`UpdateCategoryDoc`/`UpdateProductDoc` gain a required `updatedBy: Types.ObjectId` (required, not optional, so a forgotten stamp is a compile error, not a silent gap) via `Partial<Omit<CreateXDoc, "createdBy">> & {updatedBy: Types.ObjectId, ...}`.
- **Products' variant add/update stamps the parent product's own `updatedBy`, not a new field on the embedded variant** — `ProductVariant` has no `createdBy`/`updatedBy` of its own (only `createdAt`/`updatedAt`, since #121), so `products.repository.ts`'s `replaceVariants(id, variants, updatedBy)` gains a third parameter, included in its existing raw-driver `$set` (`{variants, updatedAt: new Date(), updatedBy}`) alongside the Issue #121 fix that already routes this write around Mongoose's schema casting.
- **Every catalog admin Supertest suite adopts the real-session `mongodb-memory-server` harness** — brands/categories/products/categorySpecifications/categoryVariants/uploads all previously mocked every repository call with no real DB connection at all (confirmed by inspection: none called or mocked `connectDB()`); since `rbac.ts` needs a real Better Auth session to resolve, all six now bootstrap a real Mongo connection for session resolution while continuing to mock each module's own catalog repository — the two don't conflict, since Better Auth's `users`/`session`/etc. collections and each module's own catalog collection are entirely separate. A new shared `backend/__tests__/testHelpers/adminSession.ts` (`bootstrapMemoryMongo`/`teardownMemoryMongo`/`signInFully`/`authRequest`) extracts this bootstrap plus `__tests__/authentication/adminUsers/adminUsers.api.test.ts`'s own `signInFully`/`adminRequest` on their second-through-seventh use. Each rewritten suite signs in one `catalog-manager` fixture once, in `beforeAll`, and reuses that bearer token for the whole file (these suites don't test auth boundary conditions themselves beyond a single no-session-401 and, for products, an `order-manager`-rejected-403 case — only `rbac.test.ts` covers the guard's own branch logic).
- **`__tests__/admin-auth/admin-auth.api.test.ts` is deleted** — it tested `adminAuth.ts` directly, which no longer exists.
- Test: `src/middleware/tests/rbac.test.ts` (unit) — 401 vs. 403 distinction, `req.user` attachment, multi-role allow-list acceptance. Rewritten by #260 to mock `@/lib/session` (`extractSessionToken`/`verifySessionToken`) instead of `auth.api.getSession`, adding no-token / invalid-token / expired-session cases. Plus cases in the rewritten `products.api.test.ts`: an `order-manager` session rejected 403 on `POST /api/admin/products`, and a product created by the signed-in `catalog-manager` fixture storing that fixture's real user id in `createdBy`.

## Account Self-Service

> **`POST /api/account/change-password` rewired by Issue #259 / M3.21.** It no longer calls Better Auth's `auth.api.changePassword` (which has no session to resolve once admin sessions move to the custom engine): `account.service.ts` now bcrypt-verifies the current password against `users.passwordHash`, writes the new hash (`account.repository.ts`'s new `getPasswordHash`/`setPasswordHash`), and excludes the current session from the bulk revoke by its exact `jti` via `session.ts`'s `revokeAllSessionsForUser(userId, currentJti)` — a clean primary-key exclusion, no more `<rawToken>.<signature>` substring matching. `INVALID_CURRENT_PASSWORD` (401) unchanged. Profile routes (`GET`/`PATCH /api/account/profile`) are untouched.

Issue #144 / M3.6 (`FR-AUTH-036`–`039`) adds small, session-scoped "my own account" endpoints distinct from `adminUsers`' admin-managing-other-admins surface (#142) — a buyer profile and an admin change-password capability, neither overlapping the other's role.

- **`src/middleware/rbac.ts`'s role guard is widened** from `AdminRole`-only to a new exported `Role = AdminRole | "buyer"`, so `GET`/`PATCH /api/account/profile`'s buyer-only guard (`rbac(["buyer"])`) reuses the identical session+role middleware every admin route already uses, instead of a parallel one.
- **`src/scripts/seed/createAdminUser.ts` exports a new `ADMIN_ROLES` runtime array** (`["catalog-manager", "order-manager", "super-admin"] as const`) — `POST /api/account/change-password`'s guard (`rbac(ADMIN_ROLES)`) is the first call site to need "any admin role" as a shared constant rather than a locally-redeclared literal (`adminUsers.controller.ts`/`.repository.ts` each still keep their own pre-existing copy, out of scope to consolidate here).
- **`src/modules/authentication/account/`** — no repository-layer indirection beyond raw driver reads/writes on the `users` collection (`account.repository.ts`, same no-separate-Mongoose-model convention `adminUsers.repository.ts` established): `getProfile`/`updateProfile` read/patch `{name, email, phone}` by the authenticated user's id (`requireActorId(req)`, unchanged). `PATCH /api/account/profile` requires at least one of `name`/`phone` via a zod `.refine()`.
- **`POST /api/account/change-password`'s session exclusion is the real story of this issue.** The obvious approaches all failed against real CI, in order: (1) Better Auth's own `changePassword` `revokeOtherSessions: true` option killed the _current_ session too, not just the others; (2) excluding the pre-change request's bearer token from a manual `revokeSessionsForUser` call failed identically; (3) proxying through `auth.handler()` directly (the same call `betterAuthHandler.ts` makes for every `/api/auth/*` route) to read a rotated token off a `set-auth-token` response header found no such header — `changePassword` never reissues one. A temporary diagnostic build (removed once the fix landed) finally showed the actual root cause: the bearer token a client sends is `<rawToken>.<signature>`, but the `session` collection's own `token` field only ever stores the raw part before that `.` — every prior attempt's exclusion filter was comparing against a value that could never match, so `revokeSessionsForUser` silently deleted every session for the user, current one included, on every single run. The fix splits the bearer token on its first `.` before any comparison.
- **`src/utils/sessions.ts`'s `revokeSessionsForUser(userId, excludeToken?)` gains an optional second parameter** — when passed, the `deleteMany` filter adds `token: {$ne: excludeToken}`, keeping that one session alive; omitted (as #141's password-reset and #142's admin-deactivation flows still call it), behavior is unchanged — every session for the user is revoked.
- Test: `__tests__/authentication/account/account.api.test.ts` (new, real `mongodb-memory-server` DB via the shared `testHelpers/adminSession.ts` harness, extended with a new `signInBuyer` helper for this issue's first buyer-facing auth test outside of `__tests__/authentication/auth/`) — profile view/update, empty-body validation rejection, 401 with no session, 403 when an admin session hits the buyer-only profile routes and vice versa, the happy-path password change (two independent sessions for the same admin, change on one, confirm the other is dead and the changing one still works), and a wrong-current-password rejection that invalidates neither session.

## Abuse Mitigation & Error Contract (Issue #145 / M3.7)

> **Partially superseded by #258–261.** #145 originally split rate limiting across Better Auth's own native limiter (`src/lib/auth.ts`'s `rateLimit` option + `nativeIpRateLimitStorage`) and a per-email `hooks.before` hook. Once #258/#259 hand-rolled the auth routes, all of it moved in-handler: `src/lib/rateLimit.ts`'s `consumeIpLimit` (per-IP) and `consumeEmailLimit` (per-email) are called directly from `auth.service.ts`'s sign-in / OTP / reset handlers. #261 deleted `nativeIpRateLimitStorage` and `src/lib/auth.ts` outright. The Redis client, limiter groups, `insuranceLimiter` fallback, `resetAllRateLimiters()`/`vitest.setup.ts`, and the `FR-AUTH-045` error-code contract all carried over unchanged — the rest of this section describes the pre-#258 Better Auth wiring and is historical.
>
> **Since then:** `RATE_LIMITING_ENABLED` (env, default `"true"`) is a hard on/off switch — set `"false"` and `src/lib/rateLimit.ts`'s `consume()` returns `{ allowed: true }` immediately, so `consumeIpLimit`/`consumeEmailLimit` become no-ops and `429 RATE_LIMITED` can never fire (`auth.service.ts`'s `if (!limit.allowed) throw` branches just don't trigger). Intended for local dev / manual Postman testing only — production keeps it on (`render.yaml` sets it explicitly). `REDIS_URL` is also now **optional**: when rate limiting is on and no `REDIS_URL` is set (or under test), the limiters use per-instance `RateLimiterMemory` instead of `RateLimiterRedis` — still enforced, just not shared across instances. The backend boots with neither var. `vitest.config.ts` no longer injects a dummy `REDIS_URL`; `src/lib/tests/rateLimit.test.ts` gained a case asserting the disabled-flag no-op (flipping the mutable `env.RATE_LIMITING_ENABLED` directly, since `consume()` reads it live).

Issue #145 (`FR-AUTH-040`–`045`) closes the one gap every prior M3 auth issue (#139–144) shipped without: rate limiting on the auth surface, and a verified `code` for every category `FR-AUTH-045` names. First Redis dependency in this codebase — `ioredis` + `rate-limiter-flexible`, a new `REDIS_URL` env var (Upstash's Redis-protocol connection string, not its REST API — chosen specifically because `docs/architecture.md` §5 already earmarks the same Upstash instance for BullMQ, which needs a real Redis-protocol client, not REST-only).

- **Two independent rate-limiting layers, not one hand-rolled Express middleware.** `src/lib/auth.ts`'s `betterAuth({rateLimit: {...}})` option turns on Better Auth's own **native** per-request rate limiter (confirmed by reading the installed `better-auth@1.7.1` package's own `api/rate-limiter/index.mjs` — it already resolves the real client IP via `x-forwarded-for`/`advanced.ipAddress` and applies rules atomically in the router's own `onRequest`, before any endpoint handler or even `hooks.before` runs) — this repo only supplies `rateLimit.customStorage` (a thin Redis/memory bridge, `src/lib/rateLimit.ts`'s `nativeIpRateLimitStorage`) and `rateLimit.customRules` (this issue's own literal-path window/max values, overriding whatever built-in default/plugin rule — `/sign-in*`, `/two-factor/*` already ship their own — would otherwise apply). That native system is **IP-only**; a second, independent layer (`src/lib/auth.ts`'s `enforceEmailRateLimits`, a new `hooks.before` hook run first in the existing chain) adds the **per-email** dimension `FR-AUTH-040`/`042`/`043` also want, for the three paths whose request body actually carries an email (`/sign-in/email`, `/request-password-reset`, `/email-otp/send-verification-otp`).
- **Why not a hand-rolled Express middleware ahead of `betterAuthHandler` instead**: `routes/index.ts` mounts the auth module _before_ `express.json()` runs (Better Auth needs the raw body stream, per Authentication (Buyer) above) — so `req.body` genuinely isn't parsed yet at that point in the pipeline. Email-based keying is only possible from inside Better Auth's own `hooks.before`, where `ctx.body` is already reliably populated (proven by every pre-existing hook in this file).
- **The five named limits and their keys** (`src/lib/auth.ts`'s `rateLimit.customRules` for IP, `EMAIL_RATE_LIMIT_GROUPS`/`src/lib/rateLimit.ts`'s `EMAIL_LIMIT_CONFIG` for email):

  | Limit                                           | Path(s)                                    | Key(s)                                                                                                 | Window / max |
  | ----------------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------ | ------------ |
  | Admin sign-in + OTP-verify (`FR-AUTH-040`)      | `/sign-in/email`, `/two-factor/verify-otp` | IP (both) + email (`/sign-in/email` only — `/two-factor/verify-otp`'s body is just `{code}`, no email) | 15 min / 5   |
  | Admin OTP-resend (`FR-AUTH-041`)                | `/two-factor/send-otp`                     | IP only — same body-shape gap as above                                                                 | 10 min / 3   |
  | Admin forgot-password (`FR-AUTH-042`)           | `/request-password-reset`                  | IP + email                                                                                             | 1 hour / 3   |
  | Buyer OTP-request (`FR-AUTH-043`)               | `/email-otp/send-verification-otp`         | IP + email                                                                                             | 10 min / 5   |
  | Google OAuth callback / One Tap (`FR-AUTH-044`) | `/callback/google`, `/one-tap/callback`    | IP only                                                                                                | 1 hour / 20  |

  `/two-factor/send-otp`/`/two-factor/verify-otp`'s missing email dimension is a documented, accepted scope decision, not an oversight: the pending 2FA challenge is resolved from a signed cookie inside those endpoints' own handlers, not exposed to this global pre-request hook — same "documented, not fixed" treatment this codebase gives its other infra-shaped gaps (delete-guard TOCTOU races, the unprovisioned Atlas Search index). `customRules` keys are exact literal paths, not wildcards, so nearby paths this issue doesn't name (`/sign-in/social`, `/sign-in/email-otp`) keep whatever built-in default/plugin rule Better Auth already applies to them, untouched.

- **`src/lib/rateLimit.ts`** (new): one `ioredis` client (constructed eagerly at module load, like `mailer.ts`/`r2.ts`'s own clients — no Mongoose-style ordering hazard, `ioredis` doesn't depend on `connectDB()` — but only when `NODE_ENV !== "test"`, since Vitest's injected `REDIS_URL` has nothing listening on it). `RateLimiterRedis`/`RateLimiterMemory` instances are memoized lazily by `(keyPrefix, points, duration)`, not one-per-path up front — Better Auth's native system calls `customStorage.consume(key, rule)` with a _dynamic_ rule per matched path, so a fixed one-limiter-per-path set can't work for that half; `consumeEmailLimit`'s four groups reuse the identical cache with their own fixed values. Each Redis-backed limiter carries an `insuranceLimiter` (in-memory fallback) so a transient Redis outage degrades gracefully instead of crashing every auth request or silently disabling rate limiting.
- **`resetAllRateLimiters()` + a new `backend/vitest.setup.ts`** (first `setupFiles` entry this workspace has needed): a global `afterEach(() => resetAllRateLimiters())` — not per-file. Necessary because enabling `rateLimit.enabled: true` unconditionally (including under `NODE_ENV=test`) would otherwise let the in-process `RateLimiterMemory` singleton's state persist for a whole test file's worker lifetime, and several existing suites repeatedly sign in with one fixture email/loopback IP by design (`admin-sign-in.api.test.ts`, `auth.api.test.ts`) — those would start legitimately tripping these new limits mid-file without a reset. Centralizing the reset in `vitest.setup.ts` was simpler and safer than auditing and patching every affected existing suite individually.
- **`enforceAccountNotDeactivated`** (new `hooks.before` hook, `src/lib/auth.ts`) makes `FR-AUTH-045`'s `ACCOUNT_DEACTIVATED` code real for the first time — deactivating an admin (`adminUsers.service.ts`, `status: false`) had only ever revoked _existing_ sessions (`src/utils/sessions.ts`, since #142), never blocked a fresh sign-in/OTP-request attempt from that account. Covers `/sign-in/email`, `/sign-in/email-otp`, and `/email-otp/send-verification-otp` with one raw-driver lookup, reusing the exact pattern `findExistingNonBuyer`/`rejectBuyerOnPasswordSignIn` already established in this file.
- **`OTP_REQUIRED` made real** (`betterAuthHandler.ts`): Better Auth's `/sign-in/email` returns `{twoFactorRedirect: true}` (no session, no `code`) when the password is correct but 2FA is still pending, confirmed against the installed package's own `plugins/two-factor/index.mjs`. The bridge now stamps `code: "OTP_REQUIRED"` **inside** the response's `data` (not top-level) specifically for this one path, keeping the envelope's existing "`code` only appears on `success:false`" convention intact everywhere else — a frontend reads `data.code` only for this endpoint's pending-2FA signal.
- **Two real gaps in `betterAuthHandler.ts` found and fixed while wiring this in**: (1) Better Auth's native rate-limit response is a plain `new Response(JSON.stringify({message}), {status:429})` with **no explicit `Content-Type`** — the Fetch spec's default USVString-body MIME (`text/plain;charset=UTF-8`) applied instead, which would have sent the whole response through the bridge's raw-buffer passthrough branch and skipped the `{success,code,message}` envelope entirely; the JSON-detection check now also treats status `429` as JSON regardless of header. (2) that same native response carries no `code` field at all — the generic-error branch's fallback now resolves to `"RATE_LIMITED"` specifically for a `429`, not the generic `"AUTH_ERROR"`, so it's identical to the code this issue's own `enforceEmailRateLimits` hook throws, regardless of which of the two layers actually tripped.
- **No renames of pre-existing codes.** `FR-AUTH-045`'s illustrative names (`INVALID_CREDENTIALS`, `FORBIDDEN_ROLE`) stay illustrative — `INVALID_EMAIL_OR_PASSWORD`, `FORBIDDEN`, `UNAUTHENTICATED`, `GOOGLE_ACCOUNT_IS_ADMIN` are untouched, same "endpoint table/real behavior over issue-body/SRS phrasing" precedent this codebase has followed since brands/categories. `INVALID_OTP`/`OTP_EXPIRED` (buyer `emailOTP` plugin) were already confirmed real per Issue #146's own audit (`buyer-app/AGENTS.md`); this issue additionally confirms the admin `twoFactor` plugin's own wrong/expired-OTP codes are genuinely different values — `INVALID_CODE`/`OTP_HAS_EXPIRED` — by reading the installed package's `plugins/two-factor/error-code.mjs` directly rather than assuming they matched buyer's. Both flows having different literal values is fine — `FR-AUTH-045` only requires each is internally stable, never that admin and buyer OTP errors are cross-distinguishable.
- **`advanced.ipAddress.trustedProxies` is deliberately left unset** — Render's managed reverse-proxy hop has no published, stable IP/CIDR range an operator could hardcode there (unlike a self-managed proxy). With no `trustedProxies` configured, Better Auth's own `getIPFromHeader` trusts a single-value `x-forwarded-for` header directly, which matches a single-hop PaaS deployment's actual shape closely enough — a documented interpretive call, not a config gap, flagged for revisiting if Render's real forwarded-header chain ever turns out to carry more than one value.
- Test: `__tests__/authentication/auth/rate-limiting.api.test.ts` (new — each of the five limits independently triggered via a distinct `X-Forwarded-For` value per test, confirming one tripping doesn't affect the other four), `__tests__/authentication/auth/error-codes.api.test.ts` (new — one end-to-end suite gathering all eight `FR-AUTH-045` categories' real codes and asserting they're pairwise distinct), `src/lib/tests/rateLimit.test.ts` (new, unit), plus new cases folded into the existing `admin-sign-in.api.test.ts` (`ACCOUNT_DEACTIVATED`, `OTP_REQUIRED`, `INVALID_CODE`/`OTP_HAS_EXPIRED`) and `auth.api.test.ts` (`ACCOUNT_DEACTIVATED`, `INVALID_OTP`/`OTP_EXPIRED`).

## Startup

`src/index.ts` exports an async `startServer()`: it awaits `connectDB()` first — on rejection it logs the error and calls `process.exit(1)` **without** calling `app.listen()`, so the process never silently serves traffic against a dead DB connection; only on success does it call `app.listen(env.PORT, ...)`. The unconditional call at the bottom of the file is guarded with `if (require.main === module) startServer();`, so requiring/importing `index.ts` (e.g. from a test) doesn't trigger a real startup as a side effect — only running it directly (`node`/`tsx src/index.ts`, which is what `npm run dev`/the compiled `dist/index.js` do) does. This is what makes the failure path unit-testable — see `src/tests/index.test.ts`, which imports `{ startServer }` and mocks `connectDB`.

- **`./app` is a plain static `import app from "./app"` at the top of the file** (Issue #261/M3.23). It wasn't always: while Better Auth was in the tree, `src/lib/auth.ts` built its instance at module-load via `mongodbAdapter(mongoose.connection.db!, ...)`, capturing `db` as a plain value before `connectDB()` had run — so `index.ts` (and `superAdmin.ts`/`seedUsers.ts`) had to `await connectDB()` first and then dynamically `import()` the auth-pulling module. That dynamic `import()` then hit a _second_ bug: `backend/package.json` is `"type": "commonjs"`, and Node's ESM loader wraps a CJS module's `module.exports` as `namespace.default`, so `import("./app.js")` needed a two-shape unwrap (`"listen" in candidate ? … : candidate.default`) to work in both the compiled CJS runtime and the dev/test ESM loaders. Deleting `auth.ts` removed the only module-load `db` capture, so both the dynamic import and the unwrap are gone — verified against the compiled bundle (`require("./dist/src/app.js").default.listen` is a function).

## Dev workflow

- `npm run dev --workspace backend` — `tsx watch src/index.ts`
- `npm run build --workspace backend` — `tsc && tsc-alias`, must produce zero errors
- `npm run test --workspace backend` — `vitest run`
- `npm run test:coverage --workspace backend` — `vitest run --coverage` (v8 provider, text + HTML reporters; no enforced threshold yet)
- Copy `.env.example` to `.env` before running locally (`PORT`, `NODE_ENV`, `MONGODB_URI`, `CORS_ORIGINS`, `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, `R2_PUBLIC_URL_BASE`, `APP_BASE_URL`, `JWT_SECRET` (≥32 chars), `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `MAILTRAP_HOST`, `MAILTRAP_PORT`, `MAILTRAP_USER`, `MAILTRAP_PASS`, `MAILTRAP_FROM_EMAIL`) — `src/config/env.ts` validates all of these with `zod` at import time and throws immediately if any required value is missing, so a misconfigured `.env` fails on startup rather than partway through a request. `RATE_LIMITING_ENABLED` (default `"true"`) turns the auth abuse limiters into no-ops when set to `"false"` — a local-dev / manual-Postman-testing escape hatch, must stay `"true"` in a real deployment (`FR-AUTH-040`–`044`). `REDIS_URL` is now **optional**: with it, the limiters are Upstash-Redis-backed (`rediss://...` Redis-protocol string, not the REST URL) and shared across instances; without it they fall back to per-instance `RateLimiterMemory` — see Abuse Mitigation & Error Contract above. `APP_BASE_URL` is the backend's own public base URL — `https://` there switches the session/2FA cookies to `SameSite=None; Secure` for cross-site (Vercel↔Render) delivery, and it's the fallback origin for admin password-reset links (Issue #261/M3.23 renamed it from `BETTER_AUTH_URL`; `JWT_SECRET` is the sole token-signing secret now that `BETTER_AUTH_SECRET` is gone). The five `R2_*` vars are read via `env.R2.ACCOUNT_ID` etc., `GOOGLE_*`/`MAILTRAP_*` via `env.GOOGLE.*`/`env.MAILTRAP.*` — not the flat names. (`ADMIN_API_KEY` no longer exists — removed by Issue #143/M3.5 alongside `adminAuth.ts`, see RBAC Guard & createdBy/updatedBy Backfill below; this list previously mentioned it in error.)
- `env.ts` loads env files by mode: `.env.${NODE_ENV}` first, then `.env` fills in anything not already set (dotenv never overwrites an existing `process.env` key). So `.env.development`/`.env.test`/`.env.production` are optional per-mode overrides layered on top of the shared `.env` — a missing mode-specific file is a silent no-op, not an error. All of them are gitignored; only `.env.example` is committed.
- Tests don't need a real `.env`: `vitest.config.ts`'s `test.env` injects dummy values for all required vars (`MONGODB_URI`, the five `R2_*` vars, `APP_BASE_URL`, `JWT_SECRET`, `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`, the five `MAILTRAP_*` vars) directly into `process.env` for the whole run — `REDIS_URL` is deliberately **not** injected (it's optional now), and `src/lib/rateLimit.ts` uses `RateLimiterMemory` under `NODE_ENV=test` regardless; `RATE_LIMITING_ENABLED` defaults to `"true"` so the rate-limiting suites still exercise the real sliding-window logic (see Abuse Mitigation & Error Contract above, including why a global `vitest.setup.ts` resets that in-memory state after every test); `MAILTRAP_*`'s dummy values are never actually dialed either, for the identical reason — every test mocks `@/externalService/mailer` wholesale (Vitest also sets `NODE_ENV=test` itself, so `env.ts` would look for `.env.test` too — it doesn't need to exist, since the injected values already satisfy the `zod` schema before `dotenv.config()` would matter). Nothing in most of the test suite opens a real MongoDB connection or a real R2 bucket; `connectDB` and `r2.ts`'s `createPresignedPutUrl` are mocked wherever they're exercised — the one exception is `__tests__/authentication/auth/` (Issue #139), which overrides `MONGODB_URI` at runtime to point at a `mongodb-memory-server` instance instead (see Authentication (Buyer) above).

Test files: colocated `src/modules/<feature>/tests/*.test.ts` (or `src/<area>/tests/*.test.ts` for cross-cutting utilities, e.g. `src/utils/tests/apiResponse.test.ts`, `src/tests/index.test.ts`) for unit tests, workspace-root `__tests__/<feature>/*.test.ts` for Supertest integration tests. Both globs are wired in `vitest.config.ts`.

## Current module inventory

- `auth` — `/api/auth/*` (Issues #139, #140, #141, #145, #258, #259, #260, #261). Buyer sign-in (#258) and admin password + OTP + reset (#259) are hand-rolled in `auth.{controller,service,repository}.ts` on the #257 session engine (`src/lib/session.ts` + `jwt.ts` + `otp.ts` + `googleAuth.ts` + `password.ts` + `adminChallenge.ts` + `rateLimit.ts`). Better Auth is **fully removed** as of #261/M3.23 — `auth.routes.ts` is hand-rolled routes only, and every `/api/auth/*` path the plugins used to serve (the full-page Google OAuth redirect included) now 404s. Mounted first in `routes/index.ts`; each hand-rolled route carries its own `express.json()`. See Authentication (Buyer), Authentication (Admin), Admin Password Reset, and Abuse Mitigation & Error Contract above.
- `adminUsers` — `POST|GET|PATCH /api/admin/users[/:id]` (Issue #142 / M3.4). Admin-only, no public surface; gated by `rbac(["super-admin"])` (`src/middleware/rbac.ts`, generalized from #142's own `requireRole.ts` by Issue #143) — see Admin Account Provisioning and RBAC Guard & createdBy/updatedBy Backfill above.
- `account` — `GET|PATCH /api/account/profile` (buyer-only session) + `POST /api/account/change-password` (admin-only session, any role) (Issues #144 / M3.6, #259 / M3.21). No shared surface with `adminUsers` — this module is "my own account," never someone else's. Change-password verifies/writes `users.passwordHash` directly since #259 — see Account Self-Service above.
- `cart` — `GET|DELETE /api/cart` + `POST /api/cart/items` + `PATCH|DELETE /api/cart/items/:variantId` (Issues #150/#151 / M4.1–M4.2). Buyer-session-only, no admin surface; owns the `carts` collection; every read resolves pricing/availability live from the referenced product variant (`findByVariantId`/`findByVariantIds` on `products.repository.ts`) — see Cart above.
- `health` — `GET /health`, no DB access, reference implementation for the module pattern above.
- `uploads` — `POST /api/admin/uploads/presign` (Issue #26 / M2.2). Owns the `presignedUploads` tracking collection but no product-like resource of its own; exports `consumeImageKeys`/`validateImageCount`/`normalizeImages`/`buildPublicUrl` for reuse by later modules — see R2 uploads above.
- `brands` — `POST|GET|PATCH|DELETE /api/admin/brands[/:id]` + `PATCH /api/admin/brands/:id/status` + `?search=`/`?page=`/`?limit=`/`?sortBy=`/`?orderBy=` on the admin list + public `GET /api/brands` (Issues #27 / M2.3, #33 / M2.9, #34 / M2.10, and #104). First DB-backed catalog-entity module — see Brands, Status Updates, Admin Search, and Shared Admin List Query Parsing above.
- `categories` — `POST|GET|PATCH|DELETE /api/admin/categories[/:id]` + `PATCH /api/admin/categories/:id/status` + `?search=`/`?page=`/`?limit=`/`?sortBy=`/`?orderBy=` on the admin list + public `GET /api/categories` + `GET /api/categories/search` + `GET /api/categories/:slug/products` (Issues #28 / M2.4, #33 / M2.9, #34 / M2.10, #35 / M2.11, and #104). First module with a hierarchy and a two-reason delete guard — see Categories, Status Updates, Admin Search, Buyer Browsing, and Shared Admin List Query Parsing above.
- `categorySpecifications` — `GET|PUT|PATCH /api/admin/categories/:id/specifications` (Issue #29 / M2.5). Admin-only, no public surface (its filterable-field data is exposed indirectly via the buyer listing's `cardSpecifications`/filters, #36); first module with nested-array data and a field-level (not document-level) delete guard — see Category Specifications and Buyer Filtering & Sorting above.
- `categoryVariants` — `GET|PUT|PATCH /api/admin/categories/:id/variant-types` (Issue #30 / M2.6). Admin-only, no public surface; same shape as categorySpecifications but flat (no groups) and with no in-use delete guard at all — see Category Variant Types above.
- `products` — `POST|GET|PATCH|DELETE /api/admin/products[/:id]` + `PATCH /api/admin/products/:id/status` + `?search=`/`?status=`/`?sortBy=`/`?orderBy=` on the admin list + `POST /api/admin/products/:id/variants` + `PATCH /api/admin/products/:id/variants/:variantId` + public `GET /api/products` (+`?q=`, price/brand/category/variant-attribute/spec/on-sale filters, `?sort=`) + `GET /api/products/:slug` (Issues #31 / M2.7, #32 / M2.8, #33 / M2.9, #34 / M2.10, #35 / M2.11, #36 / M2.12, #102, #104, and #121). First module with real pagination/sorting, cross-module specification validation, a shared `src/utils/pricing.ts`, embedded sellable variants, and a full buyer-facing browse-and-filter surface — see Products, Product Variants, Status Updates, Admin Search, Buyer Browsing, Buyer Filtering & Sorting, Variant-Only Pricing, Shared Admin List Query Parsing, and Newest Sort By Primary Variant above. `PATCH /api/admin/products/:id/stock`, listed in earlier issues' own sections above, no longer exists as of #102 — this module tracks no stock of its own at all (see the new `inventory` module below). The buyer-facing `?inStock=` filter and `availability` field, also removed by #102, were reinstated by Issue #189/M10.1 — backed by `inventory`'s stock, not a field on `products` itself. The admin list's `?sort=-field` enum from earlier issues' own sections above no longer exists as of #104 — replaced by `?sortBy=`/`?orderBy=`.
- **M2 (Product Catalog) is complete** — Issues #25 through #36 are all merged; #102 and #104 are post-completion SRS v0.2 amendments against the same milestone. No further catalog modules are planned. The temporary `X-Admin-Key` guard every module above once relied on was replaced by Issue #143/M3.5's `rbac.ts` — see RBAC Guard & createdBy/updatedBy Backfill above.
- `addresses` — `GET|POST|PATCH|DELETE /api/addresses[/:id]` (Issue #154 / M5.1). Buyer-session-only; a standalone address book with no reference back to `orders` — see Orders above.
- `orders` — `GET|POST /api/orders` + `GET /api/orders/:id` + `POST /api/orders/:id/cancel` (buyer, `rbac(["buyer"])`) + `GET /api/admin/orders[/:id]` + `PATCH /api/admin/orders/:id/status` + `POST /api/admin/orders/:id/cancel` (admin, `rbac(ORDER_ADMIN_ROLES)`) + `POST /api/orders/:id/payment[/verify]` (payments module's handlers, mounted here since both are order-scoped) (Issues #155–#159 / M5.2–M5.6, #121). Owns the `orders` collection and `orders.stateMachine.ts`'s fixed status lifecycle — `transitionOrder()` is the single write path for every status change anywhere in this codebase — see Orders above.
- `payments` — `POST /api/orders/:id/payment[/verify]` (payments-owned handlers, mounted on `orders`'s routers) + `POST /api/admin/orders/:id/refund` (Issues #164, #165, #167 / M6.1, M6.2, M6.4). Owns the `payments` collection (paise-denominated, the one exception to this codebase's whole-rupees money convention) — see Payments above.
- `webhooks` — `POST /api/webhooks/razorpay` (Issue #166 / M6.3). No `rbac()` at all — authenticated by HMAC signature alone; mounted ahead of the shared `express.json()` with its own `express.raw()`, mirroring the `authModule` precedent — see Payments above.
- `dashboard` — `GET /api/admin/dashboard/{summary,sales,top-products,catalog-summary}` (Issues #171/#172 / M7.1–M7.2). A cross-cutting read layer, no collection of its own — see Dashboard above. `account`'s existing module also gains `GET /api/account/dashboard` (Issue #173 / M7.3, buyer-only).
- `inventory` — `POST|GET /api/admin/warehouses` + `GET /api/admin/inventory` + `PATCH /api/admin/inventory/:id` (Issue #189 / M10.1). Owns two collections (`warehouses`, `inventory`) in one module; no public surface of its own — buyer availability is exposed indirectly via `products`' own buyer-facing endpoints (`attachAvailability`, `?inStock=`). `cart` (Issue #190 / M10.2) calls into this module's service for allocation/restoration on every add/update/remove/clear — see Inventory above.
- **M5 (Orders) is complete** — Issues #154 through #159 are all merged (plus #121, a cross-cutting sort fix filed against `buyer-app` but implemented here). **M6 (Payments) is complete** — Issues #164 through #170 are all merged. **M7 (Dashboard) is complete** — Issues #171 through #173 (backend half) are all merged. **M10 (Inventory Management) is complete** — Issues #189 and #190 (backend half) are all merged.
