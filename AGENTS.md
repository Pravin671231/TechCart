# AGENTS.md

Guidance for AI coding agents working in this repository.

## Project

TechCart is a production e-commerce platform: a Next.js buyer storefront and a React/Vite admin console sharing one Node/Express API and one MongoDB database. Target market is India-first (Razorpay), initial scale is small-to-medium, hosting is managed platforms only (Vercel, Render, MongoDB Atlas, Upstash Redis). Full stack rationale and version pins live in the "E-Commerce Platform — Technology Blueprint," referenced from `docs/srs/SRS.md` §1.5.

## Current status

Root workspace tooling is scaffolded (Issue #1 / M0.1, merged): root `package.json` (npm workspaces — `backend`, `buyer-app`, `admin-app`; `engines.node >=24`), `tsconfig.base.json`, `eslint.config.ts` (flat config), `.prettierrc`, `.nvmrc`/`.node-version`. `npm install`, `npx eslint .`, and `npx prettier --check .` all work at the repo root. `backend/` is scaffolded (Issue #2 / M0.2, merged): Express 5 + TypeScript, module-based structure (`src/modules/<feature>/`), `health` module, error contract, TS path aliases (`@/*`), Vitest+Supertest wired, plus coverage reporting (Issue #3 / M0.3, merged) — see `backend/AGENTS.md` for detail. `npm run build|dev|test|test:coverage --workspace backend` all work. `buyer-app/` is scaffolded (Issue #4 / M0.4, merged): Next.js 16 App Router + Tailwind CSS 4, feature-based structure (`src/features/<feature>/`), a working placeholder home route, plus a Vitest + React Testing Library + MSW test suite (Issue #5 / M0.5, merged) — see `buyer-app/AGENTS.md` for detail. `npm run build|dev|lint|test --workspace buyer-app` all work. `admin-app/` is scaffolded (Issue #6 / M0.6, merged): Vite + React 19 + TypeScript, React Router, Tailwind CSS 4, feature-based structure (`src/features/<feature>/`), a placeholder landing route, plus a Vitest + React Testing Library + MSW test suite (Issue #7 / M0.7, merged) — see `admin-app/AGENTS.md` for detail. `npm run build|dev|lint|test --workspace admin-app` all work. Root fan-out scripts are wired (Issue #8 / M0.8, merged): `npm run build`, `npm run lint`, `npm test` (plus `test:backend`/`test:buyer-app`/`test:admin-app`) all work at the repo root, verified against a genuinely clean clone — **M0 Foundation is complete.** M1 (CI Pipeline) is complete: `.github/workflows/ci.yml` is wired (Issue #9 / M1.1, merged) — `lint` + a `[backend, buyer-app, admin-app]` test matrix run on every PR into `main`, verified live (matrix isolation and lint failure both confirmed against real Actions runs before merge) — and branch protection on `main` is configured (Issue #10 / M1.2, merged): PR + passing CI required, linear history, squash-merge only, direct pushes (including doc-only syncs) rejected. SRS v0.2 (Product Catalog) is spec-drafted (PR #23, merged): `docs/srs/features/0.2-product-catalog.md` covers `FR-CAT-001`–`096` across all 10 template sections, since amended to `FR-CAT-100` (see below). Its backend implementation is broken into 12 issues, opened as GitHub Issues #25–#36 (PR #37). M2 implementation is underway: Issue #25 / M2.1 (core plumbing, merged) gives `backend/` a live Mongoose connection, a `zod`-validated `env.ts`, the `{ success, data }` response envelope, and the temporary `X-Admin-Key` admin guard ahead of v0.3 Authentication. Issue #26 / M2.2 (Cloudflare R2 image uploads, merged) adds a presigned direct-to-R2 upload path (`FR-CAT-077`–`084`) plus a backend-proxied `multipart/form-data` upload path (SRS amendment `FR-CAT-097`–`100`) for clients that can't PUT to R2 directly. Issue #27 / M2.3 (brand management, merged) adds `backend/src/modules/brands/` — the first DB-backed catalog-entity module — with admin create/update/list-with-product-count/get/guarded-delete plus a public active-only brand list (`FR-CAT-023`–`029`), a shared slug-generation utility (`src/utils/slug.ts`), and a minimal product stub that categories (#28) and products (#31) build on. Issue #28 / M2.4 (category management, merged) adds `backend/src/modules/categories/` — a two-level category hierarchy (`FR-CAT-014`–`022`) with admin CRUD, a combined products-and-subcategories delete guard, and a public active-only list ordered by `sortOrder`/`name` with a computed SEO fallback. Issue #29 / M2.5 (category-governed specifications, merged) adds `backend/src/modules/categorySpecifications/` — a nested, one-per-category specification schema (`FR-CAT-030`–`035`) reachable only via `GET`/`PUT`/`PATCH` (no `POST`/`DELETE`), with a field-level guarded delete and a resolved cascade so deleting a category also deletes its specification document. Issue #30 / M2.6 (category-governed variant types, merged) adds `backend/src/modules/categoryVariants/` — a flat, one-per-category list of variant axes (`FR-CAT-036`–`038`) with the identical `GET`/`PUT`/`PATCH`-only shape, but **no in-use delete guard at all**, since the definition drives admin form rendering only and is never enforced against stored variant attributes; deleting a category now also cascades to this document, resolving `categories.service.ts`'s last deferred cascade comment. Issue #31 / M2.7 (product core CRUD and pricing, merged) adds `backend/src/modules/products/` — graduating from a stub to a full catalog-entity module built on top of brands/categories/categorySpecifications rather than just a stub they query — with admin create/update/get/paginated-list/soft-delete plus a dedicated stock-only path (`FR-CAT-001`–`013`), a shared `src/utils/pricing.ts` computing `sellingPrice` server-side on every write (`FR-CAT-085`–`087`, reused by variants in #32), and this codebase's first paginated, sortable admin list. Issue #32 / M2.8 (product variants, merged) fills in `products.model.ts`'s `variants` array — stubbed to just `{ sku }` since #31 — into the full embedded, sellable subdocument (`FR-CAT-039`–`044`), with `POST`/`PATCH .../variants[/:variantId]` endpoints added to the same module (no new module: variants are embedded, not a standalone collection), reusing `computeSellingPrice` and completing the SKU cross-check `products.repository.ts` already had. Issue #33 / M2.9 (status update APIs, merged) adds a dedicated `PATCH .../status` to all three entities — `PATCH /api/admin/products/:id/status` (`FR-CAT-045`, a tri-state setter reusing a new shared `PRODUCT_STATUSES` const) and `PATCH /api/admin/categories/:id/status` / `PATCH /api/admin/brands/:id/status` (`FR-CAT-046`–`047`, boolean toggles) — each a thin pass-through that deliberately never touches its entity's own delete guard (`FR-CAT-048`), since deactivating and deleting are different operations with different preconditions. Issue #34 / M2.10 (admin search, merged) adds a `search` query param to all three admin list endpoints (`FR-CAT-050`–`052`, a plain case-insensitive MongoDB regex on `name`, plus an exact-or-prefix match on `sku` for products) and a `status` query param to the product grid that composes independently with `search` (`FR-CAT-053`). Issue #35 / M2.11 (buyer browsing, merged) adds the first buyer-facing product endpoints — `GET /api/products` (paginated, `published`-only, `?q=` MongoDB Atlas Search), `GET /api/products/:slug`, `GET /api/categories/:slug/products`, and `GET /api/categories/search` (`FR-CAT-054`–`067`, `095`–`096`) — with a new shared `src/utils/availability.ts` deriving the buyer-facing `availability` enum in place of raw stock counts; the Atlas Search index itself still needs provisioning against a real Atlas cluster (`backend/atlas-search/README.md`). Issue #36 / M2.12 (buyer filtering, sorting, and card content, merged) extends the two buyer listing endpoints with a full filter/sort surface — price range, brand, category, variant-attribute, filterable-specification, in-stock, and on-sale filters, all composable with `?q=` and a `sort` option (`FR-CAT-068`–`076`) — plus `cardSpecifications` on every list item, a category's first four `filterable` specification fields in schema declaration order (`FR-CAT-091`–`092`); Express 5's default query parser was switched to `"extended"` to support the bracket-notation filter params this issue needs — see `backend/AGENTS.md` for full detail. **M2 (Product Catalog) is complete** — Issues #25 through #36 are all merged. Frontend build-out for M2 is drafted in `docs/issues.md` (PR #70); Issue #76 / M2.19 (admin-app: Redux Toolkit store, RTK Query API setup & X-Admin-Key auth, merged) gives `admin-app/` its first state-management/HTTP layer — `src/store/{authSlice,api,store}.ts` for the RTK Query client (`baseUrl` scoped to `/api/admin`, a 401-triggered `clearAdminKey` guard) plus a throwaway `src/features/adminKey/` prompt collecting the `X-Admin-Key` header (mirroring `backend/src/middleware/adminAuth.ts`) into `sessionStorage` ahead of v0.3 Authentication. Issue #77 / M2.20 (admin-app: brand management, merged) adds `src/features/brands/` — the first real RTK Query mutation consumer of that store — a list/create/edit/delete UI against `/api/admin/brands`, an inline `BRAND_IN_USE` 409 delete-guard, and a status toggle on the list (kept separate from the edit form, since the backend's `PATCH .../:id` doesn't accept `status` at all — only the dedicated `PATCH .../:id/status` does), plus a feature-agnostic `src/features/uploads/` presign-to-R2 logo upload flow reusable by later category/product-image issues — see `admin-app/AGENTS.md` for detail. Issue #71 / M2.14 (buyer-app: Redux Toolkit store & RTK Query API setup, merged) gives `buyer-app/` its own first state-management/HTTP layer — `src/store/api.ts`'s `createApi` instance wraps `fetchBaseQuery` in a custom `baseQuery` that unwraps `backend`'s `{ success, data }` envelope and normalizes both of its error shapes into one `{ code, message }`, with `tagTypes: ["Product", "Category"]` declared for later cache invalidation and zero endpoints yet (added per-feature via `injectEndpoints` starting M2.15); `src/store/store.ts`/`StoreProvider.tsx` create a store per `StoreProvider` instance, mounted into the App Router via a client boundary that keeps `layout.tsx` a Server Component; `src/store/env.ts` validates `NEXT_PUBLIC_API_URL` with `zod` at import time and fails the build loudly if it's unset — see `buyer-app/AGENTS.md` for detail. Issue #72 / M2.15 (buyer-app: home / all-products listing, merged) replaces `HomePlaceholder` with the first real screen (`src/features/home/HomeContent.tsx`) and adds `src/features/products/` — the shared, cross-screen product-listing feature (`ProductCard`, `ProductGrid`, `Pagination`, `SortSelect`, and the loading/empty/error state components, plus a `getProducts` RTK Query endpoint) that later listing screens (M2.16/M2.17) are expected to reuse rather than duplicate; also resolves `src/store/api.ts`'s pagination TODO left by #71 by threading `pagination` through RTK Query's `meta`, combined with `data` in each list endpoint's own `transformResponse`. Issue #73 / M2.16 (buyer-app: category listing page, merged) adds `src/app/category/[slug]/page.tsx` — the first dynamic route in the monorepo — and `src/features/category/` (breadcrumb, price/brand/in-stock/on-sale filter rail, a row-layout product card/list distinct from home's grid, and a dedicated not-found state for `CATEGORY_NOT_FOUND`), plus two new resource features, `src/features/categories/` and `src/features/brands/` (each a single public `GET`-list endpoint); reuses `products/`'s `Pagination`/`SortSelect`/`ProductListEmpty`/`ProductListError` and adds a `getCategoryProducts` endpoint alongside `getProducts`, while `ProductCard`/`ProductGrid` turned out to be home-specific, not shared, once category's actual row-card mock was read directly. Variant-attribute and filterable-spec filter _controls_ are a deliberate, documented gap — `categorySpecifications`/`categoryVariants` are admin-only with no buyer-facing surface, so a real fix needs a new backend endpoint — see `buyer-app/AGENTS.md` for detail. Issue #78 / M2.21 (admin-app: category management, merged) adds `src/features/categories/` — mirroring brands' `getCategories`/`create·update·delete·updateStatus` pattern and `objectKey`-in/`url`-out image shape — with the two-level hierarchy rendered as one flat, indented list (not a nested tree/accordion) and a parent `<select>` filtered to root-level categories only as a client-side UX nicety (the four hierarchy-validation error codes remain backend-enforced and are surfaced as inline field-level text), plus the combined `CATEGORY_IN_USE` delete guard rendered verbatim — see `admin-app/AGENTS.md` for detail. Issue #74 / M2.17 (buyer-app: search results page, merged) adds `src/app/search/page.tsx` + `src/features/search/` — an active-keyword heading, a `relevance`-default sort (search-only, since home never sends `q`), and two distinct empty states (`FR-CAT-065`, `067`, `075`) — via a new `searchProducts` endpoint on the existing `productsApi`, reusing `M2.15`'s `ProductGrid`/`ProductCard`. Issue #75 / M2.18 (buyer-app: product detail page, merged) adds `src/app/products/[slug]/page.tsx` + `src/features/productDetail/` — gallery, a variant selector that updates price/availability/images from the already-cached response's `variants[]` array with no refetch (`FR-CAT-064`), grouped specifications, and an availability badge (`FR-CAT-056`, `059`, `063`, `064`, `084`) — via a new `getProductBySlug` endpoint; `PRODUCT_NOT_FOUND`/`INVALID_SLUG` render a dedicated not-found state. **buyer-app's M2 frontend build-out (M2.14–M2.18) is complete.** Issue #79 / M2.22 (admin-app: category specification editor, merged) adds `src/features/categorySpecifications/` — nested groups/fields, type-dependent inputs (`options` required for `enum`, forbidden for `text`), and an inline `SPECIFICATION_FIELD_IN_USE` guard naming the blocking count (`FR-CAT-030`, `031`, `035`, `092`) — against the identical `GET`/`PUT`/`PATCH`-only backend shape; bulk edits stay local until a "Save schema" full-replace `PUT`, while renaming a group, deleting a group/field, and toggling `filterable` on an already-persisted field persist immediately via the matching `PATCH` op. Issue #80 / M2.23 (admin-app: category variant-type editor, merged) adds `src/features/categoryVariants/` — near-identical shape to `#79` but flat (matched by `code`, not `name`) and, the one deliberate divergence, **no in-use guard on delete at all**, matching backend's `deleteAxis` exactly (`FR-CAT-036`–`038`). Issue #81 / M2.24 (admin-app: product list + read-only detail, merged) adds `src/features/products/` — the admin grid (all statuses, keyword/status/low-stock filters composing independently, pagination, an inline stock-only quick-edit, archive/restore) and a read-only detail view rendering every stored field and every variant regardless of `active` (`FR-CAT-005`, `006`, `008`, `011`, `013`, `043`, `045`, `050`, `053`); `brand`/`category` are resolved client-side against the already-fetched lists, since admin product reads never populate those refs. Issue #82 / M2.25 (admin-app: product create/edit form, merged) adds `src/features/products/productForm/` — the most complex admin screen: `sku` disabled on edit (`FR-CAT-004`), a 1–8/0–2 presigned-upload image widget with primary selection, specification inputs rendered from the selected category's live schema, and an embedded variant editor (one control per category variant axis, add/edit sharing one form) — with `SPECIFICATION_VALIDATION_FAILED`/`DUPLICATE_SKU`/`DUPLICATE_VARIANT_ATTRIBUTES` surfaced inline, verbatim (`FR-CAT-001`–`004`, `033`, `038`, `083`–`087`) — see `admin-app/AGENTS.md` for detail. **M2 (Product Catalog) frontend build-out is complete (buyer-app M2.14–18, admin-app M2.19–25) — the full Product Catalog feature, backend and both frontends, is done.** Issue #107 (admin-app: shell layout rework, merged) reworks `admin-app`'s shell — spun out of Issue #101's shared UI library into focused, reusable components: `Header`, `Sidebar`/`SidebarItems`, `Footer`, `MainSection` (composed by `AppShell`, replacing the single `SidebarNav`) — with a two-tier icon sidebar (`w-20` tablet icon-only with a hover/focus tooltip, `lg:w-50` desktop icon+label), an opt-in `breadcrumbs` prop on `PageHeader` (deliberately unused by default — `admin-app`'s flat nav has no real multi-level hierarchy to show; the existing `BreadcrumbHeading` back-link stays the pattern for drill-in pages), and a new `TableLayout` "common table layout" component built and unit-tested in isolation, not yet adopted by `BrandsPage`/`CategoriesPage`/`ProductsPage` — see `admin-app/AGENTS.md` for detail. Issue #114 (admin-app: sync products/brands/categories with backend contract, merged) brings `admin-app` in line with two backend-only amendments it had never caught up to — #102 (variant-only pricing: a product carries no `sku`/`images`/`mrp`/`discount`/`sellingPrice`/`stock`/`lowStockThreshold` of its own, every sellable unit is a variant) and #104 (admin sort moves to `?sortBy=`/`?orderBy=`, and brands'/categories' admin lists now paginate) — dropping every removed field from the product list/detail/create-edit form and embedded variant editor, and adding real pagination UI to Brands/Categories with every lookup call site switched to a `{limit:100}` query instead of assuming a full unpaginated array — see `admin-app/AGENTS.md` for detail. Issue #108 (admin-app: table, pagination, sortable header, and alert modal rework, merged) reworks `SortableHeader` into a tri-state sort control (asc → desc → none, replacing the old asc/desc-only toggle), `Pagination` into a three-zone layout (record range, windowed numbered pages with an active state, and a new optional page-size selector left unwired), and `Table` with an opt-in `fillHeight` + sticky-header mode (unused by any page yet); also replaces `ConfirmDialog` with a variant-aware `AlertModal` (`deleted`/`warning`/`confirm`, red/amber/green) across its 4 existing delete call sites. Merged on top of #114's own conflicting rewrite of `ProductList`/`BrandList`/`CategoryList`, so Brands/Categories/Products already render the reworked `Pagination`/`SortableHeader` components without further migration work — see `admin-app/AGENTS.md` for detail. Issue #119 (buyer-app: self-hosted fonts and branded loading state, merged) replaces `globals.css`'s render-blocking Google Fonts `@import` with `next/font/google`-self-hosted Inter/Plus Jakarta Sans, wired into the existing brand-token `@theme` block via a new `@theme inline` block referencing `next/font`'s runtime CSS variables, and adds a root `src/app/loading.tsx` — a placeholder brand-token spinner shown during route navigation, swappable for a real branded GIF later. Converting the app's established 100%-client-side RTK Query data fetching to server-side rendering (the highest-impact fix for the reported ~10s initial load) was investigated and deliberately left out of scope, since it would re-architect the pattern set by Issues #71–75 — see `buyer-app/AGENTS.md` for detail. Issue #120 (buyer-app: product cards navigate to product detail, merged) wraps `ProductCard` (Home/Search) and `CategoryProductCard` (Category) in `next/link`, so the whole card links to `/products/[slug]` — neither had any navigation wired up before. Issue #121 (backend: derive newest sort from the primary variant's createdAt, merged) fixes a real persistence bug found while investigating — `replaceVariants()` was resetting every variant's `createdAt` to "now" on every write via Mongoose's `$set`-based array replace, not just the one being touched, since fixed by routing that write through the raw MongoDB driver instead — then sorts the buyer "newest" listing by the primary (lowest-price active) variant's `createdAt` instead of the product's own top-level field, via a new shared `newestSortStages()` aggregation helper mirroring the existing `priceSortStages()` — see `backend/CLAUDE.md`'s Products section for detail. Issue #122 (buyer-app: reusable Header/Footer layout shell, merged) adds `src/components/layout/{Header,Footer,AppShell}.tsx` — a new top-level `src/components/` directory, peer to `app/`/`features/`/`store/` — translated directly from `mock-ui/buyer-app/`'s own header/footer (confirmed byte-identical across all 4 page mocks) and wired into `src/app/layout.tsx` so every route gets it automatically; the header's search box is a static link to `/search`, not a functional input, and the mock's Cart icon/"All Categories" dropdown are omitted since neither has a real feature or route behind it yet — see `buyer-app/AGENTS.md` for detail. Issue #123 (buyer-app: shared components/ui and layout primitives, merged) adds `src/components/ui/{PriceDisplay,NotFoundState,Checkbox,PriceRangeInput,SkeletonBox}.tsx` and `src/components/layout/PageContainer.tsx` — genuinely feature-agnostic primitives extracted only after a direct-code survey found each one duplicated across 2+ features, not moved speculatively; `PriceDisplay` takes pre-formatted `price`/`mrp` strings rather than raw numbers specifically so `components/ui/` never imports from `src/features/*`. Also fixes `ProductDetailContent`'s hand-rolled duplicate of `ProductListError`'s error state (which gained an optional `message` prop instead) and deletes the now-superseded `ProductNotFound.tsx`/`CategoryNotFound.tsx` — see `buyer-app/AGENTS.md` for detail. SRS v0.3–v0.9 and v0.10 are all spec-drafted (PR #135, merged): `docs/srs/features/0.3-authentication.md` (`FR-AUTH-001`–`045`, Google/OTP buyer sign-in and admin password+OTP), `0.4-shopping-cart.md` (`FR-CART-001`–`021`, cart access and item addition now require authentication), `0.5-orders.md` (`FR-ORD-001`–`033`, including a buyer address book), `0.6-payments.md` (`FR-PAY-001`–`028`), `0.7-dashboard.md` (`FR-DASH-001`–`024`, amended for v0.10's out-of-stock count), `0.8-backend-nfr.md` (`FR-NFR-BE-001`–`028`), `0.9-frontend-nfr.md` (`FR-NFR-FE-001`–`026`), and `0.10-inventory-management.md` (`FR-INV-001`–`012`, per-warehouse stock tracking reinstating the inventory concept Issue #102 removed, with cart-time stock allocation forward-referencing v0.4's not-yet-built cart module) — each covering all 10 template sections in `docs/srs/SRS.md` §4's format. `docs/srs/SRS.md`'s version history, feature index, and traceability matrix are updated accordingly (Final Consolidated SRS renumbered to v1.1 to make room for v0.10), and `docs/milestone.md` now carries a matching M10 (Inventory Management) / M11 (Launch Readiness) split, with M3–M10 all "Spec drafted — issues pending." None of M3–M11 has a GitHub milestone or issues opened yet — that's the next step per `SRS.md` §5.

SRS v0.3 (Authentication) implementation is underway, opened as Issues #139–#149 under Milestone #4 (PR #198). Issue #139 / M3.1 (backend: buyer passwordless authentication) shipped once (PR #204) but was reverted the same day alongside admin-auth issues #140/#141 (PR #208): Better Auth's default cookie session doesn't reliably survive a cross-site `fetch` between `buyer-app` (Vercel) and `backend` (Render) — no shared parent domain, and Safari blocks third-party cookies outright — so sign-in passed CI and worked locally while being broken on every real deployed preview. Redone in PR #209 with the same scope (Google OAuth, Google One Tap, and email OTP all resolving to one `users` collection by email, `FR-AUTH-001`–`008`, `023`) plus the fix: Better Auth's `bearer` plugin now runs alongside the existing cookie, returning the session token via a `set-auth-token` response header for a client to resend as `Authorization: Bearer <token>` — documented as SRS amendment `FR-AUTH-046`. Admin password + OTP sign-in and password reset (#140/#141) remain reverted, not yet redone. See `backend/CLAUDE.md`'s Authentication (Buyer) section for full implementation detail.

Admin password + mandatory OTP sign-in (Issue #140 / M3.2) is in place, also redone: it was reverted alongside #139's original attempt (PR #208) for the identical cross-domain-cookie reason, and stayed unredone through #139's own redo (#209) since `admin-app`'s cross-domain shape from `backend` needed its own verification before shipping. Merged in PR #211: `src/lib/auth.ts` adds Better Auth's `emailAndPassword` + `twoFactor` plugins for a two-step password-then-mandatory-OTP admin challenge (`FR-AUTH-009`–`018`, `030`) — a correct password alone never establishes a session, reusing the identical `emailOTP`/`sendOtpEmail`/envelope-bridge machinery #139 already built. Bearer-token support needed no new code at all, since `bearer()` is already registered globally and covers any session the instance establishes. A new `src/scripts/seed/createAdminUser.ts` (`npm run seed:admin --workspace backend`) provisions a real admin account outside of tests, with temporary hardcoded credentials rather than new env vars. `X-Admin-Key`/`adminAuth.ts` and rate limiting (`FR-AUTH-040`/`041`) are both deliberately untouched, left to follow-up issues. See `backend/CLAUDE.md`'s Authentication (Admin) section for full implementation detail.

Admin self-service password reset (Issue #141 / M3.3) is in place, built on Better Auth's own reset-password support rather than hand-rolled token logic: `POST /api/auth/request-password-reset` (`FR-AUTH-019`–`022`) — Better Auth's real endpoint name, confirmed via CI after both the SRS's illustrative `/forgot-password` and the plausible-sounding `/forget-password` 404'd — and `POST /api/auth/reset-password`. A reset request always returns the identical generic response regardless of registered-admin, registered-buyer, or unregistered email; the email itself is only actually sent for a non-buyer account. Session invalidation on reset (`FR-AUTH-022`) is built explicitly via a `hooks.before` matcher and a new `src/lib/passwordResetTokens.ts` tracking collection (mirroring `uploads.model.ts`'s `presignedUploads` precedent). See `backend/CLAUDE.md`'s Admin Password Reset section for full implementation detail.

Admin account provisioning (Issue #142 / M3.4) is in place: a super-admin can now create, list, and update further admin accounts over REST (`POST`/`GET`/`PATCH /api/admin/users`), gated by `backend/src/middleware/requireRole.ts` — the first identity-aware session guard in this codebase, layered alongside the existing `X-Admin-Key` router guard rather than replacing it. A newly created admin is routed through #141's real password-reset flow, never an auto-established session or an exposed temporary password, and the full password+OTP sign-in from #140 still applies with no bypass. `backend/src/scripts/seed/admin.ts` (#140's hardcoded stopgap) is replaced by an env-var-driven `superAdmin.ts` (`npm run seed:super-admin --workspace backend`). See `backend/CLAUDE.md`'s Admin Account Provisioning section for full implementation detail. Issue #143 / M3.5 (`FR-AUTH-031`–`035`) is in place: the temporary `X-Admin-Key` shared-secret guard (`backend/src/middleware/adminAuth.ts`, live since #25/M2.1) is replaced entirely by a real session+role guard — `backend/src/middleware/rbac.ts` consolidates #142's `requireRole.ts` into one guard taking an array of allowed roles, wired onto every catalog route (products, categories, brands, categorySpecifications, categoryVariants, uploads) with no route/controller/service business logic changed. It also backfills `createdBy`/`updatedBy` from the authenticated admin's id on every brand/category/product write. See `backend/CLAUDE.md`'s RBAC Guard & createdBy/updatedBy Backfill section for full implementation detail. Issue #144 / M3.6 (`FR-AUTH-036`–`039`) is in place: a new `backend/src/modules/account/` adds session-scoped "my own account" endpoints — `GET`/`PATCH /api/account/profile` (buyer-only session, name/phone) and `POST /api/account/change-password` (admin-only session, current-password required) — with `backend/src/middleware/rbac.ts`'s role guard widened to also accept `"buyer"` so the profile routes reuse the same session+role middleware every admin route already uses. A successful password change invalidates every other session for that admin while leaving the one making the change intact; the real fix needed was that a client's bearer token is `<rawToken>.<signature>` while the `session` collection only stores the raw part, so `backend/src/utils/sessions.ts`'s `revokeSessionsForUser` now accepts an optional token to exclude, matched against that raw segment. See `backend/CLAUDE.md`'s Account Self-Service section for full implementation detail. Abuse mitigation and the `FR-AUTH-045` error-code contract are in place (Issue #145 / M3.7): five independent Redis-backed rate limiters (`ioredis`/`rate-limiter-flexible`, this codebase's first Redis dependency) split across two layers — Better Auth's own native per-request limiter (IP-only) and a new `hooks.before` hook (per-email, for the paths whose body carries one) — plus new enforcement making `ACCOUNT_DEACTIVATED` and `OTP_REQUIRED` real for the first time. See `backend/CLAUDE.md`'s Abuse Mitigation & Error Contract section for full implementation detail.

Issue #146 / M3.8 (buyer-app: sign-in via Google/One Tap/OTP and session bootstrap, merged PR #221) is `buyer-app`'s first auth surface — no password field anywhere. `src/features/auth/` (`GoogleSignIn.tsx`, an auto-triggered One Tap prompt plus a rendered Google button; `OtpSignIn.tsx`, email → OTP entry with a 30s resend cooldown; `SignInContent.tsx`, composing both and redirecting home once a session exists) plus `src/app/sign-in/page.tsx`; a `getSession` RTK Query endpoint (`src/features/auth/api.ts`, hitting the real `GET /api/auth/get-session`, not the SRS's illustrative `/api/auth/session`) bootstraps header/nav state via a new `src/components/layout/AuthStatus.tsx`, wired into `Header.tsx`. A bearer token from the `set-auth-token` response header (Issue #139's cross-domain mechanism) persists in `localStorage` (`tokenStorage.ts`) and is attached to every request via `store/api.ts`'s `prepareHeaders`. `GOOGLE_ACCOUNT_IS_ADMIN`/`INVALID_OTP`/`OTP_EXPIRED` render distinct inline messages on the OTP verify step. Two real bugs were found and fixed during a later audit (no separate issue): the invalid-OTP branch checked for a code string, `OTP_INVALID`, that doesn't match Better Auth's actual `emailOTP` plugin code, `INVALID_OTP` (confirmed directly against the installed `better-auth` package source), so a wrong OTP code never triggered the custom message against a real backend — the test suite's own MSW mocks had hardcoded the same wrong string, masking the gap; and `handleResendOtp` caught its thrown error under the wrong shape, always showing a generic resend-failure message regardless of the real reason. See `buyer-app/AGENTS.md`'s Feature-based structure section for full implementation detail.

Issue #147 / M3.9 (buyer-app: account profile) adds the buyer-facing half of `M3.6`'s backend profile endpoint (`GET`/`PATCH /api/account/profile`), previously unbuilt: `src/features/account/` (`ProfileForm.tsx` — `name`/`phone` edit, email read-only; `AccountContent.tsx` — a `getSession`-driven guard, the inverse of `SignInContent.tsx`'s own redirect: no session sends the buyer to `/sign-in` instead of home), rendered by `src/app/account/page.tsx`. `api.ts`'s new `getProfile`/`updateProfile` endpoints reuse the existing `"Session"` RTK Query tag rather than a new one, so a successful edit also refreshes `AuthStatus.tsx`'s header name for free. `AuthStatus.tsx`'s signed-in name is now a link to `/account` — previously plain text, unreachable. No mock-ui wireframe exists for this screen; its layout follows `SignInContent.tsx`'s established form-card style directly. See `buyer-app/AGENTS.md`'s Feature-based structure section for full implementation detail.

A real startup-ordering bug was found and fixed on the Issue #146 branch (no separate issue — surfaced as `npm run seed:super-admin` crashing with `Cannot read properties of undefined (reading 'collection')`): `backend/src/lib/auth.ts` builds its Better Auth instance at module-load time via `mongodbAdapter(mongoose.connection.db!, ...)`, capturing `mongoose.connection.db` as a plain value rather than a live reference. Both `backend/src/index.ts` (statically importing `./app`) and `backend/src/scripts/seed/superAdmin.ts` (statically importing `createAdminUser.ts` → `@/lib/auth`) resolved that import before `connectDB()` had run, freezing the adapter on `undefined` for the process's lifetime and crashing every Mongo query Better Auth made — most likely including real deployed sign-in requests, not just the seed script. Both entrypoints now `await connectDB()` first, then dynamically `import()` whatever transitively pulls in `auth.ts` — matching the pattern `__tests__/testHelpers/adminSession.ts` already used for the identical reason. See `backend/CLAUDE.md`'s Startup section for full implementation detail.

Issue #223 (`ci: add build verification and fix current TS build errors`, infra, no milestone, merged PR #225) closes a real CI gap: `.github/workflows/ci.yml` had only ever run `lint`+`test`, never `npm run build` — so a real `tsc` failure could merge to `main` undetected, which is exactly what happened, breaking Render's backend deploy with 7 TypeScript errors (four stale `createdBy`/`updatedBy` test-mock literals left over from #143's `createdBy`/`updatedBy` backfill, TS2783; three `exactOptionalPropertyTypes` violations in `auth.ts`/`account.controller.ts`/`adminUsers.controller.ts`, TS2379 — all fixed with zero behavior change). A new `build` job, matrixed over `[backend, buyer-app, admin-app]` mirroring the existing `test` job's shape, is now a required status check on `main` alongside the 4 pre-existing ones (`lint`, `test (backend/buyer-app/admin-app)`). Investigating this surfaced a second, unrelated pre-existing bug: `buyer-app`'s Vercel deploys had been silently broken, since `vitest.setup.ts`'s `dotenv` import only ever resolved via npm workspaces hoisting `backend`'s own `dotenv` devDependency (never declared as `buyer-app`'s own) — hoisting that doesn't happen under Vercel's isolated per-workspace install. Fixed by declaring `dotenv` as `buyer-app`'s own devDependency.

A second, unrelated production-crash bug (no separate issue) was found and fixed on the `fix/render-deploy-app-listen` branch: `backend/src/index.ts`'s dynamic `import("./app.js")` after `connectDB()` (see the Issue #146 startup-ordering fix above) resolves differently depending on the module loader — Vitest/`tsx` unwrap it in one level, but the real compiled build Render runs (`node dist/src/index.js`, this package's `"type": "commonjs"`) has Node's ESM loader wrap CommonJS's `module.exports` as `namespace.default` verbatim, leaving `app.listen` undefined and crashing every deploy right after `MongoDB connected` — confirmed directly against Render's deploy logs. Fixed by detecting which shape came back and unwrapping accordingly; verified by actually running the compiled `node dist/src/index.js` locally end-to-end, not just by `tsc` succeeding, since #223's new `build` CI job would not have caught this either. See `backend/CLAUDE.md`'s Startup section for full detail.

Issue #224 (`M3.12 — backend: Postman API manuals for Authentication module`, M3 milestone, merged PR #229) backfills `docs/postman/` for the three backend modules M3 shipped with zero Postman coverage — `auth`, `adminUsers`, `account` (Issues #139–#142, #144): a new `docs/postman/authentication/` folder (`auth.api.md`, `adminUsers.api.md`, `account.api.md`), mirroring `docs/postman/product-catalog/`'s exact per-module structure (scope paragraph, one-time setup, per-endpoint request/response/error-case walkthroughs, an Error Code Reference table), plus a `docs/postman/README.md` index update (a new Authentication files table and full endpoint index, 18 endpoints). Also states two going-forward conventions in `README.md` for future issues: every new/updated backend endpoint gets its matching Postman doc in the same PR, and every `docs/postman/<feature>/` folder follows `product-catalog/`'s structure exactly. Flags, but deliberately doesn't fix (out of scope for this issue), a real pre-existing gap found while writing it: `docs/postman/product-catalog/*.api.md` still documents every admin endpoint as guarded by the retired `X-Admin-Key` header, never updated since Issue #143/M3.5 replaced that guard with real session/RBAC auth — left as a follow-up.

Issue #148 / M3.10 (admin-app: password→OTP sign-in flow and route guard, merged PR #237) replaces `admin-app`'s throwaway `X-Admin-Key` prompt (#76/M2.19) entirely with real Better Auth session-based sign-in — mirroring `buyer-app`'s own `src/features/auth/` shape (#146/M3.8) but for admin's two-step password-then-mandatory-OTP challenge: `PasswordSignIn.tsx`/`OtpVerify.tsx`/`SignInContent.tsx`, a bearer token persisted via `tokenStorage.ts` (`localStorage`, read off `set-auth-token` at the `verifyOtp` step), and `RequireAuth.tsx` — a React Router layout-route guard redirecting to `/sign-in` with no session and rendering a distinct no-access state for a non-admin role (a coarse, client-side role check only — no per-page role gating exists in this app). A real cross-domain bug was found and fixed alongside it, not just a frontend gap: the two-factor plugin's own pending-challenge cookie (the intermediate state between the password and OTP steps) inherited Better Auth's default `SameSite=Lax`, which a cross-site fetch from `admin-app` (Vercel) to `backend` (Render) never sends — `backend/src/lib/auth.ts` gained a `BETTER_AUTH_URL`-gated `advanced.defaultCookieAttributes` override (`SameSite=None; Secure`) for exactly this window, mirroring how `bearer()` already fixed the identical class of bug for the final session itself (#139/#208). See `admin-app/AGENTS.md`'s Session-based sign-in and route guard section and `backend/CLAUDE.md`'s Authentication (Admin) section for full detail.

Issue #149 / M3.11 (admin-app: admin user management and self-service password change, merged PR #239) builds the `admin-app` frontend for two backend modules that had shipped with zero UI consumer since M3.4/M3.6 — `adminUsers` (#142) and `account` (#144). It adds this app's first per-page role guard, `RequireRole.tsx` (nested inside the existing coarse `RequireAuth`), gating a new super-admin-only `/admin-users` page — list, create with no password field (the backend routes a new admin through a real password-reset email instead), and inline role/status editing that deliberately hides both controls on the signed-in admin's own row, matching the backend's `CANNOT_MODIFY_OWN_ACCOUNT` guard exactly rather than just the issue's narrower "no delete/deactivate control" wording. A new `/account` page gives any admin role self-service password change. `navItems.ts`/`Sidebar.tsx` gain this app's first role-aware nav filtering, hiding "Admin Users" for non-super-admins, and `Footer.tsx` now reads the real signed-in session instead of a static placeholder. See `admin-app/AGENTS.md`'s Admin user management, self-service password change, and role-aware nav section for full detail. **M3 (Authentication) shipped complete at Issue #149** — three further bug-fix/improvement issues have since been filed and merged against the same M3 milestone (numbered M3.13+, continuing past #149's own M3.11), not tied to any new SRS feature: Issue #241 / M3.13 (merged PR #246) removes `seed:super-admin`'s hardcoded fallback (`admin@techcart.dev` / `TechCart@Admin123`) — `SUPER_ADMIN_EMAIL`/`NAME`/`PASSWORD` are now required with no default, failing fast with a clear message naming whichever var is missing, since a real production-bootstrap script silently provisioning a well-known account was judged a real risk. Issue #245 / M3.17 (merged PR #247) adds a second, deliberately different seed script, `npm run seed:users` (`backend/src/scripts/seed/seedUsers.ts`) — idempotent local-dev/test *sample* data (3 buyer fixtures upserted directly against `users`, since buyers are passwordless; one `catalog-manager` and one `order-manager` via the existing `provisionAdminUser()`), with hardcoded fixture credentials that are fine specifically because this script is never used for a real deployment, unlike `seed:super-admin`. Issue #242 / M3.14 (merged PR #248) removes Resend outright (TechCart doesn't own a verified sending domain it requires) — Mailtrap becomes the sole email provider in every environment, `src/externalService/resend.ts` renamed to `mailer.ts` — and additionally fixes buyer and admin sign-in/2FA OTP codes to `"123456"` in every environment including production, so manual testing never needs real inbox access: buyer sign-in via `emailOTP`'s real `generateOTP` override (fully consistent, stored and emailed code always match), admin 2FA via a new `databaseHooks.verification.create.before` hook since `twoFactor` has no equivalent override in `better-auth@1.7.1`'s public API (confirmed by reading the installed package source) — a documented asymmetry where verification always accepts `"123456"` but the actually-emailed code stays real/random. This is a deliberate, explicit, pre-launch-only security tradeoff (a fixed OTP is a known, guessable constant, not a real secret), documented in `backend/CLAUDE.md` and an SRS amendment (`docs/srs/features/0.3-authentication.md`) rather than left implicit — must not carry into a real production launch. Issue #243 / M3.15 (merged PR #251) fixes a real `Error 400: origin_mismatch` a user hit signing in via Google on buyer-app — confirmed not a code bug (`GoogleSignIn.tsx`'s `NEXT_PUBLIC_GOOGLE_CLIENT_ID` wiring is correctly env-driven, no hardcoded origin/client ID anywhere), but an undocumented operational gap: nothing told anyone which origins to register in Google Cloud Console. Adds `docs/architecture.md` §7.1, distinguishing **Authorized JavaScript origins** (the client-side One Tap/button flow, no wildcard support unlike this repo's own backend `CORS_ORIGINS`) from **Authorized redirect URIs** (the separate field for the full-page OAuth redirect, pointing at the backend not buyer-app) — with a cross-reference from `backend/CLAUDE.md`'s Authentication (Buyer) section (`backend/AGENTS.md` is a separate, shorter doc with no Authentication section of its own — unlike `admin-app`/`buyer-app`, `backend`'s `CLAUDE.md` isn't just a pointer to `AGENTS.md`). Real domains are deliberately left as generic placeholders, not hardcoded. One further M3.x issue from this same follow-up batch — #244/M3.16 (regroup the `auth`/`adminUsers`/`account` modules under one `authentication` parent across all three workspaces) — is filed but not yet implemented.

Issue #262 / M3.24 (buyer-app: verify/rewire auth and account against the custom backend contract, merged PR #278) is a verification pass over `buyer-app`'s `src/features/authentication/{auth,account}/` against the hand-rolled session backend that replaced Better Auth entirely (Issues #257–#261). The contract held: routes, request bodies, the `set-auth-token` bearer flow, and the `GOOGLE_ACCOUNT_IS_ADMIN`/`INVALID_OTP`/`OTP_EXPIRED`/`RATE_LIMITED` error codes all still line up. One latent frontend bug surfaced and was fixed: `getSession`'s `transformResponse` assumed the signed-out session came back as `data: { user: null }` and threw on the backend's actual `data: null`, leaving the header with no "Sign in" link and `/account` un-redirected for signed-out users — every MSW mock had encoded the wrong shape, masking it. The test mocks were also corrected (`GOOGLE_ACCOUNT_IS_ADMIN` is a 403, resend rate-limiting is `RATE_LIMITED`) and stale Better Auth references dropped from `OtpSignIn.tsx` and `buyer-app/AGENTS.md`. No visible UX or route change. See `buyer-app/AGENTS.md`'s Feature-based structure section for full detail.

Issue #263 / M3.25 (admin-app: verify/rewire auth, route guards, and admin-user features against the custom backend contract, merged PR #280) is the `admin-app` mirror of #262 — a verification pass over `src/features/authentication/{auth,adminUsers,account}/` against the same custom session backend. The contract held: routes, request bodies, the `set-auth-token` bearer flow, every `describeAuthError.ts` code (`INVALID_EMAIL_OR_PASSWORD`/`ACCOUNT_DEACTIVATED`/`RATE_LIMITED`/`INVALID_CODE`/`OTP_HAS_EXPIRED`/`INVALID_TWO_FACTOR_COOKIE`/`INVALID_CURRENT_PASSWORD`), and — the one point the issue specifically re-checked — the cross-site pending-challenge cookie fix, which has its equivalent in `backend/src/lib/adminChallenge.ts` (`SameSite=None; Secure` gated on `APP_BASE_URL`). `admin-app`'s `getSession` `transformResponse` already null-guards, so it has no equivalent of #262's `data: null` crash. One pre-existing bug surfaced and was fixed: `OtpVerify.tsx` never called `POST /two-factor/send-otp` when the OTP step opened (only the 30s-delayed "Resend" button did), so after the password step no code was minted server-side and `verify-otp` failed until the user waited out the cooldown — masked since Issue #148 by the hardcoded `"123456"` OTP and a test that mocked `verify-otp` without `send-otp`. Now fired once on mount. MSW mocks corrected to `data: null` shapes; stale Better Auth references dropped. No visible UX or route change. See `admin-app/AGENTS.md`'s Session-based sign-in and route guard section for full detail.

Issue #264 / M3.26 (docs: update Postman manuals for the custom auth endpoints, merged PR #282) is a docs-only re-verification of `docs/postman/authentication/{auth,adminUsers,account}.api.md` against `backend/__tests__/authentication/**` after the Better Auth → custom session/OTP engine migration, plus closing the deferred Issue #224 gap. `auth.api.md`: scope/prereqs rewritten for the hand-rolled engine (`JWT_SECRET`/`APP_BASE_URL`, no `BETTER_AUTH_*`/`betterAuthHandler.ts`); buyer sign-in / One Tap responses corrected to the bare session-user object (was Better Auth's `{user,token,redirect}`); the retired `TOO_MANY_ATTEMPTS` case and `AUTH_ERROR` fallback row removed; the `POST /sign-in/social` + `GET /callback/google` walkthroughs removed (never rebuilt on the custom engine, now 404); `get-session` → `data:{user}`, `sign-out` → `data:null`, `request`/`reset-password` → `data:{status:"ok"}`. `account.api.md`/`adminUsers.api.md`: light custom-engine notes. All six `docs/postman/product-catalog/*.api.md` + `README.md`: every retired `X-Admin-Key`/`admin_api_key` reference swapped to `Authorization: Bearer {{admin_access_token}}` from the admin sign-in flow, `UNAUTHORIZED`/`adminAuth.ts` error rows → `UNAUTHENTICATED` + `FORBIDDEN` / `src/middleware/rbac.ts`. **M3 (Authentication) is now fully complete** — Issues #139–#149 plus the M3.12–M3.27 follow-up batch are all merged.

`RATE_LIMITING_ENABLED` toggle + optional `REDIS_URL` (infra, no milestone, merged PR #284): `backend`'s auth abuse limiters (`FR-AUTH-040`–`044`, `src/lib/rateLimit.ts`) gain a `RATE_LIMITING_ENABLED` env var (default `"true"`) — set `"false"` and `consume()` short-circuits to `{ allowed: true }`, so `429 RATE_LIMITED` never fires (a local-dev / manual-Postman-testing escape hatch; `render.yaml` pins it `"true"`). `REDIS_URL` is now **optional**: with it the limiters are Redis-backed and shared across instances, without it (or under test) they fall back to per-instance `RateLimiterMemory` — still enforced when the toggle is on. The backend boots with neither var. Default config is unchanged behaviour. See `backend/CLAUDE.md`'s Abuse Mitigation & Error Contract section for full detail.

**M4 (Shopping Cart) is complete** — Issues #150–#153 under Milestone #5, all merged (SRS v0.4, `FR-CART-001`–`021`). Issue #150/#151 / M4.1–M4.2 (backend, PR #289): a new flat `backend/src/modules/cart/` module — one lazily-created persistent cart per authenticated buyer (`carts` collection, unique on `user`, variant-only line items), `GET`/`POST`/`PATCH`/`DELETE /api/cart[/items[/:variantId]]` all `rbac(["buyer"])`-guarded; dedupe-by-variant, quantity 1–10 rejected-not-clamped; every read resolves pricing/availability **live** from the referenced embedded variant (deactivated variant / unpublished product / over-cap line → `unavailable`, kept in `items`, out of `subtotal`, still in `itemCount`); empty cart is a `200` shape, never `404`. New `products.repository.ts` lookups `findByVariantId`/`findByVariantIds`; `PublicProductListItem` gains `defaultVariantId`. Issue #152/#153 / M4.3–M4.4 (buyer-app, PR #290): `buyer-app/src/features/cart/` + `/cart` route — cart page, the shared `AddToCartButton` (cards + PDP, auth-gated, "Go to Cart" from cached cart), and `components/layout/CartIndicator` in the header (badge + mini-cart dropdown; unauthenticated → sign-in link, no badge). `cart/api.ts` is the workspace's first optimistic-`updateQueryData` consumer; `store/api.ts` gains a `"Cart"` tag; `SignInContent` honours a same-origin `?redirect=` param. Postman: `docs/postman/shopping-cart/cart.api.md`. `v0.4.0` tagging follows separately.

**Note:** this file's status log was not kept in sync through M5–M10 (Orders, Payments, Dashboard, Inventory) — `CLAUDE.md`'s own "Current status" section is the up-to-date record for that span; this file resumes below with Issue #329, the first status-sync after that gap.

Issue #329 (`docs: Postman manuals for M5–M10 endpoints + refresh stale docs`, docs-only, merged PR #332) backfills `docs/postman/` for every backend module M5–M10 shipped since M4 with zero Postman coverage: four new folders — `order-management/` (`addresses.api.md`, `orders.api.md`, `ordersAdmin.api.md` — Issues #154–#158), `payments/` (`payments.api.md`, `webhooks.api.md` — Issues #164–#167, with the paise-vs-rupee unit boundary called out and a literal HMAC-signature-computation walkthrough for testing the webhook by hand), `dashboard/` (`dashboard.api.md` — Issues #171–#172, the reciprocal `order-manager`/`catalog-manager` role split across its four routes), and `inventory/` (`warehouses.api.md`, `inventory.api.md` — Issue #189) — each mirroring `product-catalog/`'s established structure exactly. Also refreshes three docs that had drifted since their own features shipped: `product-catalog/products.api.md` (reinstates `availability`/`?inStock=true`, Issue #189/M10.1, previously documented as permanently removed by #102), `authentication/account.api.md` (adds the missing `GET /api/account/dashboard`, Issue #173), and `shopping-cart/cart.api.md` (adds the missing `409 INSUFFICIENT_STOCK` case, Issue #190/M10.2). `docs/postman/README.md` gains matching index sections and corrected per-domain endpoint totals (Authentication 16→17). Every documented route/shape was verified directly against `backend/__tests__/**` and each module's own `*.routes.ts`/`*.controller.ts`/`*.service.ts`, not summarized from memory.

## Development process — read before starting any feature

This repo is built strictly feature-by-feature against a versioned SRS, not from a single upfront spec:

```
Feature → Update SRS → Add to Milestone → Add to Issue → Implement Code
```

- The SRS lives at `docs/srs/SRS.md` (version history, scope, feature index, per-feature template) with detail docs per feature at `docs/srs/features/<version>-<feature>.md` as they're written.
- Before implementing any feature, its SRS doc must exist and follow the template in `docs/srs/SRS.md` §4 — functional requirements numbered `FR-<CODE>-<NNN>`, plus a feature-scoped Baseline NFR checklist. Full system-wide NFRs are deliberately deferred to SRS v0.8/v0.9, but that section exists so each feature still gets a lightweight security/data/error-handling pass rather than none at all.
- Feature order follows `docs/srs/SRS.md` §3's Feature Index unless explicitly reprioritized.
- Each issue gets a branch `feature/<issue-number>-<scope>`, cut from `main` and squash-merged back into `main` (no `develop` branch). See `docs/architecture.md` §8 for commit format and release tagging.
- Before marking an issue complete: push the branch and open a PR into `main` (squash-merge, per the convention above); once merged, sync the status sections of `AGENTS.md`, `CLAUDE.md`, and `docs/architecture.md` §10 to reflect the new state in a small follow-up PR (branch protection on `main`, Issue #10, rejects direct pushes — even doc-only syncs go through a PR now; `README.md` deliberately stays status-free — see its own Documentation section for where status actually lives), then close the GitHub issue referencing the merge.
- Whenever changes are made within a scaffolded workspace (`backend/`, and later `buyer-app/`/`admin-app/` once they exist), update that workspace's own `CLAUDE.md`/`AGENTS.md` (see the workspace-docs mapping in Architecture below) to reflect them.

## Architecture

`docs/architecture.md` has the full system diagram, layered backend structure, per-route rendering strategy for `buyer-app`, and the data/collection map. This is a plain **npm workspaces** monorepo — three workspaces flat at repo root (`backend/`, `buyer-app/`, `admin-app/`), no `apps/` nesting and no shared `packages/` directory. Points worth internalizing before touching code:

- **One backend, two frontends** — all business logic lives in `backend/`; neither `buyer-app/` nor `admin-app/` duplicates it. This is why the backend is a plain Express service rather than Next.js API routes (`admin-app` isn't a Next.js app and would need the logic duplicated).
- **No shared validation package** — `backend/` owns its Zod schemas internally; `buyer-app`/`admin-app` do their own separate client-side validation. This is a deliberate simplification (matches the sibling `LeafFlow` project), not an oversight — treat `backend`'s validation as the one that actually enforces correctness.
- **Backend layering** — implementation detail owned by `backend/` itself; not yet locked in as a root-level architectural decision. See the workspace-docs mapping below (`backend/CLAUDE.md`, `backend/AGENTS.md`, `backend/docs/architecture.md`) for the current structure.
- **Auth/RBAC** — Better Auth issues sessions for both apps; Admin routes check role claims (`catalog-manager`, `order-manager`, `super-admin`) server-side on every request — never rely on a client-side route guard alone.
- **Error contract** — every backend error responds `{ "success": false, "code": "string", "message": "string" }`.
- **Workspace-level docs** — once a workspace is scaffolded, it may have its own `CLAUDE.md`/`AGENTS.md`/`docs/architecture.md` for implementation-level detail. Those never override the root-level decisions in this file or `docs/architecture.md`.

  | Workspace    | Own docs                                                                       |
  | ------------ | ------------------------------------------------------------------------------ |
  | `backend/`   | `backend/CLAUDE.md`, `backend/AGENTS.md`, `backend/docs/architecture.md`       |
  | `buyer-app/` | `buyer-app/CLAUDE.md`, `buyer-app/AGENTS.md`, `buyer-app/docs/architecture.md` |
  | `admin-app/` | `admin-app/CLAUDE.md`, `admin-app/AGENTS.md`, `admin-app/docs/architecture.md` |

## Git conventions

- Do not add a `Co-Authored-By: Claude` (or any other agent) trailer to commit messages in this repository.
- Conventional Commits: `type(scope): message (Issue #N)` — types `feat, fix, test, chore, docs, refactor`; scopes `backend, buyer-app, admin-app, ci, infra`.
